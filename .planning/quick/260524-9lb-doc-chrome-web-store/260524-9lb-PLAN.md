---
phase: quick-260524-9lb-doc-chrome-web-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - doc/chrome-web-store/README.md
  - doc/chrome-web-store/assets/templates/*
  - scripts/screenshot-assets.mjs
  - .store-assets/*
autonomous: true
requirements:
  - QUICK-260524-9LB
must_haves:
  truths:
    - "仓库新增 `doc/` 目录，且 Chrome Web Store 相关材料可从一个固定入口集中找到。"
    - "现有会被仓库文档或校验脚本引用的上架文案路径不会在本 quick task 中被贸然打断。"
    - "截图模板迁移后，现有生成命令仍可产出 CWS 素材图。"
  artifacts:
    - path: "doc/chrome-web-store/README.md"
      provides: "Chrome Web Store 物料总入口与归档规则"
      min_lines: 20
    - path: "doc/chrome-web-store/assets/templates/"
      provides: "集中后的 CWS 截图与 promo HTML 模板"
      contains: "screenshot-1-capture.html"
    - path: "scripts/screenshot-assets.mjs"
      provides: "从新模板目录生成素材截图的脚本"
      contains: "doc/chrome-web-store/assets/templates"
  key_links:
    - from: "doc/chrome-web-store/README.md"
      to: "STORE-LISTING.md"
      via: "index link with ownership note"
      pattern: "STORE-LISTING\\.md"
    - from: "doc/chrome-web-store/README.md"
      to: "PRIVACY.md"
      via: "index link with ownership note"
      pattern: "PRIVACY\\.md"
    - from: "scripts/screenshot-assets.mjs"
      to: "doc/chrome-web-store/assets/templates"
      via: "SRC constant"
      pattern: "chrome-web-store/assets/templates"
---

<objective>
新增 `doc/` 目录，并把 Chrome Web Store 上架材料整理成一个低风险、可持续维护的集中入口。

Purpose: 用户要“集中放置上架物料”，但当前 `STORE-LISTING*.md`、`PRIVACY*.md`、`public/icon/`、`scripts/screenshot-assets.mjs` 已被 README、校验脚本或构建路径引用。此 quick task 优先最小变更：先建立 `doc/chrome-web-store/` 作为集中入口，只迁移低风险模板资产，不打断现有引用链。

Output: 新的 `doc/chrome-web-store/` 目录、集中后的模板资产目录、以及仍可工作的素材截图脚本。
</objective>

<context>
@/Users/seven/data/coding/projects/seven/web2chat/.planning/STATE.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/PROJECT.md
@/Users/seven/data/coding/projects/seven/web2chat/CLAUDE.md
@/Users/seven/data/coding/projects/seven/web2chat/STORE-LISTING.md
@/Users/seven/data/coding/projects/seven/web2chat/STORE-LISTING.en.md
@/Users/seven/data/coding/projects/seven/web2chat/PRIVACY.md
@/Users/seven/data/coding/projects/seven/web2chat/PRIVACY.zh_CN.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/milestones/v1.0-phases/07-distribution/07-VERIFICATION.md

<reference_risk_notes>
- `README.md` / `README.en.md` 直接链接 root 下的 `PRIVACY*.md`。
- `scripts/verify-readme-anchors.ts` 显式要求 root 下存在 `PRIVACY.md` 与 `PRIVACY.zh_CN.md`。
- `STORE-LISTING*.md` 彼此相对链接，并引用 root 下的 `PRIVACY.md`。
- `.store-assets/` 目前只被 `scripts/screenshot-assets.mjs` 引用，迁移风险最低。
- `public/icon/` 很可能参与扩展构建与打包；本 quick task 只做索引，不移动。
</reference_risk_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the doc hub and define safe ownership boundaries</name>
  <files>doc/chrome-web-store/README.md</files>
  <action>
    Create the new `doc/chrome-web-store/` hub under a top-level `doc/` directory. In `doc/chrome-web-store/README.md`, inventory every reusable CWS artifact already present in the repo: `STORE-LISTING.md`, `STORE-LISTING.en.md`, `PRIVACY.md`, `PRIVACY.zh_CN.md`, `scripts/screenshot-assets.mjs`, `public/icon/`, and `.planning/milestones/v1.0-phases/07-distribution/07-VERIFICATION.md`.

    State explicitly that root `STORE-LISTING*.md` and `PRIVACY*.md` remain in place in this quick task because existing README links and `scripts/verify-readme-anchors.ts` depend on those exact paths. State explicitly that `public/icon/` remains in place because it is part of the extension asset tree. Use `doc/chrome-web-store/` as the single human-facing entry point without introducing a broad path refactor.
  </action>
  <verify>
    <automated>test -f doc/chrome-web-store/README.md && rg -n "STORE-LISTING\.md|STORE-LISTING\.en\.md|PRIVACY\.md|PRIVACY\.zh_CN\.md|screenshot-assets\.mjs|public/icon|07-VERIFICATION\.md" doc/chrome-web-store/README.md</automated>
  </verify>
  <done>`doc/` exists, `doc/chrome-web-store/README.md` exists, and the index explains which artifacts are centralized by link vs kept at root for compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate only low-risk screenshot templates into the doc hub</name>
  <files>doc/chrome-web-store/assets/templates/*, scripts/screenshot-assets.mjs, .store-assets/*</files>
  <action>
    Move `.store-assets/` into `doc/chrome-web-store/assets/templates/` because repository search shows it is only consumed by `scripts/screenshot-assets.mjs`. Update `scripts/screenshot-assets.mjs` so its source directory points to `doc/chrome-web-store/assets/templates`, and update any user-facing log/help text in that script to match the new location. Keep the screenshot output location unchanged unless the implementation reveals a concrete path dependency.

    Do not move `STORE-LISTING*.md`, `PRIVACY*.md`, or `public/icon/` in this task. The reason must remain encoded in comments or README prose: they are still referenced by existing docs, validation, or build/package flows, so moving them would turn a quick organization task into a wider refactor.
  </action>
  <verify>
    <automated>pnpm assets:screenshot && test -d .output/store-assets && test -f doc/chrome-web-store/assets/templates/screenshot-1-capture.html && test ! -d .store-assets</automated>
  </verify>
  <done>All screenshot/promo HTML templates live under `doc/chrome-web-store/assets/templates/`, the old `.store-assets/` directory is gone, and `pnpm assets:screenshot` still succeeds.</done>
</task>

</tasks>

<verification>
<automated>pnpm assets:screenshot</automated>
<automated>pnpm verify:readme</automated>
<automated>rg -n "STORE-LISTING(\.en)?\.md|PRIVACY(\.zh_CN)?\.md|doc/chrome-web-store/assets/templates|public/icon" . --glob '!node_modules' --glob '!.git'</automated>
</verification>

<success_criteria>
- 仓库新增 `doc/chrome-web-store/`，可作为上架物料的统一入口。
- `STORE-LISTING*.md`、`PRIVACY*.md`、`public/icon/` 未因本次整理而断链。
- 截图模板迁移到 `doc/chrome-web-store/assets/templates/` 后，`pnpm assets:screenshot` 仍然可用。
- 这次改动保持在“整理/索引”范围内，没有演变为大范围文档或构建重构。
</success_criteria>

<output>
After completion, create `/Users/seven/data/coding/projects/seven/web2chat/.planning/quick/260524-9lb-doc-chrome-web-store/260524-9lb-SUMMARY.md`.
</output>
