---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - entrypoints/popup/components/SendForm.tsx
  - entrypoints/popup/App.tsx
autonomous: true
requirements: [bug-fix]

must_haves:
  truths:
    - "needs_confirmation 时 popup 保持打开，用户能看到 SelectorWarningDialog"
    - "confirmSelectorWarning 使用 warningRecord.snapshot（原始捕获）构建 dispatch payload"
  artifacts:
    - path: "entrypoints/popup/components/SendForm.tsx"
      provides: "startDispatch 不再在 needs_confirmation 时关闭 popup"
      contains: "needs_confirmation"
    - path: "entrypoints/popup/App.tsx"
      provides: "confirmSelectorWarning 使用 warningRecord.snapshot"
      contains: "warningRecord.snapshot"
  key_links:
    - from: "entrypoints/popup/components/SendForm.tsx"
      to: "DispatchStartOutput.state"
      via: "res.data.state check before window.close()"
      pattern: "needs_confirmation"
    - from: "entrypoints/popup/App.tsx"
      to: "selectorWarningSig.value.snapshot"
      via: "warningRecord.snapshot as dispatch payload base"
      pattern: "warningRecord\\.snapshot"
---

<objective>
修复 popup needs_confirmation 级联 bug：popup 过早关闭 + snapshot 被 re-capture 覆盖。

Purpose: 当适配器返回 SELECTOR_LOW_CONFIDENCE 时，popup 不应关闭，且确认时必须使用原始捕获的 snapshot（而非 popup 重开后的新捕获）。
Output: 两个文件的精准修复。
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/feishu-wrong-content-injected.md

<interfaces>
<!-- DispatchStartOutput returned by sendMessage('dispatch.start', input) -->
From shared/messaging/routes/dispatch.ts:
```typescript
export const DispatchStateEnum = z.enum([
  'pending', 'opening', 'awaiting_complete', 'awaiting_adapter',
  'needs_confirmation', 'done', 'error', 'cancelled',
]);
export const DispatchStartOutputSchema = z.object({
  dispatchId: z.string().uuid(),
  state: DispatchStateEnum,
});
```

From shared/messaging/routes/dispatch.ts (DispatchStartInput):
```typescript
export const DispatchStartInputSchema = z.object({
  dispatchId: z.string().uuid(),
  send_to: z.string().url().max(2048),
  prompt: z.string().max(10_000),
  snapshot: ArticleSnapshotSchema,
  selectorConfirmation: SelectorConfirmationSchema.optional(),
});
```

From shared/storage/repos/dispatch.ts (DispatchRecord):
- DispatchRecord has a `snapshot: ArticleSnapshot` field containing the original captured content
- selectorWarningSig holds a DispatchRecord (the pending needs_confirmation record)
- warningRecord.snapshot = original ArticleSnapshot from the first dispatch attempt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix startDispatch to not close popup on needs_confirmation</name>
  <files>entrypoints/popup/components/SendForm.tsx</files>
  <action>
In `startDispatch` (line 236-242), change the `window.close()` call to only execute when `res.data.state` is NOT `'needs_confirmation'`. The current code:

```typescript
if (res.ok) {
  window.close();
}
```

Change to:

```typescript
if (res.ok && res.data.state !== 'needs_confirmation') {
  window.close();
}
```

When state IS `needs_confirmation`, the popup stays open so the `storage.onChanged` listener in App.tsx (line 293-317) detects the state change, sets `selectorWarningSig.value = rec`, and renders the `SelectorWarningDialog` for user confirmation.

Do NOT change anything else in this file.
  </action>
  <verify>
    <automated>cd /Users/seven/data/coding/projects/seven/web2chat && npx tsc --noEmit 2>&1 | grep -c "error" | xargs -I{} bash -c 'test {} -eq 0 && echo "type-check-pass" || echo "type-check-fail"'</automated>
  </verify>
  <done>startDispatch 不再在 needs_confirmation 状态时关闭 popup。TypeScript 编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: Fix confirmSelectorWarning to use warningRecord.snapshot</name>
  <files>entrypoints/popup/App.tsx</files>
  <action>
In `confirmSelectorWarning` (lines 231-283), replace the snapshot construction to use `warningRecord.snapshot` as the base instead of `snapshotSig.value`.

The current code at lines 231-248:

```typescript
const snapshotForDispatch = snapshotSig.value;
const warningRecord = selectorWarningSig.value;
if (!snapshotForDispatch || !warningRecord) {
  selectorWarningSig.value = null;
  return;
}
const dispatchId = crypto.randomUUID();
const input: DispatchStartInput = {
  dispatchId,
  send_to: sendToSig.value,
  prompt: promptSig.value,
  snapshot: {
    ...snapshotForDispatch,
    title: titleSig.value,
    description: descriptionSig.value,
    content: contentSig.value,
  },
  selectorConfirmation: { warning: 'SELECTOR_LOW_CONFIDENCE' },
};
```

Change to:

```typescript
const warningRecord = selectorWarningSig.value;
if (!warningRecord) {
  selectorWarningSig.value = null;
  return;
}
const dispatchId = crypto.randomUUID();
const input: DispatchStartInput = {
  dispatchId,
  send_to: warningRecord.send_to,
  prompt: warningRecord.prompt,
  snapshot: warningRecord.snapshot,
  selectorConfirmation: { warning: 'SELECTOR_LOW_CONFIDENCE' },
};
```

Key changes:
1. Remove `snapshotForDispatch` — use `warningRecord.snapshot` directly (contains the original captured content from the first dispatch attempt).
2. Use `warningRecord.send_to` and `warningRecord.prompt` instead of `sendToSig.value` and `promptSig.value` — these signals may have been overwritten by the re-captured popup mount.
3. Do NOT spread user-edited title/description/content signals over the snapshot — when popup reopens on a different page, those signals contain the wrong page data. The original snapshot from `warningRecord` is the authoritative source.

Do NOT change anything else in this function or file.
  </action>
  <verify>
    <automated>cd /Users/seven/data/coding/projects/seven/web2chat && npx tsc --noEmit 2>&1 | grep -c "error" | xargs -I{} bash -c 'test {} -eq 0 && echo "type-check-pass" || echo "type-check-fail"'</automated>
  </verify>
  <done>confirmSelectorWarning 使用 warningRecord 的 snapshot/send_to/prompt 构建 dispatch payload，不再依赖可能被新 capture 覆盖的 signals。TypeScript 编译通过。</done>
</task>

</tasks>

<verification>
```bash
cd /Users/seven/data/coding/projects/seven/web2chat && npx tsc --noEmit
```
TypeScript 编译零错误。

手动验证（需要用户在 browser 中测试）：
1. 打开一个文章页面，点击扩展图标，填入一个会触发 SELECTOR_LOW_CONFIDENCE 的目标 URL（如 Discord 或 Slack 频道）
2. 点击 Confirm — popup 应保持打开，显示 SelectorWarningDialog
3. 点击确认发送 — 消息内容应为原始文章（不是目标 IM 页面的元数据）
</verification>

<success_criteria>
- SendForm.startDispatch 在 res.data.state === 'needs_confirmation' 时不调用 window.close()
- confirmSelectorWarning 使用 warningRecord.snapshot/send_to/prompt 而非 signal 值
- TypeScript 编译通过
- 不引入新逻辑，只修正数据源选择
</success_criteria>

<output>
After completion, create `.planning/quick/260517-aa3-fix-popup-needs-confirmation-bug/260517-aa3-SUMMARY.md`
</output>
