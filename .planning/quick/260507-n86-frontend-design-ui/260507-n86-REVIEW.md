---
task: 260507-n86-frontend-design-ui
reviewed: 2026-05-07
files_reviewed: 20
verdict: PASS_WITH_NOTES
findings:
  blocker: 0
  high: 0
  medium: 2
  low: 2
checks_run:
  - typecheck (pnpm typecheck) — clean
  - lint (pnpm lint) — clean
  - unit tests (pnpm test --run) — 235 passed
  - build (pnpm build) — clean
  - data-testid count: popup 35 / options 14 (matches baseline)
  - sky/slate cliché grep — only DESIGN.md prose mentions remain
  - locked files (locales/, package.json, pnpm-lock.yaml, shared/, background, content scripts, wxt.config.ts) — 0 changes
---

# Code Review — Quick Task 260507-n86

**Date:** 2026-05-07
**Files reviewed:** 20 (1 doc + 1 css + 2 css edits + 2 html + 14 tsx)
**Verdict:** PASS_WITH_NOTES

## BLOCKER
_(none)_

## HIGH
_(none)_

## MEDIUM

- [Severity: MEDIUM] Dropdown arrow color hardcoded as `%236b6b62` (light-mode `--color-ink-muted`) — `entrypoints/options/components/LanguageSection.tsx:53` — the inline-SVG arrow embedded as `bg-[image:url('data:image/svg+xml;...stroke=%236b6b62...')]` does not adapt to dark mode (in dark mode `--color-ink-muted` becomes `#9a968b`, but the arrow stays `#6b6b62`). Functional but breaks the "tokens drive every UI surface" promise for the only inline-SVG decoration in the diff. — Fix: use `currentColor` and rely on the `<select>`'s `text-[var(--color-ink-strong)]` cascade, or render the arrow as an absolutely-positioned sibling `<svg>` consuming `currentColor`. Minimal change: swap `stroke=%236b6b62` for `stroke=currentColor` and verify the data-URL renders inheritance correctly (some browsers ignore `currentColor` in `url(data:...)` backgrounds — fallback to a sibling SVG if so).

- [Severity: MEDIUM] Hover/active motion utilities run even with `prefers-reduced-motion: reduce` — multiple files (e.g. `PopupChrome.tsx:26` `hover:rotate-[60deg]`, `SendForm.tsx:331` `active:translate-y-[0.5px]`, `ResetSection.tsx:70` `active:translate-y-[0.5px]`, `ConfirmDialog.tsx:67` `active:translate-y-[0.5px]`). Only `@keyframes` are guarded by the `@media (prefers-reduced-motion: no-preference)` block in `_shared-tokens.css`; CSS `transition` and `transform` declarations on hover/active states still fire. DESIGN.md §1 states "Everything sits behind `prefers-reduced-motion: no-preference`," so transition-based motion is in scope of that promise. — Fix: wrap motion-changing utilities in a media-query class group, e.g. add a global rule `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { transition: none !important; transform: none !important; animation: none !important; } }` to `_shared-tokens.css`.

## LOW

- [Severity: LOW] Listbox/dialog drop-shadow nearly invisible in dark mode — `Combobox.tsx:160`, `Combobox.tsx:225`, `ConfirmDialog.tsx:85` use `shadow-[0_1px_2px_rgb(0_0_0/0.04),0_12px_32px_rgb(0_0_0/0.06)]`. The hardcoded black-with-low-alpha values vanish against the dark canvas (`#0c0c0b`). Functionally OK because `border-[var(--color-border-strong)]` carries the elevation cue, but DESIGN.md §6 promises shadows for "true overlays." Tokenizing shadow values would let dark mode use brighter shadows. — Fix: add `--shadow-pop-light` / `--shadow-pop-dark` tokens to `_shared-tokens.css` and switch to `shadow-[var(--shadow-pop)]`. Optional polish.

- [Severity: LOW] Two stale doc strings in DESIGN.md prose mention `slate-100` / `slate-900` / `sky-600` — `DESIGN.md:327, 391`. These are legitimate "Don't" examples in §7 "Do's and Don'ts" and §9 Iteration Guide, so semantically correct, but they show up in audit greps for cliché residue. — Fix (optional): wrap them in inline-code spans only (already done) and leave a comment that grep should exclude `DESIGN.md`. No code change required.

## Passed checks

- **All baseline counts preserved** — popup `data-testid`=35, options `data-testid`=14 match must-haves frontmatter. Functional ARIA additions (popup +2, options +1) are exclusively decorative `aria-hidden="true"` attributes on the new asterism `∗ ∗ ∗` decorations and Discord ToS footnote `¹` marker (consistent with PLAN frontmatter `i18n_decisions`). No ARIA was removed; `for=id` relationships intact (Combobox `id`, FieldLabel `for`, ConfirmDialog `aria-labelledby`/`aria-describedby`, dialog `aria-modal="true"`, `role="dialog"`/`combobox`/`listbox`/`option` all present).
- **Locked files unchanged** — `git diff HEAD~3 HEAD -- locales/ package.json pnpm-lock.yaml shared/ entrypoints/background.ts entrypoints/*.content.ts wxt.config.ts` returns empty.
- **0 KB new dependencies** — package.json/lockfile not modified.
- **No new i18n keys** — `Intl.RelativeTimeFormat(navigator.language)` used in `CapturePreview.tsx:149` exactly as planned; asterism + footnote `¹` are character literals with `aria-hidden="true"`.
- **Tailwind v4 `@theme` correctness** — dark-mode override correctly nested under `:root` (not `@theme`) inside `@media (prefers-color-scheme: dark)`; comments at `_shared-tokens.css:80-87` document the rationale; build emits all tokens. All keyframes inside `@media (prefers-reduced-motion: no-preference)`.
- **No new XSS surface** — `CapturePreview` renders `snapshot.url`, `snapshot.title`, etc. via JSX `{}` text nodes (Preact-escaped); no `innerHTML` / `dangerouslySetInnerHTML`. Host extracted via `new URL(...).host` with try/catch.
- **No service-worker / shared / content-script changes** — diff scope confined to entrypoints/popup, entrypoints/options, root DESIGN.md.
- **typecheck / lint / 235 unit tests / build all green.**
- **Editorial design contract** — `text-sky-*`, `bg-sky-*`, `text-slate-N`, `bg-slate-N`, `rounded-md`, `rounded-lg` all 0 occurrences in `entrypoints/`. PlatformIcon openclaw gradient hardcoded brand colors preserved (`#ff4d4d`/`#991b1b`/`#00e5cc`). FieldLabel applies 11px UPPERCASE/tracking-[0.06em]. inputClass uses bottom-border (`border-b-[1.5px]` + `rounded-none`); textareaClass keeps full box (`border` + `rounded-[var(--radius-sharp)]`). ErrorBanner is margin-note (3px left stripe + transparent bg + hover-soft). Edge-line cards on options (`bg-transparent border border-[var(--color-border-strong)]`). Serif on display headings (PopupChrome wordmark, ConfirmDialog title, options page heading, capture title), mono on URL/host chip/dispatchId/captureAt timestamp/origin rows.
- **`dark:` className discipline** — only one `dark:` reference remains in component code (`ConfirmDialog.tsx:72` `dark:bg-black/60`), which is an opacity adjustment on the modal overlay, NOT a tokenized color — explicitly listed in DESIGN.md §4 as the documented modal overlay treatment. Out of scope for the "no `dark:` for tokenized colors" rule.

---

_Note for user: e2e (Playwright headed) is out of scope for this review; user should run separately to confirm DOM-test contracts hold._
