---
phase: 02-capture
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - entrypoints/extractor.content.ts
autonomous: true
requirements:
  - CAP-02
  - CAP-03

must_haves:
  truths:
    - "pnpm build 成功，.output/chrome-mv3/content-scripts/extractor.js 存在"
    - "extractor 使用 document.cloneNode(true) 传给 Readability（D-14）"
    - "DOMPurify 在 Turndown 之前调用（D-20）"
    - "description fallback 三段查询：meta[name=description] → og:description → Readability.excerpt"
    - "extractor main() 返回 { title, description, content } plain object"
    - "registration: 'runtime' 在 defineContentScript 中配置"
  artifacts:
    - path: entrypoints/extractor.content.ts
      provides: "WXT runtime-registration content script，Readability+DOMPurify+Turndown 流水线"
      contains: "registration: 'runtime'"
      min_lines: 50
  key_links:
    - from: "entrypoints/extractor.content.ts"
      to: "background/capture-pipeline.ts (Wave 3)"
      via: "chrome.scripting.executeScript({ files: ['content-scripts/extractor.js'] })"
      pattern: "registration.*runtime"
    - from: "entrypoints/extractor.content.ts"
      to: "tests/unit/extractor/*.spec.ts (Wave 2 并行)"
      via: "getDescription helper 单独导出供单元测试"
      pattern: "export.*getDescription"
---

<objective>
创建 extractor content script：WXT runtime-registration bundle，在 active tab 的 ISOLATED world 中运行 Readability + DOMPurify + Turndown，通过 executeScript return value 通道将 { title, description, content } 返回给 SW。

Purpose: CAP-02 要求 extractor 在当前 active tab 中抽取并净化页面内容。本 plan 创建 entrypoints/extractor.content.ts——它既是 WXT build 产物（生成 content-scripts/extractor.js），也是 Wave 3 capture-pipeline 的注入目标。
Output: entrypoints/extractor.content.ts；pnpm build 后 .output/chrome-mv3/content-scripts/extractor.js 存在。
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture/02-CONTEXT.md
@.planning/phases/02-capture/02-RESEARCH.md
@.planning/phases/02-capture/02-PATTERNS.md

<interfaces>
<!-- extractor 依赖的库签名（Wave 1 安装后可用）-->

From @mozilla/readability:
```typescript
// Readability 必须传 cloneNode(true)（D-14）
import { Readability } from '@mozilla/readability';
const article = new Readability(clone).parse();
// article 可能为 null；article.title, article.content (HTML), article.excerpt 是字符串
```

From dompurify:
```typescript
import DOMPurify from 'dompurify';
// 默认 profile（D-20）——不传任何选项
const cleanHtml = DOMPurify.sanitize(rawHtml); // 返回 string
```

From turndown + turndown-plugin-gfm:
```typescript
import TurndownService from 'turndown';
import { gfm } from 'turndown-plugin-gfm';
const td = new TurndownService();
td.use(gfm);
const markdown = td.turndown(cleanHtml); // 返回 string
```

WXT content script pattern（RESEARCH.md Pattern 1）:
```typescript
export default defineContentScript({
  registration: 'runtime',  // 不加入 manifest content_scripts，仅生成独立 bundle
  main() {
    // 所有副作用在 main() 里，不在模块顶层
    return { title, description, content };  // return value 通过 executeScript 通道回 SW
  },
});
```

注意：getDescription 查询原始 document（非 clone），因为 Readability.parse() 会修改 clone 的 DOM。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 entrypoints/extractor.content.ts</name>
  <files>entrypoints/extractor.content.ts</files>

  <read_first>
    - entrypoints/background.ts (参考 defineBackground 的结构模式——extractor 用 defineContentScript 平行)
    - .planning/phases/02-capture/02-PATTERNS.md (Pattern 1 + getDescription 实现)
    - .planning/phases/02-capture/02-RESEARCH.md (Code Examples 节)
  </read_first>

  <behavior>
    - defineContentScript 配置 registration: 'runtime'（不进 manifest content_scripts）
    - main() 内：document.cloneNode(true) → Readability(clone).parse() → getDescription(document, article) → DOMPurify.sanitize(rawHtml) → TurndownService + gfm → return { title, description, content }
    - title = article?.title?.trim() || document.title.trim()（空 title 回退 document.title）
    - description fallback 三段：meta[name="description"] → meta[property="og:description"] → article?.excerpt
    - rawHtml = article?.content ?? ''（Readability 可能返回 null）
    - DOMPurify.sanitize 在 TurndownService 之前调用（D-20）
    - getDescription 导出为命名导出（供单元测试直接 import）
    - ExtractorPartial 类型导出（{ title: string; description: string; content: string }）
    - 模块顶层无副作用（全部逻辑在 main() 内）
  </behavior>

  <action>
创建 entrypoints/extractor.content.ts，内容如下：

```typescript
/**
 * Extractor content script (CAP-02, D-13..D-20).
 *
 * WXT registration: 'runtime' — this script is NOT listed in manifest
 * content_scripts. It is bundled to content-scripts/extractor.js and
 * injected on-demand by the SW via:
 *   chrome.scripting.executeScript({ files: ['content-scripts/extractor.js'] })
 *
 * The return value of main() flows back to the SW through the executeScript
 * result channel (structuredClone, all fields are strings — serialisation safe).
 *
 * Pipeline (D-18, D-20):
 *   document.cloneNode(true) → Readability(clone).parse()
 *     → DOMPurify.sanitize(article.content)   ← must precede Turndown
 *     → TurndownService + gfm                 ← HTML → Markdown
 *
 * Security (D-14, D-20):
 *   - Readability MUST receive a clone — parse() mutates the DOM
 *   - DOMPurify uses default profile — no ALLOWED_TAGS relaxation
 *   - popup never calls innerHTML on the returned Markdown (PITFALLS §安全错误 #1)
 */

import { Readability } from '@mozilla/readability';
import DOMPurify from 'dompurify';
import TurndownService from 'turndown';
import { gfm } from 'turndown-plugin-gfm';

// ─── Types ────────────────────────────────────────────────────────────────────

/** Fields produced by the extractor; SW adds url + create_at to form ArticleSnapshot. */
export interface ExtractorPartial {
  title: string;
  description: string;
  content: string;
}

// ─── Helpers (exported for unit tests — mirror pattern from bumpHello) ────────

/**
 * Resolve description via 3-stage fallback (CAP-03, PATTERNS.md Pattern 2):
 *   1. <meta name="description">
 *   2. <meta property="og:description">
 *   3. Readability excerpt
 *
 * Uses the ORIGINAL document (not clone) because Readability.parse() may
 * remove meta tags from the clone during DOM mutation. (RESEARCH.md A5)
 */
export function getDescription(
  doc: Document,
  article: ReturnType<Readability['parse']>,
): string {
  const metaDesc = doc
    .querySelector('meta[name="description"]')
    ?.getAttribute('content')
    ?.trim();
  if (metaDesc) return metaDesc;

  const ogDesc = doc
    .querySelector('meta[property="og:description"]')
    ?.getAttribute('content')
    ?.trim();
  if (ogDesc) return ogDesc;

  return article?.excerpt?.trim() ?? '';
}

// ─── Entrypoint ───────────────────────────────────────────────────────────────

export default defineContentScript({
  registration: 'runtime', // NOT in manifest content_scripts; bundled separately
  main(): ExtractorPartial {
    // D-14: MUST pass clone — Readability.parse() mutates the DOM tree
    const clone = document.cloneNode(true) as Document;
    const article = new Readability(clone).parse();

    const title = article?.title?.trim() || document.title.trim();

    // D-20: description uses original document (meta may be stripped from clone)
    const description = getDescription(document, article);

    // D-18, D-20: sanitize before Turndown — order is mandatory
    const rawHtml = article?.content ?? '';
    const cleanHtml = DOMPurify.sanitize(rawHtml); // default profile

    const td = new TurndownService();
    td.use(gfm); // GFM: tables, strikethrough, fenced code, task lists
    const content = td.turndown(cleanHtml);

    return { title, description, content };
  },
});
```

文件创建完成后运行 `pnpm build` 并验证产物路径：
```bash
pnpm build
ls .output/chrome-mv3/content-scripts/extractor.js
```
  </action>

  <verify>
    <automated>pnpm build 2>&1 | grep -c "error" ; ls /data/coding/projects/seven/web2chat/.output/chrome-mv3/content-scripts/extractor.js 2>/dev/null && echo "extractor.js exists"</automated>
  </verify>

  <acceptance_criteria>
    - entrypoints/extractor.content.ts 包含字符串 "registration: 'runtime'"
    - entrypoints/extractor.content.ts 包含字符串 "document.cloneNode(true)"（D-14）
    - entrypoints/extractor.content.ts 包含字符串 "DOMPurify.sanitize"（在 td.turndown 之前）
    - entrypoints/extractor.content.ts 包含字符串 "export function getDescription"（供单元测试导入）
    - entrypoints/extractor.content.ts 包含字符串 "export interface ExtractorPartial"
    - pnpm build 退出码 0
    - .output/chrome-mv3/content-scripts/extractor.js 文件存在
    - pnpm typecheck 退出码 0
    - 模块顶层无 document.cloneNode / new Readability 等调用（全部在 main() 内）
  </acceptance_criteria>

  <done>extractor.content.ts 创建完毕，pnpm build 成功，content-scripts/extractor.js 存在，typecheck 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 任意网页 DOM → extractor | 网页内容不可信；extractor 在 ISOLATED world 中运行，不访问页面 JS 状态 |
| extractor return value → SW | 经 structuredClone 序列化；仅包含 string 字段，无对象引用泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | article.content XSS payload 注入 | mitigate | DOMPurify.sanitize(rawHtml) 在 Turndown 之前运行（D-20）；Markdown 输出作为纯文本渲染（textarea.value），popup 绝不调用 innerHTML（PITFALLS §安全错误 #1） |
| T-02-03-02 | Tampering | Readability 修改 live DOM | mitigate | D-14 强制传 document.cloneNode(true)；getDescription 查询原始 document 而非 clone |
| T-02-03-03 | Information Disclosure | extractor bundle 被页面 JS 读取 | mitigate | registration:'runtime' + ISOLATED world；不加入 web_accessible_resources（PITFALLS Pitfall 5）；verify-manifest.ts 防御性断言 |
| T-02-03-04 | Elevation of Privilege | DOMPurify 在 happy-dom 下漏放 XSS | mitigate | 单元测试强制 jsdom environment（// @vitest-environment jsdom）；运行时在真实 content script DOM 环境，DOMPurify 完全可信 |
</threat_model>

<verification>
```bash
cd /data/coding/projects/seven/web2chat
grep -c "registration: 'runtime'" entrypoints/extractor.content.ts
grep -c "document.cloneNode(true)" entrypoints/extractor.content.ts
grep -c "DOMPurify.sanitize" entrypoints/extractor.content.ts
grep -c "export function getDescription" entrypoints/extractor.content.ts
pnpm build 2>&1 | grep -v "^$" | tail -3
ls .output/chrome-mv3/content-scripts/extractor.js
pnpm typecheck
```
所有 grep >= 1，build 成功，extractor.js 存在，typecheck 通过。
</verification>

<success_criteria>
entrypoints/extractor.content.ts 创建完毕，实现 registration:'runtime' + Readability + DOMPurify + Turndown + description fallback 三段查询；pnpm build 产物中有 content-scripts/extractor.js；pnpm typecheck 通过；getDescription 和 ExtractorPartial 可从文件中导入（供单元测试）。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-capture/02-03-SUMMARY.md`
</output>
