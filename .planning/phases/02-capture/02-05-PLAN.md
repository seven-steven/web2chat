---
phase: 02-capture
plan: "05"
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - background/capture-pipeline.ts
  - entrypoints/background.ts
  - locales/en.yml
  - locales/zh_CN.yml
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
  - CAP-04

must_haves:
  truths:
    - "background/capture-pipeline.ts 存在，runCapturePipeline 导出为命名 export"
    - "entrypoints/background.ts 在顶层同步注册 onMessage('capture.run', ...) — 绝无 await 先于此"
    - "SW pipeline 步骤与 CONTEXT.md 时序图完全一致（URL scheme 预检 → create_at → executeScript → 空检 → Ok）"
    - "locale 文件包含所有 capture.* 键（en + zh_CN 100% 同构）"
  artifacts:
    - path: background/capture-pipeline.ts
      provides: "runCapturePipeline 函数，SW capture 编排核心"
      exports: ["runCapturePipeline"]
      min_lines: 55
    - path: entrypoints/background.ts
      provides: "顶层 onMessage('capture.run') 注册"
      contains: "capture.run"
    - path: locales/en.yml
      provides: "capture.* 命名空间所有键（18 个子键）"
      contains: "capture.loading.label"
    - path: locales/zh_CN.yml
      provides: "capture.* 命名空间所有键（与 en 完全同构）"
      contains: "capture.loading.label"
  key_links:
    - from: "background/capture-pipeline.ts"
      to: "entrypoints/background.ts"
      via: "import { runCapturePipeline } from '@/background/capture-pipeline'"
      pattern: "runCapturePipeline"
    - from: "entrypoints/background.ts capture.run handler"
      to: "entrypoints/extractor.content.ts"
      via: "chrome.scripting.executeScript({ files: ['content-scripts/extractor.js'] })"
      pattern: "content-scripts/extractor.js"
    - from: "locales/en.yml"
      to: "entrypoints/popup/App.tsx (Wave 4)"
      via: "t('capture.loading.label') / t('capture.field.title') / etc."
      pattern: "capture.loading.label"
---

<objective>
实现 SW capture pipeline：创建 background/capture-pipeline.ts 封装抓取编排逻辑，在 entrypoints/background.ts 顶层注册 capture.run 路由，并补齐 en + zh_CN locale 的所有 capture.* i18n 键。

Purpose: 这是 Phase 2 的服务端核心——连接 popup RPC 与 extractor bundle。本 plan 在 Wave 2 之后执行（extractor.js 已存在，ErrorCode/ArticleSnapshot 类型已就位）。
Output: SW capture pipeline 可运行；locale 键补齐后 popup Wave 4 可安全调用 t('capture.*')。
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture/02-CONTEXT.md
@.planning/phases/02-capture/02-RESEARCH.md
@.planning/phases/02-capture/02-PATTERNS.md
@.planning/phases/02-capture/02-UI-SPEC.md

<interfaces>
<!-- 执行器需要的现有接口 -->

From entrypoints/background.ts (当前，必须保留的结构):
```typescript
import { defineBackground } from '#imports';
import { onMessage, schemas, Ok, Err } from '@/shared/messaging';
import { metaItem } from '@/shared/storage';

function wrapHandler<R>(fn: () => Promise<R>): () => Promise<R> { ... }

export default defineBackground(() => {
  onMessage('meta.bumpHello', wrapHandler(async () => { ... }));
  // Phase 2 在此追加 capture.run（绝无 await 先于任何 onMessage 调用）
});
```

From shared/messaging/result.ts (Phase 2 扩展后):
```typescript
export type ErrorCode = 'INTERNAL' | 'RESTRICTED_URL' | 'EXTRACTION_EMPTY' | 'EXECUTE_SCRIPT_FAILED';
export function Err<E extends ErrorCode>(code: E, message: string, retriable?: boolean): Result<never, E>;
```

From shared/messaging/protocol.ts (Phase 2 扩展后):
```typescript
export const { sendMessage, onMessage } = defineExtensionMessaging<ProtocolMap>();
export const ArticleSnapshotSchema: ZodObject<...>;
export type ArticleSnapshot = { title: string; url: string; description: string; create_at: string; content: string; };
```

From entrypoints/extractor.content.ts (Wave 2 创建):
```typescript
// WXT build output: .output/chrome-mv3/content-scripts/extractor.js
export interface ExtractorPartial { title: string; description: string; content: string; }
```

From locales/en.yml (当前，只有 4 个键):
```yaml
extension_name:
  message: 'Web2Chat'
popup_hello:
  message: 'Hello, world (×$COUNT$)'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 background/capture-pipeline.ts</name>
  <files>background/capture-pipeline.ts</files>

  <read_first>
    - entrypoints/background.ts (wrapHandler 签名 + bumpHello 模式参考)
    - shared/messaging/result.ts (Err/Ok + ErrorCode 确认已扩展)
    - shared/messaging/protocol.ts (ArticleSnapshot / ArticleSnapshotSchema)
    - .planning/phases/02-capture/02-PATTERNS.md (capture-pipeline.ts 核心代码节)
    - .planning/phases/02-capture/02-CONTEXT.md (流水线时序图 specifics 节)
  </read_first>

  <behavior>
    - 步骤 1: chrome.tabs.query({ active: true, lastFocusedWindow: true }) → 无 tab 则 Err('INTERNAL')
    - 步骤 2: URL scheme 预检：scheme ∉ {http, https} → Err('RESTRICTED_URL', url, false)
    - 步骤 3: create_at = new Date().toISOString()（CAP-04，由 SW 生成）
    - 步骤 4: chrome.scripting.executeScript({ target: { tabId }, files: ['content-scripts/extractor.js'], world: 'ISOLATED' }) → catch → Err('EXECUTE_SCRIPT_FAILED', msg, true)
    - 步骤 5: results[0]?.result 为 undefined → Err('EXECUTE_SCRIPT_FAILED', 'No result', true)
    - 步骤 6: !partial.content && !partial.title → Err('EXTRACTION_EMPTY', msg, false)
    - 步骤 7: 组装 snapshot = { title, url, description, create_at, content } → Ok(snapshot)
    - runCapturePipeline 是 export（供 background.ts import）
    - ExtractorPartial 类型在本文件定义（不从 extractor.content.ts import，避免 content script 包被拉入 SW bundle）
  </behavior>

  <action>
创建 background/capture-pipeline.ts：

```typescript
/**
 * Capture pipeline — SW-side orchestration (CAP-01..CAP-04, D-15..D-17).
 *
 * Invoked by entrypoints/background.ts via the onMessage('capture.run') handler.
 * Never call chrome.scripting.executeScript from popup — only SW has the
 * scripting permission (CLAUDE.md §架构).
 *
 * Pipeline sequence (D-16, D-17, CAP-04):
 *   1. chrome.tabs.query → get active tab id + url
 *   2. URL scheme precheck → Err('RESTRICTED_URL') if not http/https
 *   3. create_at = new Date().toISOString()  ← by SW, not page (CAP-04)
 *   4. chrome.scripting.executeScript({ files: ['content-scripts/extractor.js'] })
 *   5. Unwrap results[0].result → ExtractorPartial
 *   6. Empty-content check → Err('EXTRACTION_EMPTY')
 *   7. Assemble ArticleSnapshot → Ok(snapshot)
 */

import { Ok, Err, type Result } from '@/shared/messaging';
import type { ArticleSnapshot } from '@/shared/messaging';

/**
 * Fields returned by entrypoints/extractor.content.ts main() via executeScript.
 * Defined here (not imported from extractor.content.ts) so the content-script
 * bundle is not accidentally pulled into the SW bundle by the bundler.
 */
interface ExtractorPartial {
  title: string;
  description: string;
  content: string;
}

export async function runCapturePipeline(): Promise<Result<ArticleSnapshot>> {
  // Step 1: Get the currently active tab
  const [tab] = await chrome.tabs.query({ active: true, lastFocusedWindow: true });
  if (!tab?.id || !tab.url) {
    return Err('INTERNAL', 'No active tab found', false);
  }

  // Step 2: URL scheme precheck (D-16)
  // chrome:// / file:// / about: / devtools: etc. → immediate Err, no executeScript
  let scheme: string;
  try {
    scheme = new URL(tab.url).protocol.replace(':', '');
  } catch {
    return Err('INTERNAL', `Malformed tab URL: ${tab.url}`, false);
  }
  if (scheme !== 'http' && scheme !== 'https') {
    return Err('RESTRICTED_URL', tab.url, false);
  }

  // Step 3: create_at is set by SW at click time, not derived from page (CAP-04)
  const create_at = new Date().toISOString();
  const url = tab.url;

  // Step 4: Inject extractor bundle (built from entrypoints/extractor.content.ts)
  // Path 'content-scripts/extractor.js' is the WXT build output path (RESEARCH.md Pattern 1).
  let results: chrome.scripting.InjectionResult[];
  try {
    results = await chrome.scripting.executeScript({
      target: { tabId: tab.id },
      files: ['content-scripts/extractor.js'],
      world: 'ISOLATED', // default, explicit for clarity
    });
  } catch (err) {
    return Err('EXECUTE_SCRIPT_FAILED', String(err), true);
  }

  // Step 5: Unwrap return value from extractor's main()
  const partial = results[0]?.result as ExtractorPartial | undefined;
  if (!partial) {
    return Err('EXECUTE_SCRIPT_FAILED', 'Extractor returned no result', true);
  }

  // Step 6: Empty content check (D-17)
  // Both title AND content empty → page has no recognisable article body
  if (!partial.content && !partial.title) {
    return Err('EXTRACTION_EMPTY', 'Readability returned empty content', false);
  }

  // Step 7: Assemble full ArticleSnapshot — SW owns url + create_at (CAP-03, CAP-04)
  const snapshot: ArticleSnapshot = {
    title: partial.title,
    url,
    description: partial.description,
    create_at,
    content: partial.content,
  };

  return Ok(snapshot);
}
```
  </action>

  <verify>
    <automated>grep -c "export async function runCapturePipeline" /data/coding/projects/seven/web2chat/background/capture-pipeline.ts && grep -c "content-scripts/extractor.js" /data/coding/projects/seven/web2chat/background/capture-pipeline.ts</automated>
  </verify>

  <acceptance_criteria>
    - background/capture-pipeline.ts 包含字符串 "export async function runCapturePipeline"
    - background/capture-pipeline.ts 包含字符串 "content-scripts/extractor.js"（精确路径）
    - background/capture-pipeline.ts 包含字符串 "RESTRICTED_URL"
    - background/capture-pipeline.ts 包含字符串 "EXTRACTION_EMPTY"
    - background/capture-pipeline.ts 包含字符串 "EXECUTE_SCRIPT_FAILED"
    - background/capture-pipeline.ts 包含字符串 "new Date().toISOString()"（create_at 由 SW 生成）
    - background/capture-pipeline.ts 包含字符串 "world: 'ISOLATED'"
    - pnpm typecheck 退出码 0
  </acceptance_criteria>

  <done>background/capture-pipeline.ts 创建完毕，7 步流水线完整，pnpm typecheck 通过。</done>
</task>

<task type="auto">
  <name>Task 2: 在 background.ts 注册 capture.run + 补齐 locale 键</name>
  <files>entrypoints/background.ts, locales/en.yml, locales/zh_CN.yml</files>

  <read_first>
    - entrypoints/background.ts (当前全文，确认 onMessage('meta.bumpHello') 位置和 wrapHandler 定义)
    - locales/en.yml (当前全文，确认现有 4 个键)
    - locales/zh_CN.yml (当前全文)
    - .planning/phases/02-capture/02-UI-SPEC.md (Copywriting Contract 表格 — 所有 i18n 键和值)
    - .planning/phases/02-capture/02-CONTEXT.md (i18n 命名空间描述)
  </read_first>

  <action>
**修改 entrypoints/background.ts：**

在现有 import 区块追加（紧接 `import { metaItem } from '@/shared/storage';` 之后）：
```typescript
import { runCapturePipeline } from '@/background/capture-pipeline';
```

在 `defineBackground(() => {` 闭包内，`onMessage('meta.bumpHello', ...)` 调用之后，立即追加（保持顶层同步注册，绝无 await 先于此）：
```typescript
  onMessage('capture.run', wrapHandler(runCapturePipeline));
```

确保 background.ts 的整体结构不变：
- wrapHandler 函数定义保持不动
- meta.bumpHello 注册保持不动（SW 健康探针，Phase 1 测试依赖）
- capture.run 在 meta.bumpHello 之后立即追加

**修改 locales/en.yml：**

在现有 4 个键之后追加以下全部 capture.* 键（不修改现有键）：

```yaml
capture.loading.label:
  message: 'Capturing page…'

capture.field.title:
  message: 'Title'

capture.field.url:
  message: 'Source URL'

capture.field.description:
  message: 'Description'

capture.field.createAt:
  message: 'Captured at'

capture.field.content:
  message: 'Content'

capture.empty.restricted.heading:
  message: 'Can''t capture this page'

capture.empty.restricted.body.before:
  message: 'Web2Chat doesn''t run on browser-internal pages. Open a regular website, then click '

capture.empty.restricted.body.icon:
  message: 'the toolbar icon'

capture.empty.restricted.body.after:
  message: ' again.'

capture.empty.noContent.heading:
  message: 'Nothing to extract'

capture.empty.noContent.body.before:
  message: 'This page doesn''t have a recognizable article body. Try a different page, then click '

capture.empty.noContent.body.icon:
  message: 'the toolbar icon'

capture.empty.noContent.body.after:
  message: ' again.'

capture.error.scriptFailed.heading:
  message: 'Capture failed'

capture.error.scriptFailed.body.before:
  message: 'Something went wrong while reading this page. Click '

capture.error.scriptFailed.body.icon:
  message: 'the toolbar icon'

capture.error.scriptFailed.body.after:
  message: ' to try again.'
```

**修改 locales/zh_CN.yml：**

在现有 4 个键之后追加以下全部 capture.* 键（与 en.yml 完全同构，100% 覆盖）：

```yaml
capture.loading.label:
  message: '正在抓取页面…'

capture.field.title:
  message: '标题'

capture.field.url:
  message: '来源链接'

capture.field.description:
  message: '摘要'

capture.field.createAt:
  message: '抓取时间'

capture.field.content:
  message: '正文'

capture.empty.restricted.heading:
  message: '无法抓取此页面'

capture.empty.restricted.body.before:
  message: 'Web2Chat 不能在浏览器内置页面上工作。请打开一个普通网页，然后再次点击'

capture.empty.restricted.body.icon:
  message: '工具栏图标'

capture.empty.restricted.body.after:
  message: '。'

capture.empty.noContent.heading:
  message: '没有可抓取的内容'

capture.empty.noContent.body.before:
  message: '此页面没有可识别的文章正文。换一个页面，再次点击'

capture.empty.noContent.body.icon:
  message: '工具栏图标'

capture.empty.noContent.body.after:
  message: '。'

capture.error.scriptFailed.heading:
  message: '抓取失败'

capture.error.scriptFailed.body.before:
  message: '读取此页面时出错。请点击'

capture.error.scriptFailed.body.icon:
  message: '工具栏图标'

capture.error.scriptFailed.body.after:
  message: '重试。'
```

注意：UI-SPEC.md Copywriting Contract 表中 `.body` 逻辑键（全句）仅供翻译审查，物理文件只包含 `.before` / `.icon` / `.after` 三个子键。以上配置已完全按 UI-SPEC.md 拆分。

YAML 引号注意：en.yml 中有撇号（can't）需用 '' 转义（即 `Can''t`），或用双引号包裹字符串。
  </action>

  <verify>
    <automated>grep -c "capture.run" /data/coding/projects/seven/web2chat/entrypoints/background.ts && grep -c "capture.loading.label" /data/coding/projects/seven/web2chat/locales/en.yml && grep -c "capture.loading.label" /data/coding/projects/seven/web2chat/locales/zh_CN.yml</automated>
  </verify>

  <acceptance_criteria>
    - entrypoints/background.ts 包含字符串 "capture.run"（新路由注册）
    - entrypoints/background.ts 包含字符串 "meta.bumpHello"（旧路由保留）
    - entrypoints/background.ts 包含字符串 "runCapturePipeline"（pipeline 调用）
    - entrypoints/background.ts 中 capture.run 注册在 defineBackground 闭包内，且在任何 await 之前（grep "onMessage" 的行号均早于任何 "await chrome." 的行号）
    - locales/en.yml 包含 "capture.loading.label"
    - locales/en.yml 包含 "capture.error.scriptFailed.body.before"
    - locales/en.yml 包含 "capture.empty.restricted.body.icon"
    - locales/zh_CN.yml 包含 "capture.loading.label"
    - locales/zh_CN.yml 中 capture.* 键数量与 locales/en.yml 相同（18 个子键）
    - pnpm build 退出码 0（locale 文件 YAML 格式有效）
    - pnpm typecheck 退出码 0
  </acceptance_criteria>

  <done>background.ts 追加 capture.run 注册；en.yml + zh_CN.yml 各增加 18 个 capture.* 子键；pnpm build 和 typecheck 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| popup → SW (capture.run RPC) | @webext-core/messaging 验证 sender 来自同扩展；SW 不信任 popup 提供任何 tab 信息 |
| SW → content script (executeScript) | SW 通过 chrome.scripting.executeScript 注入；URL scheme 预检防止在受限页面执行 |
| content script → SW (return value) | structuredClone 序列化；仅 string 字段，无原型链注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Elevation of Privilege | executeScript 在受限页面（chrome://）执行 | mitigate | D-16：URL scheme 预检在 executeScript 之前；不仅避免 API 报错，还防止 `EXECUTE_SCRIPT_FAILED` 文案暴露 chrome:// URL 信息 |
| T-02-05-02 | Spoofing | capture.run 伪造 RPC（来自第三方扩展） | accept | @webext-core/messaging 默认验证 sender.id === chrome.runtime.id（Phase 1 D-05）；无需额外配置 |
| T-02-05-03 | Tampering | ExtractorPartial 字段为恶意 string | mitigate | Phase 2 popup 仅将 content 渲染为 textarea.value（纯文本），不调用 innerHTML；zod 的 ArticleSnapshotSchema 在 Ok 返回前验证 5 个字段 |
| T-02-05-04 | Information Disclosure | i18n 键缺失导致 t() 回退暴露键名 | mitigate | en + zh_CN 100% 同构（18 个 capture.* 子键），CI i18n coverage gate 兜底（Phase 1 已就位） |
</threat_model>

<verification>
```bash
cd /data/coding/projects/seven/web2chat
grep -c "capture.run" entrypoints/background.ts
grep -c "meta.bumpHello" entrypoints/background.ts
grep -c "runCapturePipeline" entrypoints/background.ts
grep -c "capture.loading.label" locales/en.yml
grep -c "capture.error.scriptFailed.body.after" locales/en.yml
grep -c "capture.loading.label" locales/zh_CN.yml
pnpm typecheck
pnpm build 2>&1 | tail -3
pnpm test 2>&1 | tail -5
```
所有 grep >= 1，typecheck + build + test 退出码 0。
</verification>

<success_criteria>
background/capture-pipeline.ts 实现完整 7 步流水线；background.ts 顶层同步注册 capture.run；en + zh_CN 各追加 18 个 capture.* 子键；pnpm build + typecheck + test 全绿；Phase 1 的 bumpHello 路由和测试不受影响。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-capture/02-05-SUMMARY.md`
</output>
