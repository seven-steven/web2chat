---
phase: 02-capture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - CAP-02

must_haves:
  truths:
    - "pnpm install succeeds with @mozilla/readability, dompurify, turndown, turndown-plugin-gfm in dependencies"
    - "TypeScript sees Readability, DOMPurify, TurndownService types without error"
  artifacts:
    - path: package.json
      provides: "Updated dependency list with 4 new runtime deps + @types/turndown dev dep"
      contains: "@mozilla/readability"
  key_links:
    - from: package.json
      to: "entrypoints/extractor.content.ts (Wave 2)"
      via: "pnpm add — lockfile written; imports resolve at build time"
      pattern: '"@mozilla/readability"'
---

<objective>
安装 Phase 2 所需的 4 个运行时库和 1 个类型包，解除后续所有 plan 对这些库的 import 阻塞。

Purpose: CAP-02 要求 @mozilla/readability + dompurify + turndown + turndown-plugin-gfm，这四个包目前不在 package.json 中，是 extractor 能够构建的前提条件。
Output: package.json + pnpm-lock.yaml 更新；node_modules 就位；TypeScript 类型可解析。
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 4 个运行时库 + @types/turndown</name>
  <files>package.json</files>

  <read_first>
    - package.json (确认当前 dependencies 结构，避免覆盖现有字段)
  </read_first>

  <action>
执行以下两条 pnpm 命令安装依赖（在项目根目录执行）：

```bash
pnpm add @mozilla/readability dompurify turndown turndown-plugin-gfm
pnpm add -D @types/turndown
```

精确版本说明（RESEARCH.md 2026-04-30 npm registry 核验）：
- @mozilla/readability@0.6.0 — Readability 抽取器
- dompurify@3.4.1 — HTML 净化，自带 .d.ts，不需要 @types/dompurify
- turndown@7.2.4 — HTML→Markdown 转换器
- turndown-plugin-gfm@1.0.2 — GFM 扩展（表格/删除线/fenced code/task list）
- @types/turndown@5.0.6 — Turndown TypeScript 类型（devDependencies）

注意：turndown-plugin-gfm 没有 @types 包（不在 npm registry），安装后直接写类型声明。不需要也不要安装 @types/dompurify（已内置于 dompurify 3.x）。

安装完成后运行 pnpm typecheck 验证类型可解析：
```bash
pnpm typecheck
```
  </action>

  <verify>
    <automated>ls /data/coding/projects/seven/web2chat/node_modules/@mozilla/readability && ls /data/coding/projects/seven/web2chat/node_modules/dompurify && ls /data/coding/projects/seven/web2chat/node_modules/turndown && pnpm typecheck</automated>
  </verify>

  <acceptance_criteria>
    - package.json dependencies 包含 "@mozilla/readability": 键
    - package.json dependencies 包含 "dompurify": 键
    - package.json dependencies 包含 "turndown": 键
    - package.json dependencies 包含 "turndown-plugin-gfm": 键
    - package.json devDependencies 包含 "@types/turndown": 键
    - pnpm-lock.yaml 存在且已更新（包含上述包的锁定版本）
    - pnpm typecheck 退出码为 0（依赖类型可解析）
  </acceptance_criteria>

  <done>5 个包安装完毕，pnpm-lock.yaml 已更新，pnpm typecheck 退出码 0。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | 第三方包引入；pnpm lockfile 锁定精确版本防止供应链劫持 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | npm package supply chain | mitigate | pnpm lockfile 锁定精确版本；不使用 `latest` 浮动标签；CI `--frozen-lockfile` 防止 lockfile 漂移 |
| T-02-01-02 | Information Disclosure | dompurify 版本过旧 | accept | 本次安装 3.4.1（2026-04-30 latest），建议跟踪安全更新；低风险（Phase 2 内 DOMPurify 输出不出扩展边界） |
</threat_model>

<verification>
```bash
ls /data/coding/projects/seven/web2chat/node_modules/@mozilla/readability
ls /data/coding/projects/seven/web2chat/node_modules/dompurify
ls /data/coding/projects/seven/web2chat/node_modules/turndown
grep -v '^#' package.json | grep -c '"@mozilla/readability"'
grep -v '^#' package.json | grep -c '"@types/turndown"'
pnpm typecheck
```
所有 ls 成功，grep 返回 >= 1，typecheck 退出码 0。
</verification>

<success_criteria>
pnpm install 成功（无 peer dependency 错误），package.json 包含 5 个新包，pnpm typecheck 通过，后续 Wave 2 的 extractor 可以 import 这些库。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-capture/02-01-SUMMARY.md`
</output>
