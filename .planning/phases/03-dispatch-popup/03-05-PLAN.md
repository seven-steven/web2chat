---
phase: 03-dispatch-popup
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - entrypoints/popup/components/primitives.tsx
  - entrypoints/popup/components/PlatformIcon.tsx
  - entrypoints/popup/components/Combobox.tsx
  - entrypoints/popup/components/InProgressView.tsx
  - entrypoints/popup/components/ErrorBanner.tsx
autonomous: true
requirements: [DSP-01, DSP-02, DSP-03, DSP-07, DSP-08]
requirements_addressed: [DSP-01, DSP-02, DSP-03, DSP-07, DSP-08]
must_haves:
  truths:
    - "primitives.tsx exports FieldLabel + textareaClass constant verbatim from current popup/App.tsx (lines 280-299) — surgical extraction so options page can reuse the same focus-ring chrome (UI-SPEC §Implementation Notes #1)"
    - "PlatformIcon.tsx renders inline SVG (no <img>) for 4 platform variants: mock (Lucide flask-conical), openclaw (Oc letterform placeholder), discord (D letterform placeholder), unsupported (Lucide globe) — UI-SPEC §Iconography lines 488-499; Phase 4/5 will replace placeholders"
    - "Combobox.tsx implements ARIA 1.2 editable combobox: role='combobox' on <input> + role='listbox' on <ul> + role='option' on <li> + aria-expanded/aria-controls/aria-autocomplete='list'/aria-activedescendant per UI-SPEC §Component States §Combobox lines 440-444 — D-30"
    - "Combobox keyboard contract — ↑/↓ cycles activeIdx (-1→0→...→length-1→-1); Enter selects activeOption when activeIdx≥0; Esc closes listbox keeping current input value; Tab closes listbox and lets native focus cycle (no preventDefault) — UI-SPEC §Combobox §Keyboard contract lines 433-438"
    - "Combobox listbox option = 36px (`min-h-9`) row with 16×16 PlatformIcon + truncated text + 16×16 delete button (`aria-label='Remove from history'` from i18n key history_remove_button) — UI-SPEC lines 419-431"
    - "InProgressView.tsx renders the centered status-card layout (`flex flex-col items-center text-center p-4 py-8 gap-4 min-w-[360px] min-h-[240px]`) with 24×24 sky-600 animate-spin spinner + heading + three-segment inline accent body + outline destructive Cancel button + click-to-copy dispatchId footer — UI-SPEC §In-progress placeholder lines 467-476"
    - "ErrorBanner.tsx renders the slim banner (NOT full-screen): `bg-red-50 dark:bg-red-950/40 border-l-4 border-red-600 p-4 rounded-r-md` + heading text-sm font-semibold text-red-700 + body text-sm text-slate-700 + retry button (red-600 underline-on-hover) + dismiss ✕ — UI-SPEC §Component States §Error banner lines 455-460; PLATFORM_UNSUPPORTED has no retry button"
    - "Every Phase 3 NEW component uses Preact attribute names (`for`/`class`/`stroke-width`/`tabindex` — never React `htmlFor`/`className`/`strokeWidth`/`tabIndex`) — UI-SPEC §Implementation Notes #2"
    - "Every Phase 3 NEW component uses spacing scale strict subset {4, 8, 16, 24, 32, 48, 64} only (Tailwind tokens `gap-1/2/4/6/8`, `p-1/2/4/6/8`); 36px combobox option exception is the only justified deviation — UI-SPEC §Spacing Scale lines 56-91"
    - "Every Phase 3 NEW component uses two font weights only — `font-normal` (400) + `font-semibold` (600); no `font-medium` / `font-bold` introduced — UI-SPEC §Typography lines 96-110"
    - "All user-visible copy goes through `t()` from @/shared/i18n; NO bare JSX text literals in any new component file (FND-06 + UI-SPEC §Copywriting Contract)"
    - "All user-supplied URLs/host strings interpolated via JSX text-node substitution INSIDE a span (e.g. soft-overwrite link `<>{before}<span class='text-sky-600'>{host}</span>{after}</>`) — never via i18n placeholder; PITFALLS §11 / Pattern S5"
  artifacts:
    - path: "entrypoints/popup/components/primitives.tsx"
      provides: "FieldLabel component + textareaClass + inputClass constants — extracted from popup/App.tsx for reuse"
      exports: ["FieldLabel", "textareaClass", "inputClass"]
    - path: "entrypoints/popup/components/PlatformIcon.tsx"
      provides: "Inline-SVG platform icons sized 16 or 24 — variant prop selects mock/openclaw/discord/unsupported"
      exports: ["PlatformIcon"]
    - path: "entrypoints/popup/components/Combobox.tsx"
      provides: "ARIA 1.2 editable combobox component — used by send_to + prompt fields in Plan 06"
      exports: ["Combobox", "ComboboxOption"]
    - path: "entrypoints/popup/components/InProgressView.tsx"
      provides: "Dispatch in-progress placeholder with cancel button + dispatchId footer"
      exports: ["InProgressView"]
    - path: "entrypoints/popup/components/ErrorBanner.tsx"
      provides: "Slim error banner above SendForm; dismiss + retry per ErrorCode"
      exports: ["ErrorBanner"]
  key_links:
    - from: "entrypoints/popup/components/Combobox.tsx"
      to: "entrypoints/popup/components/primitives.tsx (inputClass)"
      via: "import — input chrome shares textareaClass focus-ring style"
      pattern: "from.*primitives"
    - from: "entrypoints/popup/components/Combobox.tsx"
      to: "entrypoints/popup/components/PlatformIcon.tsx"
      via: "JSX render of 16×16 PlatformIcon inside listbox option"
      pattern: "<PlatformIcon"
    - from: "entrypoints/popup/components/InProgressView.tsx"
      to: "entrypoints/popup/components/primitives.tsx (none yet — uses raw layout)"
      via: "i18n keys dispatch_in_progress_*"
      pattern: "dispatch_in_progress"
---

<objective>
Build the four standalone Phase 3 popup components — Combobox (D-30), PlatformIcon, InProgressView, ErrorBanner — plus extract the Phase 2 primitives (FieldLabel + textareaClass) into a shared module so options page (Plan 07) can reuse them. NO touches to popup/App.tsx in this plan; Plan 06 owns App.tsx integration.

Purpose: Plan 06's SendForm + popup state machine compose these 4 components. Plan 07's options page imports primitives.tsx for FieldLabel reuse. Splitting components from App.tsx keeps each plan ≤ 3 files of meaningful logic and lets Plan 06 focus on integration glue rather than building from scratch.

Output: 5 new component files in entrypoints/popup/components/ — primitives + 4 components. No App.tsx edits.
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-dispatch-popup/03-CONTEXT.md
@.planning/phases/03-dispatch-popup/03-RESEARCH.md
@.planning/phases/03-dispatch-popup/03-PATTERNS.md
@.planning/phases/03-dispatch-popup/03-UI-SPEC.md
@entrypoints/popup/App.tsx
@locales/en.yml
@locales/zh_CN.yml

<interfaces>
<!-- Contracts created by Plan 01 + Plan 02 that this plan consumes verbatim. -->

From Plan 01 — shared/messaging:
```typescript
export type ErrorCode =
  | 'INTERNAL'
  | 'RESTRICTED_URL'
  | 'EXTRACTION_EMPTY'
  | 'EXECUTE_SCRIPT_FAILED'
  | 'NOT_LOGGED_IN'
  | 'INPUT_NOT_FOUND'
  | 'TIMEOUT'
  | 'RATE_LIMITED'
  | 'PLATFORM_UNSUPPORTED';
```

From Plan 02 — shared/storage/repos/history:
```typescript
export interface HistoryEntry {
  value: string;
  last_used_at: string;
  use_count: number;
}
```

From Plan 01 — locales/en.yml + zh_CN.yml (Phase 3 keys this plan consumes — both locales 100% parity):
- `dispatch_in_progress_heading`            → "Sending…"
- `dispatch_in_progress_body_before`        → "Web2Chat is opening the target tab. Click "
- `dispatch_in_progress_body_icon`          → "Cancel"
- `dispatch_in_progress_body_after`         → " to abort."
- `dispatch_cancel_label`                   → "Cancel"
- `dispatch_in_progress_dispatchId_label`   → "dispatchId:"
- `dispatch_in_progress_copy_toast`         → "Copied dispatch ID."
- `error_code_NOT_LOGGED_IN_heading`        → "Not logged in"
- `error_code_NOT_LOGGED_IN_body`           → "Sign in to the target platform in your browser, then retry."
- `error_code_NOT_LOGGED_IN_retry`          → "Retry"
- `error_code_INPUT_NOT_FOUND_*`            → (heading / body / retry)
- `error_code_TIMEOUT_*`                    → (heading / body / retry)
- `error_code_RATE_LIMITED_*`               → (heading / body / retry)
- `error_code_PLATFORM_UNSUPPORTED_heading` → "Unsupported platform"
- `error_code_PLATFORM_UNSUPPORTED_body`    → (no retry key for this code)
- `error_code_dismiss_label`                → aria-label for ✕
- `history_remove_button`                   → "Remove from history" (combobox option ✕ aria-label)
- `history_empty_state`                     → "No history yet — type a URL to start."
- `history_view_all`                        → "View all in settings…"
- `platform_icon_mock`                      → "Mock platform (test)"
- `platform_icon_openclaw`                  → "OpenClaw"
- `platform_icon_discord`                   → "Discord"
- `platform_icon_unsupported`               → "Unsupported platform"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract primitives + create PlatformIcon</name>
  <read_first>
    - entrypoints/popup/App.tsx (full file — copy lines 280-299 verbatim into primitives.tsx)
    - .planning/phases/03-dispatch-popup/03-PATTERNS.md §"entrypoints/popup/components/primitives.tsx (NEW — extract from App.tsx)" lines 750-786
    - .planning/phases/03-dispatch-popup/03-PATTERNS.md §"entrypoints/popup/components/PlatformIcon.tsx (NEW)" lines 559-605
    - .planning/phases/03-dispatch-popup/03-UI-SPEC.md §Iconography lines 480-503
    - .planning/phases/03-dispatch-popup/03-UI-SPEC.md §Color §Toolbar badge palette lines 143-153 (do not introduce green outside badges)
  </read_first>
  <files>
    - entrypoints/popup/components/primitives.tsx (NEW)
    - entrypoints/popup/components/PlatformIcon.tsx (NEW)
  </files>
  <action>
    Step 1 — Create `entrypoints/popup/components/primitives.tsx`. Copy `FieldLabel` (App.tsx:280-286) + `textareaClass` (App.tsx:288-299) verbatim, add a sibling `inputClass` constant for the Combobox `<input>`. Both classes use the SAME focus-ring chrome (sky-600 ring + slate borders + dark variants); inputClass differs from textareaClass only by removing `resize-none field-sizing-content` (Combobox is single-line `<input>`, not `<textarea>`).

    Exact content:
    ```tsx
    /**
     * Shared popup primitives — extracted from App.tsx (lines 280-299) during
     * Phase 3 Plan 05 so options page (Plan 07) can reuse the same focus-ring
     * chrome and label styling. App.tsx will be refactored in Plan 06 to import
     * from this module.
     *
     * Surgical-changes principle (CLAUDE.md): textareaClass is reproduced
     * VERBATIM from App.tsx — do not modify spacing or color tokens. The Phase 2
     * inherited `px-3 py-2` (12px / 8px) padding stays unchanged inside this
     * extracted constant; new components MUST use {4,8,16,24,32,48,64} spacing
     * scale (UI-SPEC §Spacing Scale) but the inherited textarea chrome is
     * exempt because Plan 06 keeps Phase 2 capture preview unchanged.
     */
    import type { ComponentChildren } from 'preact';

    /**
     * Field label using Preact's native `for` attribute (NOT the React-compat
     * `htmlFor` alias). Visible 12px slate-500 text above textareas / comboboxes.
     */
    export function FieldLabel({ id, label }: { id: string; label: string }) {
      return (
        <label
          for={id}
          class="text-xs leading-snug font-normal text-slate-500 dark:text-slate-400"
        >
          {label}
        </label>
      );
    }

    /**
     * Textarea chrome — copied verbatim from popup/App.tsx (Phase 2). Used by
     * the editable Title / Description / Content fields in capture preview.
     * Phase 3 components import this for reuse; Plan 06 also imports it from
     * App.tsx (refactor) to avoid duplicating the focus-ring tokens.
     */
    export const textareaClass = [
      'w-full px-3 py-2 rounded-md',
      'text-sm leading-normal font-normal',
      'text-slate-900 dark:text-slate-100',
      'bg-white dark:bg-slate-900',
      'border border-slate-200 dark:border-slate-700',
      'focus-visible:outline-none',
      'focus-visible:ring-2 focus-visible:ring-sky-600 dark:focus-visible:ring-sky-400',
      'focus-visible:ring-offset-2 focus-visible:ring-offset-white dark:focus-visible:ring-offset-slate-900',
      'focus-visible:border-sky-600 dark:focus-visible:border-sky-400',
      'resize-none field-sizing-content',
    ].join(' ');

    /**
     * Single-line input chrome — same focus-ring + border tokens as textareaClass,
     * minus the textarea-only `resize-none field-sizing-content`. Combobox `<input
     * role="combobox">` uses this. Phase 2 inherited `px-3 py-2` padding stays
     * because Combobox sits in the SendForm region whose vertical cadence (Plan 06
     * UI-SPEC) tolerates this pre-existing density.
     */
    export const inputClass = [
      'w-full px-3 py-2 rounded-md',
      'text-sm leading-normal font-normal',
      'text-slate-900 dark:text-slate-100',
      'bg-white dark:bg-slate-900',
      'border border-slate-200 dark:border-slate-700',
      'focus-visible:outline-none',
      'focus-visible:ring-2 focus-visible:ring-sky-600 dark:focus-visible:ring-sky-400',
      'focus-visible:ring-offset-2 focus-visible:ring-offset-white dark:focus-visible:ring-offset-slate-900',
      'focus-visible:border-sky-600 dark:focus-visible:border-sky-400',
    ].join(' ');

    /** Re-export for downstream type consumers. */
    export type { ComponentChildren };
    ```

    Step 2 — Create `entrypoints/popup/components/PlatformIcon.tsx`. Inline-SVG only (no <img>). 4 variants × optional size prop (default 24, 16 for combobox option usage). All variants use `currentColor` so dark-mode flows through. Default class is `text-slate-500 dark:text-slate-400` for unsupported / neutral; mock variant uses `text-slate-500`; openclaw / discord placeholders use `text-slate-700` (darker because they will be replaced with brand colors in Phase 4/5). NEVER `text-sky-600` for the icon — sky is reserved for Confirm CTA + focus ring + accent span (UI-SPEC §Color reserved-list).

    SVG paths (Lucide-inspired, simplified to fit ≤ 12 lines each):
    - `unsupported` (Lucide globe): `<circle cx='12' cy='12' r='10' /><line x1='2' y1='12' x2='22' y2='12' /><path d='M12 2a15 15 0 0 1 0 20M12 2a15 15 0 0 0 0 20' />`
    - `mock` (Lucide flask-conical): `<path d='M9 2v6L4.5 18.5A2 2 0 0 0 6.3 21h11.4a2 2 0 0 0 1.8-2.5L15 8V2' /><line x1='9' y1='2' x2='15' y2='2' />`
    - `openclaw` (placeholder letterform "Oc"): use `<text x='4' y='17' font-size='14' font-weight='600' fill='currentColor'>Oc</text>` inside the 24x24 viewBox; later replaced in Phase 4
    - `discord` (placeholder letterform "D"): same pattern with `<text x='8' y='17' font-size='16' font-weight='600' fill='currentColor'>D</text>`

    Component skeleton:
    ```tsx
    import { t } from '@/shared/i18n';

    type PlatformVariant = 'mock' | 'openclaw' | 'discord' | 'unsupported';

    interface PlatformIconProps {
      variant: PlatformVariant;
      size?: 16 | 24;
    }

    /**
     * Inline-SVG platform icon — 4 variants per UI-SPEC §Iconography lines 488-499.
     * Phase 4 will replace 'openclaw' letterform with a real brand glyph; Phase 5
     * does the same for 'discord'. The 'mock' + 'unsupported' icons are final.
     *
     * Color discipline (UI-SPEC §Color reserved-list):
     *   - DO NOT use `text-sky-*` here — sky-600 is reserved for Confirm + focus
     *     ring + inline accent span (NOT for platform icons).
     *   - All variants use slate-500/slate-700 + currentColor for dark mode.
     */
    export function PlatformIcon({ variant, size = 24 }: PlatformIconProps) {
      const tooltipKey = `platform_icon_${variant}` as const;
      const tooltip = t(tooltipKey as unknown as 'platform_icon_mock');
      // i18n key types are generated by @wxt-dev/i18n at build time; the cast is
      // a TS narrowing for the variant template literal — runtime uses correct key.

      const colorClass = variant === 'unsupported'
        ? 'text-slate-500 dark:text-slate-400'
        : variant === 'mock'
          ? 'text-slate-500 dark:text-slate-400'
          : 'text-slate-700 dark:text-slate-300'; // placeholders for Phase 4/5

      // Render variant-specific SVG body
      // (Full SVG markup per variant — see action notes above; share viewBox + stroke attrs.)
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width={size}
          height={size}
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          class={colorClass}
          role="img"
          aria-label={tooltip}
        >
          {/* variant body — switch on `variant` and emit correct path/text */}
          {/* See action notes above for exact <path> / <line> / <text> tags per variant */}
        </svg>
      );
    }
    ```

    Implementation notes for executor:
    - The `tooltipKey` template-literal cast is the same workaround used in Phase 2 capture for typed i18n keys; if @wxt-dev/i18n has stricter generated types, fall back to a switch statement: `const tooltip = variant === 'mock' ? t('platform_icon_mock') : variant === 'openclaw' ? t('platform_icon_openclaw') : ...`. Either form is acceptable as long as all 4 keys are referenced at compile time so the i18n CI parity check sees them.
    - Use Preact attribute names: `class` (not className), `stroke-width` (not strokeWidth), `stroke-linecap`, `stroke-linejoin` — match Phase 2 EmptyIcon at App.tsx:302-322 verbatim.
  </action>
  <acceptance_criteria>
    - `test -f entrypoints/popup/components/primitives.tsx && grep -q "export function FieldLabel" entrypoints/popup/components/primitives.tsx && grep -q "export const textareaClass" entrypoints/popup/components/primitives.tsx && grep -q "export const inputClass" entrypoints/popup/components/primitives.tsx`
    - `grep -q "for={id}" entrypoints/popup/components/primitives.tsx` (Preact attribute, NOT htmlFor)
    - `grep -c "htmlFor\|className\|strokeWidth\|tabIndex" entrypoints/popup/components/primitives.tsx entrypoints/popup/components/PlatformIcon.tsx | grep -v ':0$' | wc -l | grep -q "^0$"` (no React-style attributes)
    - `test -f entrypoints/popup/components/PlatformIcon.tsx && grep -q "export function PlatformIcon" entrypoints/popup/components/PlatformIcon.tsx`
    - `grep -E "platform_icon_(mock|openclaw|discord|unsupported)" entrypoints/popup/components/PlatformIcon.tsx | grep -v '^#' | wc -l | awk '{ if ($1 < 4) exit 1 }'` (all 4 i18n keys referenced)
    - `grep -q "stroke-width" entrypoints/popup/components/PlatformIcon.tsx` (Preact attribute)
    - `grep -q "text-sky" entrypoints/popup/components/PlatformIcon.tsx && exit 1 || exit 0` (sky-600 NOT used for icons — UI-SPEC §Color reserved list)
    - `pnpm typecheck && pnpm lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
    primitives.tsx + PlatformIcon.tsx land. Plan 06 + Plan 07 can both `import { FieldLabel, textareaClass, inputClass } from '@/entrypoints/popup/components/primitives'`. Plan 06 SendForm + Combobox listbox option import PlatformIcon.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Combobox component (ARIA 1.2 editable + list autocomplete)</name>
  <read_first>
    - .planning/phases/03-dispatch-popup/03-RESEARCH.md §"Pattern 5 ARIA 1.2 Editable Combobox" lines 539-619 (full pattern + skeleton + Playwright assertion form)
    - .planning/phases/03-dispatch-popup/03-UI-SPEC.md §Component States §Combobox lines 404-444 (visual states + keyboard contract + ARIA attributes)
    - .planning/phases/03-dispatch-popup/03-PATTERNS.md §"entrypoints/popup/components/Combobox.tsx (NEW)" lines 607-655
    - entrypoints/popup/components/primitives.tsx (after Task 1 — inputClass)
    - entrypoints/popup/components/PlatformIcon.tsx (after Task 1)
    - entrypoints/popup/App.tsx (Pattern 2 cancelled-flag pattern at lines 43-70 — NOT used here, Combobox is purely controlled)
  </read_first>
  <files>
    - entrypoints/popup/components/Combobox.tsx (NEW)
  </files>
  <action>
    Create `entrypoints/popup/components/Combobox.tsx`. ~150 lines. ARIA 1.2 editable combobox with list autocomplete (D-30). Component-local state via `useState` (NOT module-level signal — combobox is reused 2× in Plan 06 for send_to + prompt; module-level state would conflate the two instances). Wave-3 SendForm passes filtered options + value + onChange callbacks.

    Exported types + skeleton:
    ```tsx
    import { useState, useRef, useEffect, useId } from 'preact/hooks';
    import { t } from '@/shared/i18n';
    import { inputClass } from './primitives';
    import { PlatformIcon } from './PlatformIcon';

    /**
     * One option in the listbox. `key` is the unique React-key + ARIA-id suffix;
     * `label` is the visible text; `iconVariant` selects PlatformIcon variant for
     * the leading 16×16 icon; `removable` enables the trailing ✕ delete button.
     */
    export interface ComboboxOption {
      key: string;
      label: string;
      iconVariant: 'mock' | 'openclaw' | 'discord' | 'unsupported' | 'none';
      removable: boolean;
    }

    interface ComboboxProps {
      id: string;                                  // input id (e.g. 'popup-field-sendTo')
      label: string;                               // accessible name passed to listbox aria-label
      placeholder?: string;
      value: string;                               // controlled value
      onChange: (next: string) => void;            // called on every input keystroke
      options: ComboboxOption[];                   // filtered list ready to render
      onSelect: (key: string) => void;             // called on Enter or click of option
      onDelete?: (key: string) => void;            // called on ✕ click; optional
      onBlur?: () => void;                         // delegated to parent (e.g. binding upsert debounce)
      emptyStateText?: string;                     // shown when options.length === 0 and listOpen
      leadingIcon?: 'mock' | 'openclaw' | 'discord' | 'unsupported' | 'none';
                                                   // platform icon left of input (DSP-01)
      disabled?: boolean;
    }

    export function Combobox(props: ComboboxProps) {
      const listboxId = `${props.id}-listbox`;
      const [listOpen, setListOpen] = useState(false);
      const [activeIdx, setActiveIdx] = useState(-1);
      const inputRef = useRef<HTMLInputElement | null>(null);
      const blurTimerRef = useRef<number | null>(null);

      // Reset activeIdx when filtered options change so we never point past the
      // end after the user keeps typing (filter narrows below previous activeIdx).
      useEffect(() => {
        if (activeIdx >= props.options.length) setActiveIdx(-1);
      }, [props.options.length]);

      function handleKeyDown(e: KeyboardEvent) {
        if (props.disabled) return;
        const len = props.options.length;
        switch (e.key) {
          case 'ArrowDown': {
            e.preventDefault();
            if (!listOpen) { setListOpen(true); setActiveIdx(0); return; }
            setActiveIdx((idx) => (idx + 1 >= len ? -1 : idx + 1));
            return;
          }
          case 'ArrowUp': {
            e.preventDefault();
            if (!listOpen) { setListOpen(true); setActiveIdx(len - 1); return; }
            setActiveIdx((idx) => (idx <= -1 ? len - 1 : idx - 1));
            return;
          }
          case 'Enter': {
            if (activeIdx >= 0 && activeIdx < len) {
              e.preventDefault();
              const opt = props.options[activeIdx];
              if (opt) {
                props.onSelect(opt.key);
                setListOpen(false);
                setActiveIdx(-1);
              }
            }
            // else: let Enter bubble for form submit (caller decides)
            return;
          }
          case 'Escape': {
            if (listOpen) {
              e.preventDefault();
              setListOpen(false);
              setActiveIdx(-1);
            }
            // value preserved per UI-SPEC line 436
            return;
          }
          case 'Tab': {
            // Per UI-SPEC line 437: close listbox, do NOT preventDefault
            setListOpen(false);
            setActiveIdx(-1);
            return;
          }
        }
      }

      function handleBlur() {
        // Defer close so option onMouseDown fires first (without this the listbox
        // closes before click registers and onSelect never fires).
        blurTimerRef.current = window.setTimeout(() => {
          setListOpen(false);
          setActiveIdx(-1);
          props.onBlur?.();
        }, 150);
      }

      function handleFocus() {
        if (blurTimerRef.current !== null) {
          window.clearTimeout(blurTimerRef.current);
          blurTimerRef.current = null;
        }
        if (!props.disabled) setListOpen(true);
      }

      const showEmptyState = listOpen && props.options.length === 0 && !!props.emptyStateText;

      return (
        <div class="relative">
          <div class="flex items-center gap-2">
            {props.leadingIcon && props.leadingIcon !== 'none' && (
              <PlatformIcon variant={props.leadingIcon} size={24} />
            )}
            <input
              ref={inputRef}
              id={props.id}
              type="text"
              role="combobox"
              aria-expanded={listOpen}
              aria-controls={listboxId}
              aria-autocomplete="list"
              aria-activedescendant={
                activeIdx >= 0 && activeIdx < props.options.length
                  ? `${listboxId}-opt-${activeIdx}`
                  : undefined
              }
              aria-label={props.label}
              class={`${inputClass} flex-1`}
              value={props.value}
              placeholder={props.placeholder}
              disabled={props.disabled}
              onInput={(e) => props.onChange((e.currentTarget as HTMLInputElement).value)}
              onFocus={handleFocus}
              onBlur={handleBlur}
              onKeyDown={handleKeyDown}
              data-testid={`combobox-${props.id}`}
            />
          </div>

          {listOpen && props.options.length > 0 && (
            <ul
              id={listboxId}
              role="listbox"
              aria-label={props.label}
              class="absolute left-0 right-0 mt-1 max-h-64 overflow-y-auto rounded-md border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900 shadow-lg z-10"
            >
              {props.options.map((opt, i) => (
                <li
                  key={opt.key}
                  id={`${listboxId}-opt-${i}`}
                  role="option"
                  aria-selected={i === activeIdx}
                  class={`flex items-center gap-2 min-h-9 px-2 py-2 cursor-pointer ${
                    i === activeIdx
                      ? 'bg-sky-50 dark:bg-sky-950 border-l-2 border-sky-600'
                      : 'hover:bg-slate-100 dark:hover:bg-slate-800'
                  }`}
                  onMouseDown={(e) => {
                    e.preventDefault();
                    props.onSelect(opt.key);
                    setListOpen(false);
                    setActiveIdx(-1);
                  }}
                  data-testid={`combobox-${props.id}-option-${i}`}
                >
                  {opt.iconVariant !== 'none' && (
                    <PlatformIcon variant={opt.iconVariant} size={16} />
                  )}
                  <span
                    class="flex-1 text-sm leading-snug font-normal text-slate-900 dark:text-slate-100 truncate"
                    title={opt.label}
                  >
                    {opt.label}
                  </span>
                  {opt.removable && props.onDelete && (
                    <button
                      type="button"
                      class="text-slate-400 hover:text-red-600 dark:hover:text-red-400"
                      aria-label={t('history_remove_button')}
                      onMouseDown={(e) => {
                        e.preventDefault();
                        e.stopPropagation();
                        props.onDelete?.(opt.key);
                      }}
                      data-testid={`combobox-${props.id}-option-${i}-delete`}
                    >
                      {/* Lucide x icon — 16×16 */}
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        aria-hidden="true"
                      >
                        <line x1="18" y1="6" x2="6" y2="18" />
                        <line x1="6" y1="6" x2="18" y2="18" />
                      </svg>
                    </button>
                  )}
                </li>
              ))}
            </ul>
          )}

          {showEmptyState && (
            <div
              class="absolute left-0 right-0 mt-1 px-2 py-2 rounded-md border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900 shadow-lg text-xs leading-snug font-normal text-slate-400 dark:text-slate-500 z-10"
              data-testid={`combobox-${props.id}-empty`}
            >
              {props.emptyStateText}
            </div>
          )}
        </div>
      );
    }
    ```

    Critical implementation notes:
    - State is local (useState), NOT module-level signal — Combobox is reused 2× in SendForm and module-level state would mix send_to + prompt activeIdx.
    - The 150ms blur defer mirrors common pattern across UI libs; lower values race option click on slow machines, higher values hold listbox open after Tab unnecessarily.
    - `onMouseDown` (not onClick) on options ensures handler fires BEFORE input blur, so blurTimer cancellation isn't needed. e.preventDefault() prevents focus theft.
    - `aria-selected={i === activeIdx}` — true for keyboard-active option only (D-30: active-descendant model, NOT roving tabindex).
    - `aria-activedescendant` references a DOM `id` that exists ONLY when `i === activeIdx` is in range; out-of-range → undefined (omits attribute) per ARIA 1.2 spec.
    - The component handles ONLY the combobox UI. Filtering, history fetch, and binding upsert are caller responsibility (Plan 06 SendForm).
    - NO `useId()` — props.id is the canonical ID supplied by SendForm so external e2e selectors are deterministic (`data-testid="combobox-popup-field-sendTo"`).
  </action>
  <acceptance_criteria>
    - `test -f entrypoints/popup/components/Combobox.tsx && grep -q "export function Combobox" entrypoints/popup/components/Combobox.tsx && grep -q "export interface ComboboxOption" entrypoints/popup/components/Combobox.tsx`
    - `grep -q 'role="combobox"' entrypoints/popup/components/Combobox.tsx && grep -q 'role="listbox"' entrypoints/popup/components/Combobox.tsx && grep -q 'role="option"' entrypoints/popup/components/Combobox.tsx`
    - `grep -q 'aria-expanded' entrypoints/popup/components/Combobox.tsx && grep -q 'aria-controls' entrypoints/popup/components/Combobox.tsx && grep -q 'aria-autocomplete="list"' entrypoints/popup/components/Combobox.tsx && grep -q 'aria-activedescendant' entrypoints/popup/components/Combobox.tsx && grep -q 'aria-selected' entrypoints/popup/components/Combobox.tsx`
    - `grep -q "ArrowDown" entrypoints/popup/components/Combobox.tsx && grep -q "ArrowUp" entrypoints/popup/components/Combobox.tsx && grep -q "case 'Enter'" entrypoints/popup/components/Combobox.tsx && grep -q "case 'Escape'" entrypoints/popup/components/Combobox.tsx && grep -q "case 'Tab'" entrypoints/popup/components/Combobox.tsx`
    - `grep -q "min-h-9" entrypoints/popup/components/Combobox.tsx` (UI-SPEC 36px option exception)
    - `grep -q "history_remove_button" entrypoints/popup/components/Combobox.tsx` (i18n key, not bare string)
    - `grep -E "<button[^>]*>(Remove|Send|Cancel|Retry)" entrypoints/popup/components/Combobox.tsx | wc -l | grep -q "^0$"` (no JSX bare text)
    - `pnpm typecheck && pnpm lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
    Combobox component lands with full ARIA 1.2 contract. Plan 06 SendForm passes (id, label, value, onChange, options, onSelect, onDelete, leadingIcon) — same component used twice (send_to + prompt). E2E (Plan 08) drives via `popup.getByRole('combobox', { name: 'Send to' })`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: InProgressView + ErrorBanner</name>
  <read_first>
    - entrypoints/popup/App.tsx EmptyView lines 207-247 + ErrorView lines 251-272 (centered status-card layout + three-segment inline accent pattern — copy verbatim into InProgressView; ErrorBanner deviates because banner is slim, not full-screen)
    - .planning/phases/03-dispatch-popup/03-PATTERNS.md §"entrypoints/popup/components/InProgressView.tsx (NEW)" lines 657-705
    - .planning/phases/03-dispatch-popup/03-PATTERNS.md §"entrypoints/popup/components/ErrorBanner.tsx (NEW)" lines 707-746
    - .planning/phases/03-dispatch-popup/03-UI-SPEC.md §Component States §In-progress placeholder lines 467-476 + §Error banner lines 455-460
    - .planning/phases/03-dispatch-popup/03-UI-SPEC.md §Copywriting Contract §Error banner lines 244-261 (5 ErrorCode → heading/body/retry mapping)
    - .planning/phases/03-dispatch-popup/03-RESEARCH.md §"Pattern S5 inline accent span" (PITFALLS §11 mitigation; UI-SPEC line 184)
  </read_first>
  <files>
    - entrypoints/popup/components/InProgressView.tsx (NEW)
    - entrypoints/popup/components/ErrorBanner.tsx (NEW)
  </files>
  <action>
    Step 1 — Create `entrypoints/popup/components/InProgressView.tsx`. Centered status card matching the `gap-4` (NOT `gap-2` — Phase 3 strict spacing scale) Phase 3 cadence. Spinner = inline SVG Lucide loader-2 with `class="animate-spin text-sky-600"`. Three-segment inline accent on "Cancel" body. Outline destructive Cancel button. dispatchId footer with click-to-copy.

    ```tsx
    import { signal } from '@preact/signals';
    import { useEffect } from 'preact/hooks';
    import { t } from '@/shared/i18n';

    interface InProgressViewProps {
      dispatchId: string;
      onCancel: () => void;
    }

    /**
     * Dispatch in-progress placeholder (D-31, UI-SPEC §In-progress placeholder).
     * Replaces SendForm + capture preview when storage.session.dispatch:active is in-flight.
     *
     * Visual hierarchy: 24×24 sky-600 spinner + heading is the primary anchor.
     * Cancel is the secondary affordance (red-600 outline, NOT filled).
     *
     * dispatchId footer is intentionally surfaced (not hidden) so users can
     * copy-paste it into bug reports (debug aid, UI-SPEC line 475).
     */
    export function InProgressView({ dispatchId, onCancel }: InProgressViewProps) {
      // Three-segment inline accent on "Cancel" word per Pattern S5 / PITFALLS §11
      const before = t('dispatch_in_progress_body_before');
      const inlineCancel = t('dispatch_in_progress_body_icon');
      const after = t('dispatch_in_progress_body_after');

      // Click-to-copy state — show "Copied dispatch ID." toast for 2 seconds
      const copiedSig = signal(false);

      async function handleCopyDispatchId() {
        try {
          await navigator.clipboard.writeText(dispatchId);
          copiedSig.value = true;
          window.setTimeout(() => { copiedSig.value = false; }, 2_000);
        } catch {
          // clipboard refused — silent (CLAUDE.md "隐私" — no console disclosure)
        }
      }

      // Truncate dispatchId for display: first 4 chars + ellipsis. Full UUID
      // still copies to clipboard.
      const dispatchIdDisplay = dispatchId.slice(0, 4) + '…';

      return (
        <main
          class="flex flex-col items-center text-center p-4 py-8 gap-4 min-w-[360px] min-h-[240px] font-sans"
          role="status"
          aria-live="polite"
          data-testid="dispatch-in-progress"
        >
          <Spinner />
          <h2
            class="m-0 text-base leading-snug font-semibold text-slate-900 dark:text-slate-100"
            data-testid="dispatch-in-progress-heading"
          >
            {t('dispatch_in_progress_heading')}
          </h2>
          <p class="m-0 text-sm leading-normal font-normal text-slate-500 dark:text-slate-400">
            {before}
            <span class="text-sky-600 dark:text-sky-400">{inlineCancel}</span>
            {after}
          </p>
          <button
            type="button"
            class="border border-red-600 text-red-600 hover:bg-red-50 dark:hover:bg-red-950/20 px-4 py-2 rounded-md text-sm font-semibold"
            onClick={onCancel}
            data-testid="dispatch-cancel"
          >
            {t('dispatch_cancel_label')}
          </button>
          <div class="flex items-center gap-2">
            <span class="text-xs leading-snug font-normal text-slate-400 dark:text-slate-500">
              {t('dispatch_in_progress_dispatchId_label')}
            </span>
            <button
              type="button"
              class="text-xs leading-snug font-mono text-slate-400 dark:text-slate-500 hover:text-slate-700 dark:hover:text-slate-300 underline-offset-2 hover:underline"
              onClick={handleCopyDispatchId}
              aria-label={dispatchId}
              data-testid="dispatch-id-copy"
            >
              {dispatchIdDisplay}
            </button>
          </div>
          {copiedSig.value && (
            <p class="text-xs leading-snug font-normal text-slate-500 dark:text-slate-400">
              {t('dispatch_in_progress_copy_toast')}
            </p>
          )}
        </main>
      );
    }

    /** Lucide loader-2 spinner — 24×24, sky-600, animate-spin (UI-SPEC line 471). */
    function Spinner() {
      return (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          class="text-sky-600 dark:text-sky-400 animate-spin"
          aria-hidden="true"
        >
          <path d="M21 12a9 9 0 1 1-6.219-8.56" />
        </svg>
      );
    }
    ```

    Step 2 — Create `entrypoints/popup/components/ErrorBanner.tsx`. Slim banner above SendForm (NOT full-screen). Renders one of 5 ErrorCodes; PLATFORM_UNSUPPORTED has no retry button (UI-SPEC line 256 — body is guidance, not alarm). i18n keys: `error_code_<CODE>_heading` / `_body` / `_retry` (where applicable). Code → has-retry mapping comes from `retriable` flag in ErrorCode trailing comments (result.ts after Plan 01).

    ```tsx
    import { t } from '@/shared/i18n';
    import type { ErrorCode } from '@/shared/messaging';

    interface ErrorBannerProps {
      code: ErrorCode;
      onRetry?: () => void;
      onDismiss: () => void;
    }

    /**
     * Phase 3 dispatch error banner (DSP-07, UI-SPEC §Error banner lines 455-460).
     * Slim red-600 left-border treatment ABOVE SendForm — NOT full-screen.
     *
     * Five Phase 3 ErrorCodes mapped here:
     *   NOT_LOGGED_IN | INPUT_NOT_FOUND | TIMEOUT | RATE_LIMITED | PLATFORM_UNSUPPORTED
     *
     * Plus Phase 1+2 codes that may surface during dispatch (re-rendered with
     * generic heading + body): INTERNAL | RESTRICTED_URL | EXTRACTION_EMPTY |
     * EXECUTE_SCRIPT_FAILED. The latter four typically only appear in capture flow,
     * but defensive rendering here means a runtime mismatch surfaces a banner
     * rather than silently rendering nothing.
     *
     * IMPORTANT — XSS/T-03-04-03 mitigation: this component does NOT render
     * `error.message` (raw exception text from SW). Only `t('error_code_<CODE>_*')`
     * i18n strings reach the DOM. error.message remains in storage.session for
     * SW-side diagnostics; users get a code-mapped human-readable copy.
     */

    /** ErrorCodes whose human-readable `_retry` button MUST exist in i18n. */
    const RETRIABLE_CODES: ReadonlySet<ErrorCode> = new Set<ErrorCode>([
      'NOT_LOGGED_IN',
      'INPUT_NOT_FOUND',
      'TIMEOUT',
      'RATE_LIMITED',
      'EXECUTE_SCRIPT_FAILED',
      'INTERNAL',
    ]);

    export function ErrorBanner({ code, onRetry, onDismiss }: ErrorBannerProps) {
      const heading = errorHeading(code);
      const body = errorBody(code);
      const retryLabel = errorRetry(code);
      const showRetry = RETRIABLE_CODES.has(code) && !!onRetry && retryLabel !== '';

      return (
        <div
          class="bg-red-50 dark:bg-red-950/40 border-l-4 border-red-600 p-4 rounded-r-md flex items-start gap-2"
          role="alert"
          aria-live="assertive"
          data-testid={`error-banner-${code}`}
        >
          <div class="flex-1">
            <h3 class="m-0 text-sm leading-snug font-semibold text-red-700 dark:text-red-300">
              {heading}
            </h3>
            <p class="mt-1 m-0 text-sm leading-normal font-normal text-slate-700 dark:text-slate-300">
              {body}
            </p>
            {showRetry && (
              <button
                type="button"
                class="mt-2 text-sm font-semibold text-red-600 hover:text-red-700 dark:hover:text-red-400 underline-offset-2 hover:underline"
                onClick={onRetry}
                data-testid={`error-banner-${code}-retry`}
              >
                {retryLabel}
              </button>
            )}
          </div>
          <button
            type="button"
            class="text-slate-400 hover:text-slate-700 dark:hover:text-slate-200"
            aria-label={t('error_code_dismiss_label')}
            onClick={onDismiss}
            data-testid={`error-banner-${code}-dismiss`}
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <line x1="18" y1="6" x2="6" y2="18" />
              <line x1="6" y1="6" x2="18" y2="18" />
            </svg>
          </button>
        </div>
      );
    }

    // ─── i18n key resolvers (switch on ErrorCode for typed-key correctness) ──

    function errorHeading(code: ErrorCode): string {
      switch (code) {
        case 'NOT_LOGGED_IN': return t('error_code_NOT_LOGGED_IN_heading');
        case 'INPUT_NOT_FOUND': return t('error_code_INPUT_NOT_FOUND_heading');
        case 'TIMEOUT': return t('error_code_TIMEOUT_heading');
        case 'RATE_LIMITED': return t('error_code_RATE_LIMITED_heading');
        case 'PLATFORM_UNSUPPORTED': return t('error_code_PLATFORM_UNSUPPORTED_heading');
        case 'EXECUTE_SCRIPT_FAILED': return t('error_code_EXECUTE_SCRIPT_FAILED_heading');
        case 'INTERNAL': return t('error_code_INTERNAL_heading');
        case 'RESTRICTED_URL': return t('error_code_RESTRICTED_URL_heading');
        case 'EXTRACTION_EMPTY': return t('error_code_EXTRACTION_EMPTY_heading');
      }
    }
    function errorBody(code: ErrorCode): string { /* same switch shape */
      switch (code) {
        case 'NOT_LOGGED_IN': return t('error_code_NOT_LOGGED_IN_body');
        case 'INPUT_NOT_FOUND': return t('error_code_INPUT_NOT_FOUND_body');
        case 'TIMEOUT': return t('error_code_TIMEOUT_body');
        case 'RATE_LIMITED': return t('error_code_RATE_LIMITED_body');
        case 'PLATFORM_UNSUPPORTED': return t('error_code_PLATFORM_UNSUPPORTED_body');
        case 'EXECUTE_SCRIPT_FAILED': return t('error_code_EXECUTE_SCRIPT_FAILED_body');
        case 'INTERNAL': return t('error_code_INTERNAL_body');
        case 'RESTRICTED_URL': return t('error_code_RESTRICTED_URL_body');
        case 'EXTRACTION_EMPTY': return t('error_code_EXTRACTION_EMPTY_body');
      }
    }
    function errorRetry(code: ErrorCode): string {
      switch (code) {
        case 'NOT_LOGGED_IN': return t('error_code_NOT_LOGGED_IN_retry');
        case 'INPUT_NOT_FOUND': return t('error_code_INPUT_NOT_FOUND_retry');
        case 'TIMEOUT': return t('error_code_TIMEOUT_retry');
        case 'RATE_LIMITED': return t('error_code_RATE_LIMITED_retry');
        case 'EXECUTE_SCRIPT_FAILED': return t('error_code_EXECUTE_SCRIPT_FAILED_retry');
        case 'INTERNAL': return t('error_code_INTERNAL_retry');
        // PLATFORM_UNSUPPORTED / RESTRICTED_URL / EXTRACTION_EMPTY have no retry
        default: return '';
      }
    }
    ```

    Implementation notes:
    - Plan 01 must add EVERY i18n key referenced here in BOTH locales for parity. The 9 error codes × 3 sub-keys (heading/body/retry where applicable) — see the must_haves of Plan 01. The 3 codes without retry (PLATFORM_UNSUPPORTED, RESTRICTED_URL, EXTRACTION_EMPTY) only need _heading + _body.
    - The exhaustive switch with no `default` clause would force a TS error if a new ErrorCode is added without a heading key — leave it `default` style (not exhaustive) to keep the component resilient to future additions, but verify all current 9 codes are covered.
    - `error.message` (raw text from SW) is NOT rendered. Plan 04 succeedDispatch / failDispatch writes it to storage.session for SW-side diagnostics; popup never surfaces it (T-03-04-03 mitigation).
    - PLATFORM_UNSUPPORTED renders heading + body only — no retry. UI-SPEC line 256 + 261.
  </action>
  <acceptance_criteria>
    - `test -f entrypoints/popup/components/InProgressView.tsx && grep -q "export function InProgressView" entrypoints/popup/components/InProgressView.tsx`
    - `grep -q "animate-spin" entrypoints/popup/components/InProgressView.tsx && grep -q "text-sky-600" entrypoints/popup/components/InProgressView.tsx`
    - `grep -q "gap-4" entrypoints/popup/components/InProgressView.tsx` (Phase 3 spacing — NOT gap-2)
    - `grep -q "navigator.clipboard.writeText" entrypoints/popup/components/InProgressView.tsx`
    - `grep -q 'data-testid="dispatch-in-progress"' entrypoints/popup/components/InProgressView.tsx && grep -q 'data-testid="dispatch-cancel"' entrypoints/popup/components/InProgressView.tsx && grep -q 'data-testid="dispatch-id-copy"' entrypoints/popup/components/InProgressView.tsx`
    - `test -f entrypoints/popup/components/ErrorBanner.tsx && grep -q "export function ErrorBanner" entrypoints/popup/components/ErrorBanner.tsx`
    - `grep -q "border-l-4 border-red-600" entrypoints/popup/components/ErrorBanner.tsx` (slim banner, not full-screen)
    - `grep -E "case '(NOT_LOGGED_IN|INPUT_NOT_FOUND|TIMEOUT|RATE_LIMITED|PLATFORM_UNSUPPORTED)'" entrypoints/popup/components/ErrorBanner.tsx | grep -v '^#' | wc -l | awk '{ if ($1 < 15) exit 1 }'` (5 codes × 3 switch branches = 15 lines)
    - `grep -q "error.message" entrypoints/popup/components/ErrorBanner.tsx && exit 1 || exit 0` (raw error.message NEVER rendered — T-03-04-03)
    - `pnpm typecheck && pnpm lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
    InProgressView + ErrorBanner land. Plan 06 popup integration consumes both. PLATFORM_UNSUPPORTED renders without retry button. Raw SW error.message never reaches the DOM (T-03-04-03 mitigation).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| popup → user clipboard | InProgressView click-to-copy writes dispatchId to navigator.clipboard; subject to user gesture restriction |
| Combobox option onSelect → SendForm callback | Caller-controlled — Plan 06 SendForm validates via existing send_to URL parsing |
| ErrorBanner → ErrorCode union | Code is from typed RPC response; ErrorBanner switches on union exhaustively (no string interpolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | I (Information Disclosure) | ErrorBanner could leak SW internals if it rendered `error.message` | mitigate | ErrorBanner explicitly does NOT accept or render error.message; only the typed ErrorCode is passed in. Switch on ErrorCode → i18n key. Verified by grep acceptance criterion (error.message must not appear) |
| T-03-05-02 | T (Tampering) | Combobox option label could contain malicious URL-shaped text | mitigate | Labels render as Preact text nodes via `<span>{opt.label}</span>` — no innerHTML, no dangerouslySetInnerHTML. PITFALLS §11 / Pattern S5 already locked. `title={opt.label}` for hover-tooltip uses Preact attribute (text-only) |
| T-03-05-03 | I | dispatchId copy-to-clipboard could expose UUID to other browser windows via clipboard | accept | dispatchId is opaque random UUID with no PII; user explicitly clicks to copy. Clipboard API requires user gesture (click handler). Standard browser security model |
| T-03-05-04 | T | Combobox onDelete handler could be hijacked if opt.key is user-controlled | accept | opt.key is generated by SendForm caller (Plan 06) from typed HistoryEntry.value; not user-controlled at component boundary. Typed unique keys per ARIA listbox spec |
| T-03-05-05 | I | InProgressView dispatchId footer is always visible (T-03-05-03 weak ack) | accept | UI-SPEC line 475 declares dispatchId visible "as debug aid kept visible so users can attach it to bug reports". User opt-in copy. No PII in UUID |
| T-03-05-06 | E (Elevation) | Component imports from cross-package (@/shared/messaging) — typed ErrorCode | mitigate | Type-only imports (`import type`) prevent runtime bundle bloat; ErrorCode is compile-time-only |
</threat_model>

<verification>
- All 5 component files compile cleanly (`pnpm typecheck`)
- ESLint passes (`pnpm lint`) — including no-bare-JSX-string rule (FND-06)
- All 4 PlatformIcon variants reference unique i18n keys (`platform_icon_mock|openclaw|discord|unsupported`)
- All 5 ErrorCode-specific i18n keys referenced for heading + body + retry (where applicable) — Plan 01 must define these
- No React-style attributes (`className`/`htmlFor`/`strokeWidth`/`tabIndex`)
- No bare JSX text literals (FND-06 / D-12)
- error.message NEVER rendered in ErrorBanner (T-03-05-01)
- Combobox uses local useState (not module-level signal — required for 2× reuse in Plan 06)
- Phase 3 spacing scale {4,8,16,24,32,48,64} only (with documented 36px combobox-option exception)
</verification>

<success_criteria>
- Plan 06 (popup integration) imports `Combobox`, `PlatformIcon`, `InProgressView`, `ErrorBanner`, `FieldLabel`, `inputClass`, `textareaClass` and composes them into SendForm + popup state machine
- Plan 07 (options page) imports `FieldLabel` from primitives.tsx for the Reset section explainer label
- Plan 08 (e2e) drives Combobox via ARIA semantics: `popup.getByRole('combobox', { name: t('combobox label') })`
- Phase 4 swap: replacing the placeholder PlatformIcon openclaw letterform with a real SVG = 1-file change in PlatformIcon.tsx; no Combobox / SendForm changes
- Phase 6 i18n hardening sees all Phase 3 dispatch / error / history / binding / platform_icon keys already in en + zh_CN locale files (Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/03-dispatch-popup/03-05-SUMMARY.md`. Note any deviation from UI-SPEC §Component States (especially Combobox keyboard behavior — easiest to misimplement). Document the 150ms blur defer constant choice.
</output>
