---
phase: 07-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-zip.ts
  - scripts/verify-readme-anchors.ts
  - wxt.config.ts
  - package.json
autonomous: true
requirements: [DST-01, DST-03, DST-04]

must_haves:
  truths:
    - "pnpm build && pnpm zip produces a zip that passes verify-zip assertions"
    - "verify-readme-anchors.ts validates bilingual README heading parity"
    - "mock-platform.js is excluded from production zip"
    - "manifest permissions assertion still passes (DST-03 regression guard)"
  artifacts:
    - path: "scripts/verify-zip.ts"
      provides: "Zip content structural assertions"
      contains: "[verify-zip] OK"
    - path: "scripts/verify-readme-anchors.ts"
      provides: "Bilingual anchor parity validation"
      contains: "[verify-readme] OK"
    - path: "wxt.config.ts"
      provides: "Zip exclude configuration"
      contains: "zip:"
    - path: "package.json"
      provides: "New verify scripts"
      contains: "verify:zip"
  key_links:
    - from: "package.json"
      to: "scripts/verify-zip.ts"
      via: "verify:zip script"
      pattern: "tsx scripts/verify-zip.ts"
    - from: "package.json"
      to: "scripts/verify-readme-anchors.ts"
      via: "verify:readme script"
      pattern: "tsx scripts/verify-readme-anchors.ts"
---

<objective>
Build verification tooling for Phase 7 distribution: zip content assertion script, README anchor parity checker, wxt.config.ts zip.exclude, and package.json script entries.

Purpose: Provide automated validation that the CWS zip is structurally correct and bilingual documentation maintains parity — these are the "Wave 0" test infrastructure required by VALIDATION.md.
Output: Two verification scripts + config changes, all green on `pnpm verify:zip` and `pnpm verify:readme`.
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distribution/07-RESEARCH.md
@.planning/phases/07-distribution/07-PATTERNS.md

<interfaces>
From scripts/verify-manifest.ts (pattern to replicate):
```typescript
#!/usr/bin/env tsx
// JSDoc header with requirement IDs
import { readFileSync, existsSync, readdirSync } from 'node:fs';
import { resolve } from 'node:path';

// Error collector pattern
const errors: string[] = [];
// ... assertions push to errors[] ...

if (errors.length) {
  console.error('[script-name] FAIL:');
  for (const e of errors) console.error('  -', e);
  process.exit(1);
}
console.log('[script-name] OK -- summary');
```

From wxt.config.ts (current structure — add zip key as peer to modules/vite):
```typescript
export default defineConfig({
  manifest: ({ mode }) => ({ ... }),
  modules: ['@wxt-dev/i18n/module'],
  vite: () => ({
    plugins: [tailwindcss(), yamlLocalePlugin()],
  }),
});
```

From package.json scripts section (current):
```json
"verify:manifest": "wxt build && tsx scripts/verify-manifest.ts",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-zip.ts + verify-readme-anchors.ts scripts</name>
  <files>scripts/verify-zip.ts, scripts/verify-readme-anchors.ts</files>
  <read_first>
    - scripts/verify-manifest.ts (pattern: shebang, JSDoc, error collector, exit codes)
    - scripts/i18n-coverage.ts (pattern: file reading, comparison, report)
  </read_first>
  <action>
Create `scripts/verify-zip.ts`:
- Shebang: `#!/usr/bin/env tsx`
- JSDoc: cite DST-01, assert zip structure (manifest at root, icons, locales, no source maps, no mock-platform.js)
- Read `.output/` directory, find the single `*-chrome.zip` file
- Use `execSync('unzip -l "zipPath"', { encoding: 'utf-8' })` to get file listing
- Assert all of:
  - Exactly 1 `*-chrome.zip` file exists in `.output/`
  - `manifest.json` appears in listing (at root level)
  - `icon/128.png` appears in listing
  - `icon/48.png` appears in listing
  - `_locales/en/messages.json` appears in listing
  - `_locales/zh_CN/messages.json` appears in listing
  - No line matches `\.map\s` (source maps)
  - No line matches `mock-platform` (test-only dead code)
- Error collector pattern: push to `errors: string[]`
- Success: `console.log('[verify-zip] OK -- <filename> (<size>KB)')`
- Failure: `console.error('[verify-zip] FAIL:')` + each error + `process.exit(1)`
- Use `statSync` to get file size for OK message

Create `scripts/verify-readme-anchors.ts`:
- Shebang: `#!/usr/bin/env tsx`
- JSDoc: cite DST-04 (bilingual anchor parity) + DST-02 (PRIVACY file existence)
- Read `README.md` and `README.en.md` from repo root
- Extract all `## ` headings from each file into arrays
- Assert heading COUNT is equal between zh_CN and en versions
- Assert `PRIVACY.md` exists (readFileSync or existsSync)
- Assert `PRIVACY.zh_CN.md` exists
- Error collector pattern same as verify-zip
- Success: `console.log('[verify-readme] OK -- both READMEs have N sections, PRIVACY files present')`
- Failure: list which headings are missing + which PRIVACY files absent + `process.exit(1)`
  </action>
  <acceptance_criteria>
    - `scripts/verify-zip.ts` exists and contains `#!/usr/bin/env tsx`
    - `scripts/verify-zip.ts` contains `[verify-zip] FAIL:`
    - `scripts/verify-zip.ts` contains `[verify-zip] OK`
    - `scripts/verify-zip.ts` contains `mock-platform`
    - `scripts/verify-zip.ts` contains `icon/128.png`
    - `scripts/verify-zip.ts` contains `_locales/en/messages.json`
    - `scripts/verify-zip.ts` contains `process.exit(1)`
    - `scripts/verify-readme-anchors.ts` exists and contains `#!/usr/bin/env tsx`
    - `scripts/verify-readme-anchors.ts` contains `[verify-readme] FAIL:`
    - `scripts/verify-readme-anchors.ts` contains `[verify-readme] OK`
    - `scripts/verify-readme-anchors.ts` contains `PRIVACY.md`
    - `scripts/verify-readme-anchors.ts` contains `PRIVACY.zh_CN.md`
    - `scripts/verify-readme-anchors.ts` contains `README.en.md`
    - `pnpm typecheck` passes (no type errors in new scripts)
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Both scripts exist, type-check clean, follow verify-manifest.ts pattern (shebang, JSDoc, error collector, exit codes)</done>
</task>

<task type="auto">
  <name>Task 2: Add zip.exclude to wxt.config.ts + package.json scripts</name>
  <files>wxt.config.ts, package.json</files>
  <read_first>
    - wxt.config.ts (current state — insertion point after modules key)
    - package.json (current scripts section)
  </read_first>
  <action>
Edit `wxt.config.ts`:
- Add `zip` key as a peer of `modules` and `vite` (before or after `vite`):
```typescript
  zip: {
    exclude: ['content-scripts/mock-platform.js'],
  },
```

Edit `package.json` scripts section:
- Add after existing `"verify:manifest"` line:
```json
"verify:zip": "wxt build && wxt zip && tsx scripts/verify-zip.ts",
"verify:readme": "tsx scripts/verify-readme-anchors.ts"
```

Run `pnpm build && pnpm zip` to produce `.output/` artifacts, then run `tsx scripts/verify-zip.ts` to confirm assertions pass against actual build output.

Note: verify-readme will intentionally fail at this point because README.md and README.en.md have not been rewritten yet (Plan 03 handles that). This is expected -- the script is ready for when those files land.
  </action>
  <acceptance_criteria>
    - `wxt.config.ts` contains the string `zip:` (as a config key)
    - `wxt.config.ts` contains `content-scripts/mock-platform.js`
    - `package.json` contains `"verify:zip":`
    - `package.json` contains `"verify:readme":`
    - `package.json` contains `tsx scripts/verify-zip.ts`
    - `package.json` contains `tsx scripts/verify-readme-anchors.ts`
    - `pnpm build && pnpm zip && tsx scripts/verify-zip.ts` exits 0
    - zip listing does NOT contain `mock-platform`
    - `pnpm verify:manifest` still passes (DST-03 regression guard)
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm zip && tsx scripts/verify-zip.ts && pnpm verify:manifest</automated>
  </verify>
  <done>wxt.config.ts has zip.exclude, package.json has verify:zip + verify:readme scripts, verify-zip passes on actual build, verify:manifest still green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output -> zip | Build artifacts packaged without modification |
| Zip -> CWS upload | Zip must meet CWS structural requirements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | verify-zip.ts | accept | Script only reads file listing metadata, no sensitive content exposed; runs locally/CI only |
| T-07-01-02 | Tampering | zip contents | mitigate | verify-zip.ts asserts no source maps (could leak internal paths) and no test-only files in production zip |
| T-07-01-03 | Elevation of Privilege | manifest permissions | mitigate | verify-manifest.ts already asserts no static `<all_urls>` in host_permissions; verify-zip confirms same manifest in zip |
</threat_model>

<verification>
- `pnpm build && pnpm zip && tsx scripts/verify-zip.ts` exits 0
- `pnpm verify:manifest` exits 0 (DST-03 regression)
- `pnpm typecheck` clean (no errors in new scripts)
- zip file does not contain mock-platform.js
- zip file does not contain .map files
</verification>

<success_criteria>
- Two new verification scripts exist and type-check
- wxt.config.ts excludes mock-platform from zip
- package.json has verify:zip and verify:readme script entries
- verify-zip passes on real build output
- verify:manifest still green (DST-03 confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/07-distribution/07-01-SUMMARY.md`
</output>
