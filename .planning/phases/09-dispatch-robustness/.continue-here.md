---
context: phase
phase: 09-dispatch-robustness
task: 1
total_tasks: 3
status: blocked
last_updated: 2026-05-10T17:17:26.823Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Worktree CWD drift — 本次执行中 shell 一度停在 `.claude/worktrees/agent-*` 目录，导致一次 merge 判断成 “Already up to date” 的假象。— 继续任何 git merge/commit 之前，必须先 `pwd` + `git rev-parse --abbrev-ref HEAD`；若不在仓库根目录 `main`，先切回 `/Users/seven/data/coding/projects/seven/web2chat` 再操作。

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree CWD drift | 在检查/合并 executor worktree 之后，当前 shell 可能残留在 `.claude/worktrees/agent-*` 内，后续 git 命令会针对错误工作树执行并给出误导性结果。 | blocking | 每次恢复后、每次 merge/commit 前，先执行 `pwd` 和 `git rev-parse --abbrev-ref HEAD`；如不在 repo root 的 `main`，立即切回主仓库根目录。 |
| Missing completion marker despite completed work | 09-03 executor 首次因 503/缺 marker 看似失败，但 worktree 中其实已有 RED/GREEN/REFACTOR 提交，只差 SUMMARY。 | advisory | 遇到 agent failure/无 marker 时，先 spot-check worktree 的 `git log`、`*-SUMMARY.md`、目标文件，再决定重跑还是补收尾。 |

<current_state>
Phase 09（投递鲁棒性）代码实现已经全部完成：09-01..09-05 五个 plans 全部执行、合并、通过 post-merge 测试门；deep code review 也已完成并修复所有 actionable findings。当前停在 phase verifier 之后的 `human_needed` 阶段：`09-VERIFICATION.md` 给出 4/4 must-haves verified，但仍要求 3 个真实 Chrome 扩展运行时的人测项。`09-HUMAN-UAT.md` 已创建并提交。
</current_state>

<completed_work>

Completed Tasks:
- Task 1: 执行 09-01..09-05 并完成 wave 合并、回归测试、typecheck、lint、i18n、build — Done
- Task 2: 执行 deep code review，并修复 CR-01 / WR-01 / WR-02 / WR-03 — Done
- Task 3: 生成 `09-VERIFICATION.md`，确认 4/4 must-haves verified；生成人工测试清单 `09-HUMAN-UAT.md` — In progress（等待用户完成真人测试）
</completed_work>

<remaining_work>

- Human UAT 1：验证 TIMEOUT 后 Retry 按钮出现，点击后 fresh dispatch + 当前表单值
- Human UAT 2：验证 Discord tier3 selector 时 `SelectorWarningDialog` 出现；Cancel 不发送；Confirm 只发送一次
- Human UAT 3：验证 `SelectorWarningDialog` 的 focus trap / Escape / overlay click / autofocus / 读屏语义
- 若用户回复 `approved`：走 phase completion（`phase.complete` + 提交 `09-VERIFICATION.md` / ROADMAP / STATE / REQUIREMENTS）
- 若用户报告问题：转入 gap/fix 路径
</remaining_work>

<decisions_made>

- 先把可自动化的人测部分尽量测掉：`mock-platform.html?fail=timeout` 的 Retry 场景已经自动验证通过，只剩真实 Discord / a11y 部分要用户手测。
- code review 的 blocker/warnings 先修掉再进 verifier，避免把已知问题带入人工验收。
- `09-HUMAN-UAT.md` 是后续恢复时的人测唯一清单；以它和 `09-VERIFICATION.md` 为准，不再重新推导测试项。
</decisions_made>

<blockers>
- Human verification required：当前唯一阻塞是 3 项真人测试，不能自动完成。
- Uncommitted verification artifact：`09-VERIFICATION.md` 目前仍未提交，应在 human verification 结束并确认继续后一起纳入 phase completion 提交。
- Unrelated file present：`.planning/phases/08-architecture-generalization/08-CROSS-REVIEWS.md` 是本次会话遗留的无关未跟踪文件，不要误纳入 Phase 09 completion 提交。
</blockers>

## Required Reading (in order)
1. `.planning/phases/09-dispatch-robustness/09-VERIFICATION.md` — 当前 verifier 结论、3 个 human verification 项、4/4 must-haves 证据
2. `.planning/phases/09-dispatch-robustness/09-HUMAN-UAT.md` — 人测执行清单与结果记录位置
3. `.planning/phases/09-dispatch-robustness/09-REVIEW.md` — 已修复的 CR/WR 背景，避免重复怀疑已关闭问题
4. `.planning/STATE.md` — 当前 Phase 09 verifying 状态与 milestone 进度
5. `CLAUDE.md` — 项目级约束，尤其是 MV3、adapter、GSD 工作流和测试要求

## Critical Anti-Patterns (do NOT repeat these)
- **WORKTREE CWD DRIFT**：在 worktree 中执行主仓库 merge/commit → 先 `pwd` + `git rev-parse --abbrev-ref HEAD`，确认在 repo root 的 `main`
- **ASSUME AGENT FAILURE WITHOUT SPOT-CHECK**：agent 因 503/缺 marker 报错不代表工作没完成 → 先检查 worktree `git log`、`SUMMARY.md`、目标文件，再决定是否重跑

## Infrastructure State
- 本地 fixture 静态服务已由用户手动后台启动（ID: `bgp1vli24`），服务目录为 `tests/e2e/fixtures`，端口 `4321`
- `.output/chrome-mv3` 与 `.output/chrome-mv3-dev` 都已构建完成
- 用户的 Chrome 中已加载扩展（由先前对话确认）
- 自动化验证最后一次全量通过：43 test files / 297 tests，`pnpm typecheck`、`pnpm lint`、`pnpm test:i18n-coverage`、`pnpm build` 全绿

<context>
当前已经不需要继续写代码，重点是把剩余的人测项一步步引导用户完成。用户明确偏好“一个一个来”，所以恢复后不要一次性抛出全部手测步骤。应从 `09-HUMAN-UAT.md` 的第 1 项开始，逐步确认。若用户说 `approved`，可认为 3 项均通过；否则按用户逐项反馈进入修复循环。
</context>

<next_action>
Start with: 先确认当前 shell 在 `/Users/seven/data/coding/projects/seven/web2chat` 的 `main`，然后继续以“一次一步”的方式引导用户执行 `09-HUMAN-UAT.md` 的第 1 项（Retry 人测），等待用户反馈 `approved` 或具体现象。
</next_action>
