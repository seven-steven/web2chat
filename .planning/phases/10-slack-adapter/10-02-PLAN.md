---
phase: 10-slack-adapter
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/adapters/slack-login-detect.ts
  - tests/unit/adapters/slack-login-detect.spec.ts
  - tests/unit/adapters/slack-match.spec.ts
autonomous: true
requirements:
  - SLK-01
  - SLK-02

must_haves:
  truths:
    - "Slack URL 匹配只接受 app.slack.com/client/<ws>/<channel> 四段路径"
    - "Slack 登录墙 DOM 检测在邮箱输入框、登录按钮、signin class 存在时返回 true"
    - "正常频道编辑器 UI 不会触发登录检测误报"
  artifacts:
    - path: "shared/adapters/slack-login-detect.ts"
      provides: "Slack DOM login wall detection"
      exports: ["detectLoginWall"]
    - path: "tests/unit/adapters/slack-login-detect.spec.ts"
      provides: "Login detection unit tests"
    - path: "tests/unit/adapters/slack-match.spec.ts"
      provides: "URL match unit tests"
  key_links:
    - from: "shared/adapters/slack-login-detect.ts"
      to: "entrypoints/slack.content.ts"
      via: "import { detectLoginWall }"
      pattern: "detectLoginWall"
    - from: "shared/adapters/registry.ts"
      to: "tests/unit/adapters/slack-match.spec.ts"
      via: "import { findAdapter, detectPlatformId }"
      pattern: "findAdapter.*slack"
---

<objective>
实现 Slack 登录墙 DOM 检测 + Slack URL 匹配测试。

Purpose: 为 Slack 适配器提供登录检测能力（D-138），并为 registry 的 Slack URL 匹配提供测试覆盖（SLK-01）。登录检测是 Slack 适配器的关键防护——因为 Slack 登录重定向从 app.slack.com 跨域到 slack.com，URL 层 loggedOutPathPatterns 无法捕获（RESEARCH Pitfall 1）。
Output: `slack-login-detect.ts` + 测试 + URL 匹配测试
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-slack-adapter/10-CONTEXT.md
@.planning/phases/10-slack-adapter/10-RESEARCH.md
@.planning/phases/10-slack-adapter/10-PATTERNS.md

<interfaces>
<!-- Discord login detect — structural template for Slack login detect -->
From shared/adapters/discord-login-detect.ts:
```typescript
export function detectLoginWall(): boolean {
  return Boolean(
    document.querySelector('input[name="email"][type="email"]') ||
    document.querySelector('[class*="authBox"]') ||
    document.querySelector('a[href="/login"]'),
  );
}
```

<!-- Registry match function interface -->
From shared/adapters/registry.ts:
```typescript
export function findAdapter(url: string): AdapterRegistryEntry | undefined;
export function detectPlatformId(url: string): PlatformId | null;
```
</interfaces>
</context>

<feature>
  <name>Slack login detection + URL match tests</name>
  <files>shared/adapters/slack-login-detect.ts, tests/unit/adapters/slack-login-detect.spec.ts, tests/unit/adapters/slack-match.spec.ts</files>
  <behavior>
    detectLoginWall:
    - 邮箱输入框存在 -> true
    - data-qa="sign_in_button" 按钮存在 -> true
    - class 含 "signin" 子串 -> true
    - class 含 "login" 子串 -> true
    - 正常频道编辑器（.ql-editor） -> false
    - 空 body -> false
    - 残留 contenteditable + 登录 UI 共存 -> true（不误报为正常）

    URL match (via registry — tested after Plan 03 adds the entry):
    - Valid: https://app.slack.com/client/worksapce123/channel456
    - Valid: https://app.slack.com/client/w/c?foo=bar
    - Invalid: https://app.slack.com/client/ (missing ws+channel)
    - Invalid: https://app.slack.com/client/w/ (missing channel)
    - Invalid: https://slack.com/check-login (wrong host)
    - Invalid: https://discord.com/channels/1/2 (wrong platform)
    - Invalid: malformed / empty
  </behavior>
  <implementation>
    slack-login-detect.ts: 参考discord-login-detect.ts，替换 DOM 选择器为 Slack 登录页标记。纯函数，无 chrome.* 依赖。

    slack-match.spec.ts: 参考 discord-match.spec.ts 结构。注意：此测试依赖 registry 中的 slack entry（Plan 03 添加）。因此 RED 阶段测试会在 Plan 03 完成后才 GREEN。Plan 02 先创建测试文件和 login-detect 实现，URL match 测试在 Plan 03 完成后验证。
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Login detect tests + URL match tests</name>
  <files>tests/unit/adapters/slack-login-detect.spec.ts, tests/unit/adapters/slack-match.spec.ts</files>
  <read_first>
    tests/unit/adapters/discord-login-detect.spec.ts
    tests/unit/adapters/discord-match.spec.ts
    shared/adapters/discord-login-detect.ts
    .planning/phases/10-slack-adapter/10-RESEARCH.md (Pattern 4)
  </read_first>
  <acceptance_criteria>
    - File `tests/unit/adapters/slack-login-detect.spec.ts` exists
    - File imports `detectLoginWall` from `@/shared/adapters/slack-login-detect`
    - Contains tests for: normal editor (false), email input (true), sign_in_button (true), signin class (true), empty body (false), stale contenteditable + login (true)
    - File `tests/unit/adapters/slack-match.spec.ts` exists
    - File imports `findAdapter, detectPlatformId` from `@/shared/adapters/registry`
    - Contains valid URL tests and invalid URL tests for Slack
    - `pnpm test:unit -- tests/unit/adapters/slack-login-detect.spec.ts` exits with failures (RED — import missing)
  </acceptance_criteria>
  <action>
    创建 `tests/unit/adapters/slack-login-detect.spec.ts`，参考 `discord-login-detect.spec.ts` 结构：

    ```typescript
    import { afterEach, beforeEach, describe, it, expect } from 'vitest';
    import { detectLoginWall } from '@/shared/adapters/slack-login-detect';

    describe('Slack login wall detection (SLK-02)', () => {
      beforeEach(() => { document.body.innerHTML = ''; });
      afterEach(() => { document.body.innerHTML = ''; });

      it('returns false when channel UI editor is present', () => {
        document.body.innerHTML = `
          <div class="ql-editor" role="textbox" contenteditable="true" aria-label="Message #general">
            <p><br></p>
          </div>
        `;
        expect(detectLoginWall()).toBe(false);
      });

      it('returns true when Slack email input is present', () => {
        document.body.innerHTML = `
          <form>
            <input type="email" name="email" />
            <button type="submit">Sign In</button>
          </form>
        `;
        expect(detectLoginWall()).toBe(true);
      });

      it('returns true when sign_in_button data-qa is present', () => {
        document.body.innerHTML = `
          <button data-qa="sign_in_button">Sign In</button>
        `;
        expect(detectLoginWall()).toBe(true);
      });

      it('returns true when signin class fragment is present', () => {
        document.body.innerHTML = `
          <div class="signin-abc123">Please sign in</div>
        `;
        expect(detectLoginWall()).toBe(true);
      });

      it('returns true when login class fragment is present', () => {
        document.body.innerHTML = `
          <div class="loginOverlay-xyz">Redirecting...</div>
        `;
        expect(detectLoginWall()).toBe(true);
      });

      it('returns false on an empty document body', () => {
        expect(detectLoginWall()).toBe(false);
      });

      it('returns true even if stray contenteditable exists alongside login UI', () => {
        document.body.innerHTML = `
          <div contenteditable="true"></div>
          <input type="email" name="email" />
        `;
        expect(detectLoginWall()).toBe(true);
      });
    });
    ```

    创建 `tests/unit/adapters/slack-match.spec.ts`，参考 `discord-match.spec.ts` 结构：

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { findAdapter, detectPlatformId } from '@/shared/adapters/registry';

    describe('adapters/slack — match (SLK-01)', () => {
      const validUrls = [
        'https://app.slack.com/client/workspace123/channel456',
        'https://app.slack.com/client/w/c?foo=bar',
      ];

      for (const url of validUrls) {
        it(`matches: ${url}`, () => {
          const adapter = findAdapter(url);
          expect(adapter).not.toBeUndefined();
          expect(adapter!.id).toBe('slack');
        });
      }

      it('detectPlatformId returns slack for valid URL', () => {
        expect(detectPlatformId('https://app.slack.com/client/w/c')).toBe('slack');
      });

      const invalidUrls = [
        { url: 'https://app.slack.com/client/', label: 'missing workspace + channel' },
        { url: 'https://app.slack.com/client/w/', label: 'missing channel' },
        { url: 'https://slack.com/check-login', label: 'wrong host (slack.com vs app.slack.com)' },
        { url: 'https://discord.com/channels/1/2', label: 'discord URL' },
        { url: 'not-a-url', label: 'malformed' },
        { url: '', label: 'empty' },
      ];

      for (const { url, label } of invalidUrls) {
        it(`does NOT match: ${label}`, () => {
          const adapter = findAdapter(url);
          const isSlack = adapter?.id === 'slack';
          expect(isSlack).toBe(false);
        });
      }
    });
    ```

    注意：slack-match.spec.ts 在 Plan 03 添加 registry entry 后才能 GREEN。Plan 02 先创建文件。
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/adapters/slack-login-detect.spec.ts 2>&1 | tail -3; echo "EXIT: $?"</automated>
  </verify>
  <done>两个测试文件存在。slack-login-detect 测试因 import 失败而 RED。slack-match 测试因 registry 中无 slack entry 而 fail（预期行为，Plan 03 完成后 GREEN）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement slack-login-detect.ts</name>
  <files>shared/adapters/slack-login-detect.ts</files>
  <read_first>
    shared/adapters/discord-login-detect.ts
    tests/unit/adapters/slack-login-detect.spec.ts
    .planning/phases/10-slack-adapter/10-RESEARCH.md (Pattern 4, Anti-Pitfall 1)
  </read_first>
  <acceptance_criteria>
    - File `shared/adapters/slack-login-detect.ts` exists
    - Exports `detectLoginWall` function
    - NO `chrome.*` imports
    - Uses 4 DOM selectors: `input[type="email"][name="email"]`, `button[data-qa="sign_in_button"]`, `[class*="signin"]`, `[class*="login"]`
    - `pnpm test:unit -- tests/unit/adapters/slack-login-detect.spec.ts` exits 0 (GREEN)
  </acceptance_criteria>
  <action>
    创建 `shared/adapters/slack-login-detect.ts`，参考 `discord-login-detect.ts` 结构：

    ```typescript
    /**
     * Slack login wall DOM detection (D-138).
     *
     * Lives in `shared/adapters/` so tests (jsdom) and the slack
     * content-script bundle can import the same implementation. No
     * `chrome.*` APIs — pure DOM lookups.
     *
     * CRITICAL for Slack: Slack redirects logged-out users from
     * app.slack.com to slack.com/check-login — the host changes,
     * so URL-layer loggedOutPathPatterns cannot catch it (RESEARCH
     * Pitfall 1). DOM-level detection is the primary defense.
     *
     * Detection markers:
     *   1. input[type="email"][name="email"] — Slack signin email field
     *   2. button[data-qa="sign_in_button"] — Slack signin button
     *   3. [class*="signin"] — signin container class fragment
     *   4. [class*="login"] — login overlay class fragment
     */
    export function detectLoginWall(): boolean {
      return Boolean(
        document.querySelector('input[type="email"][name="email"]') ||
        document.querySelector('button[data-qa="sign_in_button"]') ||
        document.querySelector('[class*="signin"]') ||
        document.querySelector('[class*="login"]'),
      );
    }
    ```

    实现后运行测试确认 GREEN。
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/adapters/slack-login-detect.spec.ts --reporter=verbose</automated>
  </verify>
  <done>slack-login-detect 测试全部 GREEN；detectLoginWall 检测 4 种 Slack 登录标记，正常编辑器不误报</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM 环境 -> detectLoginWall | 运行在 Slack 页面的 ISOLATED world，读取 live DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service | detectLoginWall | accept | 纯 DOM querySelector 调用，无副作用；误报仅导致 NOT_LOGGED_IN 替代 INPUT_NOT_FOUND，用户体验略差但安全 |
</threat_model>

<verification>
```bash
pnpm test:unit -- tests/unit/adapters/slack-login-detect.spec.ts --reporter=verbose
```
All login detect tests pass. slack-match.spec.ts will pass after Plan 03 adds registry entry.
</verification>

<success_criteria>
- `detectLoginWall` 在 4 种登录标记场景返回 true，正常编辑器返回 false
- slack-login-detect 测试全部 GREEN
- slack-match 测试文件就绪（等待 Plan 03 registry entry 后 GREEN）
</success_criteria>

<output>
After completion, create `.planning/phases/10-slack-adapter/10-02-SUMMARY.md`
</output>
