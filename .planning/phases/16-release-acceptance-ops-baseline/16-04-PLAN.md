---
phase: 16-release-acceptance-ops-baseline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTENANCE.md
  - CHANGELOG.md
autonomous: true
requirements:
  - OPS-01
  - TRUST-03
must_haves:
  truths:
    - "A maintainer reads MAINTENANCE.md and can locate the source-first → artifact-second → page-last update path for each of: platform list, privacy claims, permission claims, screenshots/mockups, CTA text"
    - "MAINTENANCE.md includes a verification command cheatsheet so a maintainer knows which pnpm command gates each claim category"
    - "CHANGELOG.md has a ## [v1.2] section with Features, Documentation, and Known Issues subsections"
    - "The v1.2 Known Issues honestly record Telegram live UAT, Phase 11/12 Nyquist partial, and Feishu/Lark dropped — no overclaim wording"
    - "The v1.2 section is placed BEFORE the existing ## [v1.1] section (newest-first ordering, matching git-cliff convention)"
  artifacts:
    - path: "MAINTENANCE.md"
      provides: "Root-level maintainer doc formalizing 13-CONTENT-SOURCES Maintenance Rules"
      contains: "维护指南"
      min_lines: 60
    - path: "CHANGELOG.md"
      provides: "## [v1.2] section with Features + Documentation + Known Issues"
      contains: "## [v1.2]"
  key_links:
    - from: "MAINTENANCE.md verification cheatsheet"
      to: "package.json scripts (verify:manifest, verify:claims, site:verify, verify:readme)"
      via: "command table"
      pattern: "pnpm verify:claims"
    - from: "CHANGELOG.md ## [v1.2] Known Issues"
      to: "PROJECT.md deferred items + REQUIREMENTS.md TRUST-03"
      via: "honest scope boundary wording (Telegram UAT / Nyquist partial / Feishu-Lark dropped)"
      pattern: "Telegram|Nyquist|Feishu"
---

<objective>
Produce the two human-readable closeout artifacts that the release ships with: (1) a root-level `MAINTENANCE.md` that converts the 13-CONTENT-SOURCES Maintenance Rules into actionable source-first → artifact-second → page-last update paths, and (2) a `## [v1.2]` section in CHANGELOG.md with honest Known Issues that preserve the Telegram live UAT / Nyquist partial / Feishu-Lark-dropped scope boundaries.

Purpose: close SC4 (maintenance rules recorded with clear update paths) and SC5 (release notes maintain honest scope boundaries). The deferred milestone risks are documented — not silently dropped — per TRUST-03.

Output: 1 new markdown file (MAINTENANCE.md) + 1 edited markdown file (CHANGELOG.md). Zero code changes.
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/seven/data/coding/projects/seven/web2chat/.planning/PROJECT.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/ROADMAP.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/STATE.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/phases/16-release-acceptance-ops-baseline/16-RESEARCH.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/phases/16-release-acceptance-ops-baseline/16-PATTERNS.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/phases/13-information-architecture-copy-sources/13-CONTENT-SOURCES.md

<interfaces>
<!-- Existing CHANGELOG.md head (the file being edited for v1.2 section) -->
```markdown
# Changelog

All notable changes to this project are documented in this file.

## [v1.1] - 2026-05-31


### Features

- Add Slack and Telegram adapters, expanding the shipped platform set to OpenClaw, Discord, Slack, and Telegram.

### Architecture
...
### Improvements
...
### Bug Fixes
...
### Removed
...
```
The structure convention is: `## [vX.Y] - YYYY-MM-DD` h2, followed by `### Features` / `### Architecture` / `### Improvements` / `### Bug Fixes` / `### Removed` / `### Known Issues` subsections. git-cliff owns this format.

<!-- 13-CONTENT-SOURCES.md Maintenance Rules (the source-of-truth for MAINTENANCE.md body) -->
The Maintenance Rules section (lines 169-208 of 13-CONTENT-SOURCES.md) already defines source-first → artifact-second → page-last chains for: platform list, privacy claims, permission claims, screenshots/mockups, CTA text. MAINTENANCE.md formalizes (not invents) these rules — copy the chains, do not re-derive.

<!-- Project language convention -->
CLAUDE.md §用户约束 #1: use zh-CN by default. README.md is zh_CN-primary. MAINTENANCE.md headings should be bilingual (zh-CN primary) to match the README style.
</interfaces>

<locked_decisions>
- D1: MAINTENANCE.md placement = REPO ROOT (alongside README.md, PRIVACY.md, CHANGELOG.md). Confirmed during pattern mapping — project has no `docs/` dir; all root markdown docs live at repo root. Resolves RESEARCH Assumption A3.
- D2: MAINTENANCE.md content = FORMALIZED copy of 13-CONTENT-SOURCES.md §Maintenance Rules (lines 169-208). Do NOT invent new rules. Each claim category gets: source-first → artifact-second → page-last → verify-command chain. The RESEARCH explicitly forbids re-deriving.
- D3: MAINTENANCE.md language = zh-CN primary with bilingual headings (matches README.md style). The verification command table at the bottom uses the literal pnpm script names (verify:manifest, verify:claims, site:build, site:verify, verify:readme).
- D4: CHANGELOG.md = insert a new `## [v1.2] - <release-date>` section immediately AFTER the `# Changelog` header + intro line and BEFORE the existing `## [v1.1]` line. Do NOT create a separate RELEASE-NOTES.md (git-cliff + verify-changelog-release.ts own CHANGELOG.md — splitting the source of truth would create drift, per RESEARCH Anti-Pattern).
- D5: Known Issues wording must match the honest boundaries in PROJECT.md lines 47, 56, 60 and STATE.md Deferred Items. The three required items: (a) Telegram live UAT not recorded as a headed-browser artifact (carried from v1.1), (b) Phase 11/12 Nyquist closeout recorded as known risk only — not a v1.2 deliverable, (c) Feishu/Lark remains out of shipped scope (evaluated + dropped after UAT confirmed shared-URL targeting breaks dispatch). Do NOT soften or omit any of the three.
</locked_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root MAINTENANCE.md formalizing 13-CONTENT-SOURCES Maintenance Rules</name>
  <files>MAINTENANCE.md</files>
  <read_first>
    - .planning/phases/13-information-architecture-copy-sources/13-CONTENT-SOURCES.md ## Maintenance Rules section (lines 169-208 — the source-of-truth content to formalize; do NOT invent new rules)
    - README.md (head ~30 lines — confirm the structural template: zh-CN primary heading style, bilingual section headings, the `# web2chat` h1 + `> tagline` blockquote pattern)
    - PRIVACY.md (confirm it exists at repo root — MAINTENANCE.md lives alongside it)
    - .planning/phases/16-release-acceptance-ops-baseline/16-PATTERNS.md ## MAINTENANCE.md section lines 166-223 (the full structural template + the 5 claim-category chains + the verification command cheatsheet table)
    - .planning/phases/16-release-acceptance-ops-baseline/16-RESEARCH.md ## Open Question 3 lines 495-497 (confirm the recommendation: reference only `pnpm assets:screenshot`, not the internal script path)
    - package.json lines 23-37 (confirm the exact pnpm script names to reference in the cheatsheet: verify:manifest, verify:claims, site:build, site:verify, verify:readme, assets:screenshot)
  </read_first>
  <action>
    Create `MAINTENANCE.md` at repo root. Structure (copy the heading style from README.md — zh-CN primary h1, bilingual section headings):

    - H1: `# web2chat 维护指南 (Maintenance)`
    - Blockquote tagline: explain this formalizes the Maintenance Rules from `13-CONTENT-SOURCES.md` (cite lines 169-208) so maintainers hit the canonical source before touching the marketing page.

    Then 5 claim-category sections, each as an H2 with bilingual heading. Each section lists a numbered 4-step chain: (1) Source first — the authoritative file; (2) Artifact second — the planning/intermediate artifact; (3) Page last — the marketing app file; (4) Verify — the pnpm command that gates it. Source the EXACT file paths from 13-CONTENT-SOURCES.md §Maintenance Rules lines 171-208:
    - `## 平台列表 (Platform list)` — source: `.planning/PROJECT.md` Current shipped platform set + Key Decisions; artifact: `13-CONTENT-SOURCES.md` Platform Status + CLM-PLATFORM-01; page: `apps/marketing/src/data/site-content.ts` supportedPlatforms + Known limits section; verify: `pnpm verify:claims`
    - `## 隐私声明 (Privacy claims)` — source: `PRIVACY.md` / `PRIVACY.zh_CN.md`; artifact: `13-CONTENT-SOURCES.md` CLM-PRIVACY-01; page: `apps/marketing/src/i18n/locales/{en,zh_CN}.json` trust.privacy.*; verify: `pnpm verify:claims`
    - `## 权限声明 (Permission claims)` — source: `wxt.config.ts` production manifest branch; artifact: `13-CONTENT-SOURCES.md` CLM-PERM-01; page: locale trust.permissions.fact1; verify: `pnpm verify:manifest` + `pnpm verify:claims`
    - `## 截图 / mockup / diagram / placeholder` — source: `pnpm assets:screenshot`; artifact: `13-CONTENT-SOURCES.md` Asset Status Rules; page: `apps/marketing/src/components/proof/*`; verify: `pnpm site:verify` (proof-label marker)
    - `## CTA 文案 (CTA text)` — source: `apps/marketing/src/data/site-content.ts` REPO_URL/INSTALL_URL; artifact: `13-CONTENT-SOURCES.md` Page Outline CTA section; page: `apps/marketing/src/app.tsx` Hero + bottom CTA `<CtaButton>` usage; verify: `pnpm verify:readme`

    Then a final H2 `## 验证命令清单 (Verification command cheatsheet)` with a markdown table listing each verify command + what it checks. Reference the exact pnpm script names from package.json (verify:manifest, site:build + site:verify, verify:readme, verify:claims, assets:screenshot) + a "Full local CI mirror" row with the chained command `pnpm typecheck && pnpm lint && pnpm test && pnpm verify:manifest && pnpm site:build && pnpm site:verify && pnpm verify:readme && pnpm verify:claims`.

    Do NOT invent new rules — every chain comes from 13-CONTENT-SOURCES.md §Maintenance Rules. Do NOT duplicate the Claims Matrix forbidden-wording lists (those live in 13-CONTENT-SOURCES.md + verify:claims.ts; MAINTENANCE.md just points to them). Keep the doc concise (~60-90 lines).
  </action>
  <verify>
    <automated>test -f MAINTENANCE.md && grep -qE "维护指南|平台列表|隐私声明|权限声明|验证命令清单" MAINTENANCE.md && echo "OK"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f MAINTENANCE.md` exit 0 (file exists at repo root)
    - Source: `grep -cE "^## (平台列表|隐私声明|权限声明|截图|CTA 文案|验证命令清单)" MAINTENANCE.md` returns >= 6 (5 claim categories + cheatsheet)
    - Source: `grep -c "13-CONTENT-SOURCES" MAINTENANCE.md` returns >= 1 (cites the source artifact)
    - Source: `grep -c "pnpm verify:claims" MAINTENANCE.md` returns >= 2 (referenced in >= 2 claim categories + cheatsheet)
    - Source: `grep -cE "Telegram|Feishu|Lark|飞书" MAINTENANCE.md` returns 0 (MAINTENANCE.md is about update paths, NOT release-notes — those go in CHANGELOG)
    - Source: the doc does NOT re-derive rules — each chain's 4 steps can be traced back to 13-CONTENT-SOURCES.md lines 171-208
  </acceptance_criteria>
  <done>
    MAINTENANCE.md exists at repo root with 5 claim-category sections (each with source-first → artifact-second → page-last → verify chain) + a verification command cheatsheet. All chains trace to 13-CONTENT-SOURCES.md. Commit: `docs(16-04): add root MAINTENANCE.md formalizing marketing update paths (OPS-01)`
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert ## [v1.2] section into CHANGELOG.md with honest Known Issues</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-40 (the existing # Changelog header + intro + the full ## [v1.1] section — copy the heading hierarchy and subsection conventions verbatim)
    - .planning/phases/16-release-acceptance-ops-baseline/16-PATTERNS.md ## CHANGELOG.md section lines 455-499 (the exact target section body + structure rules)
    - .planning/phases/16-release-acceptance-ops-baseline/16-RESEARCH.md ## Anti-Patterns lines 304-310 (forbids creating RELEASE-NOTES.md — must inject into CHANGELOG.md)
    - .planning/PROJECT.md lines 47, 56, 60 (the three honest scope boundaries: Telegram live UAT, Nyquist partial, Feishu/Lark dropped — copy the wording, do not soften)
    - .planning/STATE.md ## Deferred Items lines 80-83 (telegram-live-uat, feishu-lark-final-scope-sync, phase-11-12-nyquist-closeout — the three required Known Issues)
    - .planning/REQUIREMENTS.md TRUST-03 definition (the requirement this section closes: "Visitor can distinguish shipped capabilities from known risks and deferred platforms")
    - cliff.toml (confirm git-cliff owns the CHANGELOG structure — do NOT break the convention)
    - scripts/verify-changelog-release.ts (confirm the verifier checks CHANGELOG.md as canonical — splitting would break it)
  </read_first>
  <action>
    Edit `CHANGELOG.md`. Insert a NEW `## [v1.2] - 2026-06-14` section IMMEDIATELY AFTER the intro line `All notable changes to this project are documented in this file.` and BEFORE the existing `## [v1.1] - 2026-05-31` line. The new section uses the same subsection conventions as the existing v1.1 section (## [vX.Y] h2, then ### Features / ### Documentation / ### Known Issues subsections). Do NOT modify the existing # Changelog header, the intro line, or any of the existing v1.1/v1.0.x sections.

    The v1.2 section contains:
    - `### Features` (h3): bullet 1 — ship the bilingual marketing page (Phase 13-15) covering OpenClaw/Discord/Slack/Telegram, structured-payload preview, privacy + permission trust section, known limits. Bullet 2 — add `verify:claims` cross-source consistency verifier + wire marketing/claims gates into CI (Phase 16; site:build/site:verify/verify:readme/verify:claims). Bullet 3 — close WCAG G201 external-link indication on CtaButton + add document lang attribute contract.
    - `### Documentation` (h3): bullet — add root-level `MAINTENANCE.md` formalizing the source-first → artifact-second → page-last update paths for marketing claims.
    - `### Known Issues` (h3): exactly 3 bullets, wording copied from PROJECT.md + STATE.md, NOT softened:
      1. Telegram automation coverage passes, but a real logged-in headed-browser UAT artifact has not yet been recorded (carried forward from v1.1).
      2. Phase 11/12 Nyquist closeout is recorded as a known risk only — not a marketing claim and not a v1.2 deliverable.
      3. Feishu/Lark remains out of shipped scope (evaluated and dropped after UAT confirmed shared-URL targeting breaks dispatch).

    Do NOT add `### Bug Fixes`, `### Architecture`, `### Improvements`, or `### Removed` subsections unless there is real content for them (Phase 16 has none — keep the section lean). Use the date `2026-06-14` to match the phase research date; the executor can adjust to the actual release date if different.
  </action>
  <verify>
    <automated>grep -cE "^## \[v1\.2\]" CHANGELOG.md | grep -q "^1$" && awk '/^## \[v1\.2\]/{f=1} /^## \[v1\.1\]/{if(f){print "v1.2-before-v1.1: OK"; exit}}' CHANGELOG.md; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -cE "^## \[v1\.2\]" CHANGELOG.md` returns 1
    - Source: `grep -cE "^### Known Issues" CHANGELOG.md` increases by 1 (new subsection in v1.2)
    - Source: the v1.2 section appears BEFORE the v1.1 section (newest-first ordering — verify by reading the file: ## [v1.2] line number < ## [v1.1] line number)
    - Source: `grep -cE "Telegram|Nyquist|Feishu|Lark" CHANGELOG.md` reflects the 3 Known Issues items (each of the 3 concepts appears at least once in the v1.2 Known Issues bullets)
    - Source: `test -f RELEASE-NOTES.md` returns non-zero / file absent (D4 — did NOT create a separate file)
    - Behavior: `pnpm verify:changelog-release` exit 0 (the verifier still accepts CHANGELOG.md structure)
    - Behavior: the existing v1.1 / v1.0.x sections are unchanged — `git diff CHANGELOG.md` shows only INSERTIONS (the new v1.2 block), no modifications to existing lines
  </acceptance_criteria>
  <done>
    CHANGELOG.md has a ## [v1.2] section with Features + Documentation + Known Issues (3 honest boundary items). The section appears before v1.1. No separate RELEASE-NOTES.md created. verify:changelog-release passes. Commit: `docs(16-04): add CHANGELOG v1.2 section with honest Known Issues (TRUST-03)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MAINTENANCE.md → maintainer action | A maintainer follows an update path in this doc; a wrong/stale path sends them to the wrong source file |
| CHANGELOG.md → release audience | The Known Issues wording shapes visitor trust; overclaim wording (e.g., "fully validated on Telegram") would violate TRUST-03 |
| 13-CONTENT-SOURCES.md → MAINTENANCE.md | The rules are hand-copied; a drift (new rule added to 13-CONTENT-SOURCES but not propagated to MAINTENANCE.md) would create a stale maintainer doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Spoofing (overclaim release scope) | CHANGELOG.md ## [v1.2] Known Issues | mitigate | D5: copy the 3 honest boundary items verbatim from PROJECT.md + STATE.md; acceptance criterion asserts each of Telegram/Nyquist/Feishu-Lark appears; reviewer cross-checks wording against 13-CONTENT-SOURCES CLM-LIMIT-01/02 (manual-only verification per 16-VALIDATION.md) |
| T-16-13 | Tampering (stale maintainer doc) | MAINTENANCE.md chains vs 13-CONTENT-SOURCES.md | mitigate | D2: formalize (not invent) — acceptance criterion asserts each chain traces to 13-CONTENT-SOURCES.md lines 171-208; cite the source lines in the doc tagline so a future drift is discoverable |
| T-16-14 | Repudiation (split release notes source) | CHANGELOG.md vs hypothetical RELEASE-NOTES.md | mitigate | D4: inject into CHANGELOG.md only; acceptance criterion asserts RELEASE-NOTES.md absent; git-cliff + verify-changelog-release.ts remain canonical |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Phase 16 installs zero packages — doc-only plan |
</threat_model>

<verification>
- MAINTENANCE.md exists at repo root with 5 claim-category sections + cheatsheet
- CHANGELOG.md has the v1.2 section with 3 honest Known Issues items
- `pnpm verify:changelog-release` exit 0 (structure still valid)
- `test ! -f RELEASE-NOTES.md` exit 0 (no separate file created)
- No code changes — `git diff --stat` shows only MAINTENANCE.md (new) + CHANGELOG.md (edited)
</verification>

<success_criteria>
- MAINTENANCE.md at repo root with 5 claim categories + verification cheatsheet; every chain traceable to 13-CONTENT-SOURCES.md (OPS-01 closed)
- CHANGELOG.md ## [v1.2] section with Features + Documentation + Known Issues; 3 honest boundary items match PROJECT.md + STATE.md wording (TRUST-03 closed)
- No RELEASE-NOTES.md created (CHANGELOG.md remains canonical)
- SC4 (maintenance rules recorded) + SC5 (honest release boundaries) closed by this plan
</success_criteria>

<output>
Create `.planning/phases/16-release-acceptance-ops-baseline/16-04-SUMMARY.md` when done
</output>
