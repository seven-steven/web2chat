---
phase: 16-release-acceptance-ops-baseline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/verify-claims.ts
  - tests/unit/scripts/verify-claims.spec.ts
  - package.json
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
  - TRUST-03
  - OPS-02
  - PROOF-03
must_haves:
  truths:
    - "Maintainer runs `pnpm verify:claims` and it exits 0 when marketing copy matches canonical sources"
    - "verify:claims fails (exit 1) with a precise error message when a forbidden privacy token leaks into trust.privacy.* locale copy"
    - "verify:claims fails when locale permission text omits a permission token present in the built manifest"
    - "verify:claims fails when locale copy claims production 'tabs' permission that the prod manifest does not include"
    - "verify:claims fails when a platform name is missing from supportedPlatforms.* or when Feishu/Lark appears outside the limits key"
    - "verify:claims fails when en.json and zh_CN.json key sets differ (locale parity guard)"
    - "verify:claims fails when a proof metadata key (proof.label/source/status/version) is missing from either locale (PROOF-03)"
    - "verify:claims assertion logic is independently unit-tested with synthetic inputs (no real filesystem mutation)"
  artifacts:
    - path: "scripts/verify-claims.ts"
      provides: "Cross-source consistency verifier — assertClaims pure fn + isDirectInvocation-guarded CLI"
      contains: "export function assertClaims"
      min_lines: 90
    - path: "tests/unit/scripts/verify-claims.spec.ts"
      provides: "TDD unit test for assertClaims() — pass + 7 failure cases"
      contains: "import { assertClaims"
      min_lines: 90
  key_links:
    - from: "scripts/verify-claims.ts CLI entry"
      to: ".output/chrome-mv3/manifest.json"
      via: "readFileSync (single source of truth for prod permission set)"
      pattern: "readFileSync.*manifest\\.json"
    - from: "tests/unit/scripts/verify-claims.spec.ts"
      to: "scripts/verify-claims.ts"
      via: "import assertClaims + synthetic ClaimsInputs overrides"
      pattern: "import.*assertClaims.*from.*verify-claims"
    - from: "package.json"
      to: "scripts/verify-claims.ts"
      via: "verify:claims script entry"
      pattern: "verify:claims.*tsx scripts/verify-claims"
---

<objective>
Build `scripts/verify-claims.ts` — the single cross-source consistency verifier that converts the 13-CONTENT-SOURCES Claims Matrix from a human-readable audit trail into a self-enforcing CI gate. This is the keystone artifact of Phase 16: every other plan (CI wiring, release notes, maintenance docs) assumes it exists.

Purpose: close SC2 (claims/privacy/permissions checklist) and SC3 (a11y/link smoke) by making drift impossible to merge. Also closes deferred WR-06/IN-04 (privacy overclaim) from Phase 15.

Output: `scripts/verify-claims.ts` (exported pure assertion fn + isDirectInvocation-guarded CLI), `tests/unit/scripts/verify-claims.spec.ts` (TDD unit test, RED-first), and `package.json` verify:claims script.
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/seven/data/coding/projects/seven/web2chat/.planning/PROJECT.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/ROADMAP.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/STATE.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/phases/16-release-acceptance-ops-baseline/16-RESEARCH.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/phases/16-release-acceptance-ops-baseline/16-PATTERNS.md
@/Users/seven/data/coding/projects/seven/web2chat/.planning/phases/13-information-architecture-copy-sources/13-CONTENT-SOURCES.md

<interfaces>
<!-- Existing verify-script shape (copy verbatim from verify-manifest.ts) -->
From scripts/verify-manifest.ts:20-22 (imports — Node built-ins only):
```typescript
import { readFileSync, existsSync } from 'node:fs';
import { resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
```

From scripts/verify-manifest.ts:164-194 (isDirectInvocation guard + CLI entry — copy verbatim, only change the script name in log prefixes and the readPaths):
```typescript
const isDirectInvocation =
  !!process.argv[1] && fileURLToPath(import.meta.url) === resolve(process.argv[1]);
if (isDirectInvocation) { /* read inputs, call assertX, branch on errors.length */ }
```

From scripts/verify-manifest.ts:47-55 (expectSet helper for set-equality):
```typescript
const expectSet = (label, actual, expected) => {
  const a = (actual ?? []).slice().sort();
  const e = expected.slice().sort();
  if (JSON.stringify(a) !== JSON.stringify(e)) { throw new Error(...); }
};
```

Existing package.json scripts block (insert verify:claims as a sibling):
```json
"verify:manifest": "wxt build && tsx scripts/verify-manifest.ts",
"verify:zip": "wxt build && wxt zip && tsx scripts/verify-zip.ts",
```

From tests/unit/scripts/verify-manifest.spec.ts:1-26 (test scaffolding + validX(overrides) factory pattern — copy verbatim shape):
```typescript
import { describe, it, expect } from 'vitest';
import { assertClaims, type ClaimsInputs } from '@/scripts/verify-claims';
function validInputs(overrides: Partial<ClaimsInputs> = {}): ClaimsInputs { /* valid baseline + overrides */ }
```

<!-- Authoritative proof metadata locale keys (verified against apps/marketing/src/i18n/locales/{en,zh_CN}.json lines 56-59) -->
The ACTUAL locale keys for PROOF-03 are: `proof.label`, `proof.source`, `proof.status`, `proof.version` (NOT `proof.mockup.*` — that namespace does not exist in either locale file).

en.json values (lines 56-59):
```
"proof.label": "mockup",
"proof.source": "code-generated",
"proof.status": "marketing demo aligned to current UI contract",
"proof.version": "current repo state",
```

zh_CN.json values (lines 56-59):
```
"proof.label": "mockup",
"proof.source": "code-generated",
"proof.status": "营销演示，与当前 UI 契约保持一致",
"proof.version": "current repo state",
```

Note `proof.label` === 'mockup' in BOTH locales — rule (e) optionally asserts this to lock the mockup-vs-screenshot status.
</interfaces>

<locked_decisions>
- D1: verify:claims = single-source-of-truth (reads BUILT `.output/chrome-mv3/manifest.json` — never parses `wxt.config.ts` source). Per RESEARCH Anti-Pattern + PATTERNS.md "Single-source-of-truth rule".
- D2: assertClaims pure fn exported for unit-test consumption; CLI guarded by `isDirectInvocation` (same pattern verify-manifest.ts had to fix in commit 0b23bb2).
- D3: Forbidden-wording lists sourced verbatim from 13-CONTENT-SOURCES.md Claims Matrix CLM-PRIVACY-01 + RESEARCH Pattern 1 (lines 182-196). Do NOT invent tokens. Per RESEARCH Caveat: do NOT blocklist the bare token 'remote server' (legitimate in "no remote server"); blocklist only the overclaim forms ('our servers', 'server-side processing').
- D4: `package.json` script chains `wxt build && tsx scripts/verify-claims.ts` (mirrors verify:manifest at package.json:27) so standalone `pnpm verify:claims` self-builds. Per RESEARCH Pitfall 5.
- D5: TDD mode ENABLED — RED test first (import from not-yet-created module → tests fail), then GREEN impl.
- D6: PROOF-03 metadata keys are `proof.label`, `proof.source`, `proof.status`, `proof.version` (verified against en.json + zh_CN.json lines 56-59 — see <interfaces>). Rule (e) MUST reference these exact keys, NOT `proof.mockup.*`. The `validInputs()` factory baseline MUST include all four proof.* keys in BOTH en and zh_CN so Test 1 "valid → no errors" stays a true positive under rule (e); Test 8 covers rule (e)'s failure mode by surgically removing one proof key.
</locked_decisions>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing verify-claims.spec.ts with 8 test cases</name>
  <files>tests/unit/scripts/verify-claims.spec.ts</files>
  <read_first>
    - tests/unit/scripts/verify-manifest.spec.ts (copy the `import { describe, it, expect } from 'vitest'` + `validX(overrides)` factory shape verbatim; lines 1-26 are the canonical scaffold)
    - .planning/phases/16-release-acceptance-ops-baseline/16-RESEARCH.md ## Code Examples lines 374-440 (full concrete test bodies for first 4 cases — lift verbatim)
    - .planning/phases/16-release-acceptance-ops-baseline/16-PATTERNS.md ## tests/unit/scripts/verify-claims.spec.ts section (lines 113-162; the 7-case test list pattern + factory)
    - apps/marketing/src/i18n/locales/en.json + zh_CN.json lines 56-59 (confirm the ACTUAL proof metadata keys exist: `proof.label`, `proof.source`, `proof.status`, `proof.version` — copy the real values into the `validInputs()` factory baseline for BOTH locales; also confirm exact trust.permissions.fact1 / trust.privacy.* keys)
    - wxt.config.ts (read once to confirm prod permissions list the test baseline hardcodes: activeTab/alarms/scripting/storage/webNavigation)
  </read_first>
  <behavior>
    - Test 1 "valid inputs produce no errors": `validInputs()` factory returns a fully-valid baseline → `assertClaims(valid, errors)` → `errors` is empty. The baseline MUST include all four proof metadata keys (`proof.label`, `proof.source`, `proof.status`, `proof.version`) in BOTH en and zh_CN (real values from en.json/zh_CN.json lines 56-59), so this test stays valid under rule (e).
    - Test 2 "locale text missing a permission token produces error": override en `trust.permissions.fact1` to omit 'alarms' and 'webNavigation' → errors contain a message matching /alarms/
    - Test 3 "forbidden privacy wording produces error": override en `trust.privacy.fact4` to 'Cloud sync enabled' AND zh_CN to '支持云端存储' → errors contain at least one message matching /cloud sync|云端存储/
    - Test 4 "locale claiming production tabs permission produces error": override `trust.permissions.fact1` in both locales to append ', tabs' → errors contain a message matching /tabs/
    - Test 5 "locale key parity violation produces error": zh_CN missing one key that en has → errors contain a message matching /parity|key.*differ|missing.*key/
    - Test 6 "platform section missing a shipped platform name produces error": remove all `supportedPlatforms.discord.*` keys → errors contain a message matching /platform|discord|OpenClaw/
    - Test 7 "Feishu/Lark leaking outside limits copy produces error": inject '飞书' into a `supportedPlatforms.*.title` key → errors contain a message matching /Feishu|飞书|Lark|leak/
    - Test 8 "missing proof metadata key produces error (PROOF-03, rule e)": override the zh_CN locale object to OMIT the `proof.status` key (start from validInputs() and produce a zh_CN clone with `proof.status` deleted, keeping all other keys) → `assertClaims` pushes at least one error matching `/proof|missing/i`. This is the RED-then-GREEN test for rule (e)'s failure mode.
  </behavior>
  <action>
    Create `tests/unit/scripts/verify-claims.spec.ts` importing `{ assertClaims, type ClaimsInputs }` from `@/scripts/verify-claims` (the module does not exist yet — this is the RED step). Define `validInputs(overrides: Partial<ClaimsInputs> = {})` factory returning the canonical valid baseline. The factory's locales.en and locales.zh_CN objects MUST each contain: `trust.permissions.fact1`, `trust.privacy.fact4`, the 4 `supportedPlatforms.*` keys, AND all four proof metadata keys with real values (`'proof.label': 'mockup'`, `'proof.source': 'code-generated'`, `'proof.status': 'marketing demo aligned to current UI contract'` for en / `'营销演示，与当前 UI 契约保持一致'` for zh_CN, `'proof.version': 'current repo state'`). Copy the proof.* values verbatim from en.json/zh_CN.json lines 56-59 (already in <interfaces>). The manifest baseline has the 5 prod permissions + 4 host_permissions. Write all 8 test cases listed in <behavior>. Each failure-case test applies exactly ONE surgical override via the factory (e.g. `validInputs({ locales: { en: { 'trust.privacy.fact4': 'Cloud sync enabled' } ... } })`) so the resulting errors are attributable. For Test 8, the override constructs a zh_CN object that omits `proof.status` while keeping all other proof.* keys (use object spread + explicit delete, or rebuild the object minus that key — the override must produce a zh_CN key set that differs from en by exactly one key, AND that key is a proof.* key). The factory's baseline MUST reflect real prod values (5 permissions, 4 host_permissions, 4 proof.* keys) so the "valid → no errors" test is a true positive under all five rules (a)-(e). Do NOT touch real locale files — the spec is hermetic. Do NOT create the scripts/verify-claims.ts module in this task (that's Task 2 GREEN).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/scripts/verify-claims.spec.ts 2>&1 | grep -qE "FAIL|Cannot find module|Error"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "import { assertClaims, type ClaimsInputs } from '@/scripts/verify-claims'" tests/unit/scripts/verify-claims.spec.ts` returns 1
    - Source: `grep -cE "^  it\(" tests/unit/scripts/verify-claims.spec.ts` returns >= 8
    - Source: `grep -c "function validInputs" tests/unit/scripts/verify-claims.spec.ts` returns 1
    - Source: `grep -c "'proof.label': 'mockup'" tests/unit/scripts/verify-claims.spec.ts` returns >= 1 (proof metadata key in baseline — en)
    - Source: `grep -cE "'proof\.(label|source|status|version)'" tests/unit/scripts/verify-claims.spec.ts` returns >= 8 (each of the 4 keys appears at least once in BOTH en and zh_CN baselines, or in the override — confirms baseline is complete enough for Test 1 to pass under rule (e))
    - Behavior: `pnpm test -- tests/unit/scripts/verify-claims.spec.ts` FAILS (RED) with module-not-found or assertion errors — exit code != 0
    - No real locale/manifest files are read by the spec (hermetic — all inputs via validInputs factory)
  </acceptance_criteria>
  <done>
    8 test cases exist in tests/unit/scripts/verify-claims.spec.ts (including Test 8 for rule (e) proof-missing failure mode); the validInputs() baseline includes all four proof.* metadata keys in both locales so Test 1 is a true positive under rule (e); running the suite fails RED because scripts/verify-claims.ts does not yet exist. Commit: `test(16-01): add failing verify-claims assertion tests incl. PROOF-03 rule-e (RED)`
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement scripts/verify-claims.ts + add verify:claims package.json script</name>
  <files>scripts/verify-claims.ts, package.json</files>
  <read_first>
    - scripts/verify-manifest.ts (full file; copy lines 20-22 imports verbatim, lines 47-55 expectSet helper shape, lines 164-194 isDirectInvocation guard + CLI entry verbatim, lines 178-183 error format verbatim)
    - scripts/verify-readme-anchors.ts (file-existence + accumulator pattern + clear error message — additional reference)
    - .planning/phases/16-release-acceptance-ops-baseline/16-RESEARCH.md ## Pattern 1 (lines 148-220; the canonical implementation including the assertClaims function body, the forbidden-wording list, and the CLI entry)
    - .planning/phases/16-release-acceptance-ops-baseline/16-PATTERNS.md ## scripts/verify-claims.ts section (lines 29-110) and ## Forbidden-wording list source-of-truth section (lines 542-551; the EXACT tokens to encode)
    - .planning/phases/13-information-architecture-copy-sources/13-CONTENT-SOURCES.md ## Claims Matrix rows CLM-PRIVACY-01 + CLM-PERM-01 + CLM-PLATFORM-01 + CLM-LIMIT-02 (forbidden + required wording)
    - apps/marketing/src/i18n/locales/en.json + zh_CN.json lines 56-59 (confirm the ACTUAL proof metadata keys are `proof.label`, `proof.source`, `proof.status`, `proof.version` — rule (e) references THESE keys, NOT `proof.mockup.*`; also confirm the 4 supportedPlatforms.* keys and the limits.* keys that should be the ONLY places Feishu/Lark may appear)
    - package.json lines 11-37 (existing scripts block — insert verify:claims as a sibling of verify:manifest at line 27)
  </read_first>
  <behavior>
    - `assertClaims(validInputs(), errors)` returns with errors empty (validInputs baseline now includes all four proof.* keys in both locales)
    - `assertClaims({ ...missing 'alarms' from fact1 }, errors)` pushes an error containing 'alarms'
    - `assertClaims({ ...'cloud sync'/'云端存储' in fact4 }, errors)` pushes a matching error
    - `assertClaims({ ...'tabs' in fact1 }, errors)` pushes an error containing 'tabs'
    - `assertClaims({ zh_CN missing one key }, errors)` pushes a parity error
    - `assertClaims({ missing supportedPlatforms.discord }, errors)` pushes a platform error
    - `assertClaims({ 飞书 in supportedPlatforms.*.title }, errors)` pushes a Feishu/Lark leak error
    - `assertClaims({ zh_CN missing proof.status (a proof.* key) }, errors)` pushes at least one proof error matching /proof|missing/ (rule (e) failure mode — Test 8 GREEN)
    - The CLI entry (when run as `tsx scripts/verify-claims.ts`) reads `.output/chrome-mv3/manifest.json` + both locale JSONs, calls assertClaims, prints `[verify-claims] FAIL:` + bullets on errors with exit 1, prints `[verify-claims] OK` on success with exit 0
    - Importing the module from a test (Task 1's spec) does NOT trigger the CLI path (isDirectInvocation guard)
  </behavior>
  <action>
    Create `scripts/verify-claims.ts` following the five-element verify-script contract in PATTERNS.md ## Verifier script shape (lines 522-538): (1) Node-builtin-only imports; (2) export `type ClaimsInputs` with `manifest: { permissions?: string[]; host_permissions?: string[] }` and `locales: { en: Record<string,string>; zh_CN: Record<string,string> }`; (3) export `assertClaims(input: ClaimsInputs, errors: string[]): void` — NO I/O, NO process.exit, NO console inside this function; (4) isDirectInvocation guard (copy verbatim from verify-manifest.ts:164-165); (5) CLI entry that reads the built manifest at `.output/chrome-mv3/manifest.json` and the two locale JSON files, calls assertClaims, and on errors prints `[verify-claims] FAIL:` + `  - <msg>` bullets then `process.exit(1)`, else prints `[verify-claims] OK — marketing claims match canonical sources`.

    Inside assertClaims, encode these assertions (PATTERNS.md lines 56-62):
    (a) Permission set check: for each locale key in ['en','zh_CN'], read text = `input.locales[localeKey]['trust.permissions.fact1'] ?? ''`. For each perm in `input.manifest.permissions ?? []` sorted, if `!text.includes(perm)` push `[<locale>] trust.permissions.fact1 missing token: <perm>`. After the includes loop, strip the word `webNavigation` then test `/\btabs\b/i` on the text; if it matches push `[<locale>] trust.permissions.fact1 must not claim 'tabs' as production`.
    (b) Privacy forbidden-wording scan: define PRIVACY_FORBIDDEN = ['cloud sync', 'our servers', 'server-side processing', 'usage analytics', 'user tracking', '云端存储', '用户行为分析'] (verbatim from RESEARCH Pattern 1 line 184-186; per D3 do NOT include bare 'remote server'). For each locale, for each factKey starting with `trust.privacy.`, for each forbidden token, if `text.toLowerCase().includes(forbidden.toLowerCase())` push `[<locale>] <factKey> contains forbidden token: <forbidden>`.
    (c) Locale key parity: compute enKeys = Object.keys(input.locales.en).sort() and zhKeys = Object.keys(input.locales.zh_CN).sort(); if JSON.stringify(enKeys) !== JSON.stringify(zhKeys) push `[parity] en and zh_CN locale keys differ — missing in zh_CN: [...], missing in en: [...]`.
    (d) Platform truth: for each platformName in ['OpenClaw', 'Discord', 'Slack', 'Telegram'] (hardcoded constant — matches verify-build.mjs:50-53 REQUIRED_PAGE_MARKERS precedent), if NEITHER locale has any `supportedPlatforms.*` key whose value contains platformName, push `[platform] shipped platform <name> missing from supportedPlatforms.* copy`. For each locale + each supportedPlatforms.* key, if the value matches /Feishu|Lark|飞书|lark/i push `[platform] Feishu/Lark leaking outside limits copy: [<locale>] <key>`.
    (e) Proof metadata presence (PROOF-03): define PROOF_REQUIRED_KEYS = ['proof.label', 'proof.source', 'proof.status', 'proof.version'] (the ACTUAL locale keys — verified in en.json + zh_CN.json lines 56-59; NOT `proof.mockup.*`). For each localeKey in ['en','zh_CN'], for each requiredKey in PROOF_REQUIRED_KEYS, if `input.locales[localeKey][requiredKey] === undefined` push `[proof] <locale> missing proof metadata key: <requiredKey>`. Additionally, if `input.locales[localeKey]['proof.label'] !== undefined && input.locales[localeKey]['proof.label'] !== 'mockup'` push `[proof] <locale> proof.label must be 'mockup' (got <value>) — locks the mockup-vs-screenshot status per 13-CONTENT-SOURCES PROOF-03`.

    Do NOT inline a fenced code block of the full implementation in <action> — name the identifiers, the rule shape, and the source-of-truth for each forbidden token list. The executor will compose the file from verify-manifest.ts scaffold + the assertion rules above.

    Then edit `package.json`: insert `"verify:claims": "wxt build && tsx scripts/verify-claims.ts",` immediately AFTER the existing `"verify:manifest"` line (line 27) so the script ordering in the file reads verify:manifest → verify:claims → verify:zip. The `wxt build &&` prefix is load-bearing (RESEARCH Pitfall 5): standalone `pnpm verify:claims` from a clean checkout self-builds the manifest before running the verifier.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/scripts/verify-claims.spec.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "export function assertClaims" scripts/verify-claims.ts` returns 1
    - Source: `grep -c "export type ClaimsInputs" scripts/verify-claims.ts` returns 1
    - Source: `grep -cE "cloud sync|our servers|server-side processing|usage analytics|user tracking|云端存储|用户行为分析" scripts/verify-claims.ts` returns >= 7 (forbidden tokens verbatim)
    - Source: `grep -c "isDirectInvocation" scripts/verify-claims.ts` returns >= 2 (declaration + usage)
    - Source: `grep -c ".output/chrome-mv3/manifest.json" scripts/verify-claims.ts` returns 1
    - Source: `grep -cE "'proof\.(label|source|status|version)'" scripts/verify-claims.ts` returns >= 4 (rule (e) references all 4 ACTUAL keys — NOT `proof.mockup.*`)
    - Source: `grep -c "proof.mockup" scripts/verify-claims.ts` returns 0 (WARNING 1 fix — no nonexistent keys referenced)
    - Source: `grep -c '"verify:claims": "wxt build && tsx scripts/verify-claims.ts"' package.json` returns 1
    - Behavior: `pnpm test -- tests/unit/scripts/verify-claims.spec.ts` passes (GREEN) — exit 0, all 8 cases pass (including Test 8 rule-e proof-missing failure case)
    - Behavior: importing the module from the spec does NOT execute the CLI path (no manifest-not-found error in test output)
  </acceptance_criteria>
  <done>
    scripts/verify-claims.ts exports `assertClaims` + `ClaimsInputs`, follows the 5-element verify-script contract, encodes all forbidden tokens verbatim from 13-CONTENT-SOURCES, rule (e) references the ACTUAL proof.* locale keys (proof.label/source/status/version), and the CLI entry is guarded by isDirectInvocation. package.json has the `verify:claims` script. All 8 unit tests GREEN (Test 1 valid baseline passes under rule (e); Test 8 locks rule (e)'s failure mode). Commit: `feat(16-01): implement verify:claims cross-source consistency verifier incl. PROOF-03 rule-e (GREEN)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale JSON (in-repo) → verify:claims | Marketing copy crosses into the assertion engine; a contributor can edit copy that drifts from canonical sources |
| built manifest (.output/) → verify:claims | The prod permission set crosses in; verify:claims treats it as the single source of truth |
| 13-CONTENT-SOURCES Claims Matrix → verify:claims source code | The forbidden-wording list is hand-transcribed from the matrix; a transcription error (or matrix update not propagated) would weaken the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing (false trust signal) | scripts/verify-claims.ts privacy forbidden-wording scan | mitigate | Encode forbidden tokens verbatim from 13-CONTENT-SOURCES + RESEARCH Pattern 1; unit test Test 3 locks the scan against regression (D3) |
| T-16-02 | Tampering | scripts/verify-claims.ts permission set check | mitigate | Read BUILT manifest (not source) per D1; unit test Test 2 + Test 4 lock the set-equality + 'tabs' rejection (Pitfall 2) |
| T-16-03 | Information disclosure (Feishu/Lark leakage) | scripts/verify-claims.ts platform truth assertion | mitigate | Hardcoded platform-name whitelist + Feishu/Lark leak scan in non-limits keys; unit test Test 7 locks the detection |
| T-16-04 | Tampering (locale parity drift) | scripts/verify-claims.ts key-parity assertion | mitigate | Symmetric difference check between en.json and zh_CN.json keys; unit test Test 5 locks the detection (Pitfall 3) |
| T-16-05 | Repudiation (silent verifier) | scripts/verify-claims.ts error emission | mitigate | follow verify-manifest.ts error format: `[verify-claims] FAIL:` header + bullet per error + exit 1 (never silent) |
| T-16-06 | Spoofing (proof metadata missing — false "production screenshot" signal) | scripts/verify-claims.ts rule (e) proof.* presence check | mitigate | Rule (e) asserts all four ACTUAL proof.* keys (proof.label/source/status/version) exist in both locales + locks proof.label === 'mockup'; unit test Test 8 locks the missing-key failure mode (D6) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Phase 16 installs zero packages (RESEARCH ## Standard Stack confirmed) — no slopcheck gate required |
</threat_model>

<verification>
- All 8 verify-claims.spec.ts cases pass (including Test 8 PROOF-03 rule-e failure case): `pnpm test -- tests/unit/scripts/verify-claims.spec.ts` exit 0
- Full test suite still green (no regression): `pnpm test` exit 0
- Typecheck clean: `pnpm typecheck` exit 0
- Lint clean: `pnpm lint` exit 0
- Module import isolation: importing scripts/verify-claims.ts from the spec does NOT trigger the CLI path (no `.output/manifest.json` read in test output)
</verification>

<success_criteria>
- scripts/verify-claims.ts exists, exports `assertClaims(input, errors)` + `ClaimsInputs` type, follows the 5-element verify-script contract
- tests/unit/scripts/verify-claims.spec.ts has >= 8 cases covering: valid baseline, permission missing-token, privacy forbidden-wording (en+zh_CN), production 'tabs' claim rejection, locale key parity, platform-name presence, Feishu/Lark leak, AND PROOF-03 proof-metadata-missing (rule e failure mode)
- The `validInputs()` factory baseline includes all four proof.* metadata keys (proof.label/source/status/version) in BOTH en and zh_CN so Test 1 stays valid under rule (e)
- package.json `verify:claims` script exists with the `wxt build && tsx scripts/verify-claims.ts` chain
- All forbidden tokens in scripts/verify-claims.ts match the 13-CONTENT-SOURCES Claims Matrix + RESEARCH Pattern 1 list verbatim (no paraphrase, no missing token, no bare 'remote server')
- Rule (e) references the ACTUAL locale keys `proof.label`, `proof.source`, `proof.status`, `proof.version` (NOT `proof.mockup.*` — that namespace does not exist in the locale files)
- TRUST-01, TRUST-02, TRUST-03, OPS-02, PROOF-03 are closed by this plan's artifacts (each requirement traces to at least one assertion in assertClaims, and PROOF-03 traces to rule (e) + Test 8)
</success_criteria>

<output>
Create `.planning/phases/16-release-acceptance-ops-baseline/16-01-SUMMARY.md` when done
</output>
