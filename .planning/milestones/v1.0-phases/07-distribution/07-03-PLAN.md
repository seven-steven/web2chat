---
phase: 07-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.en.md
autonomous: true
requirements: [DST-04]

must_haves:
  truths:
    - "README.md is a complete zh_CN user-facing document (not developer-period content)"
    - "README.en.md is a structurally equivalent English version"
    - "Both follow section order: intro -> install -> usage -> platform notes -> limitations -> dev -> privacy"
    - "Both contain Discord ToS risk notice in platform notes section"
    - "Both contain Limitations section with 5 items per D-91"
    - "Cross-link headers connect the two files"
  artifacts:
    - path: "README.md"
      provides: "zh_CN user documentation (GitHub default display)"
      contains: "# Web2Chat"
    - path: "README.en.md"
      provides: "English user documentation"
      contains: "# Web2Chat"
  key_links:
    - from: "README.md"
      to: "README.en.md"
      via: "cross-link header"
      pattern: "README.en.md"
    - from: "README.en.md"
      to: "README.md"
      via: "cross-link header"
      pattern: "README.md"
    - from: "README.md"
      to: "PRIVACY.zh_CN.md"
      via: "privacy section link"
      pattern: "PRIVACY.zh_CN.md"
    - from: "README.en.md"
      to: "PRIVACY.md"
      via: "privacy section link"
      pattern: "PRIVACY.md"
---

<objective>
Rewrite README.md as a zh_CN user-facing document and create README.en.md as a structurally equivalent English version, replacing all Phase-1 developer-period content per D-86.

Purpose: DST-04 requires bilingual README covering installation, usage, platform notes (OpenClaw + Discord ToS), limitations, development, and privacy. This is the primary user/contributor documentation.
Output: Two complete README files at repo root following D-83 through D-86 and D-90/D-91 decisions.
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-distribution/07-CONTEXT.md
@.planning/phases/07-distribution/07-RESEARCH.md
@.planning/phases/05-discord/05-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md (zh_CN)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content — will be completely replaced per D-86)
    - CLAUDE.md (project description, privacy constraints, adapter pattern)
    - .planning/phases/07-distribution/07-CONTEXT.md (D-83..D-86, D-90, D-91)
    - .planning/phases/05-discord/05-CONTEXT.md (D-59..D-61 ToS notice intent)
  </read_first>
  <action>
Completely rewrite `README.md` per D-86. Delete ALL existing content. New content is zh_CN per D-84.

Cross-link header (first line):
```
[English](./README.en.md) | 简体中文
```

Section structure per D-85 (user-priority order):

**`# Web2Chat`** + one-liner blockquote:
> 一键抓取网页结构化信息 + 预设 prompt，投递到 IM / AI Agent 聊天会话。Chrome MV3 扩展。

**`## 简介`**:
- 核心功能：点击工具栏图标 -> 抓取当前页面元数据(title/url/description/content) -> 格式化为 Markdown -> 搭配用户 prompt -> 投递到目标 IM 会话
- 支持平台：OpenClaw Web UI、Discord
- 隐私：所有数据仅本地存储，不上传任何服务器

**`## 安装`**:
- 子章节 `### 从源码加载 (Load Unpacked)`：
  1. `git clone` + `pnpm install`
  2. `pnpm build`
  3. Chrome -> `chrome://extensions` -> 开发者模式 -> Load unpacked -> 选择 `.output/chrome-mv3/`
- 子章节 `### Chrome Web Store` (预留)：
  - "即将上架，敬请期待。"

**`## 使用`**:
- 点击工具栏 Web2Chat 图标（或快捷键 `Ctrl+Shift+S` / `Cmd+Shift+S`）
- popup 自动抓取当前页面信息并显示预览
- 可编辑 title / description / content
- 输入 `send_to` 目标 URL（支持历史下拉 + prompt 绑定）
- 输入 prompt（可选）
- 点击确认投递

**`## 平台说明`**:
- 子章节 `### OpenClaw`：
  - URL 格式：`http(s)://<host>:<port>/chat?session=agent:<name>:<session>`
  - 要求 OpenClaw 服务在本地或可访问网络中运行
  - 首次使用时扩展会请求该 origin 的访问权限
  - 在设置页面管理已授权的 origin
- 子章节 `### Discord`：
  - URL 格式：`https://discord.com/channels/<server>/<channel>`
  - 要求已在同一浏览器 profile 登录 Discord
  - **ToS 风险声明** (per D-59/D-60): Web2Chat 通过 DOM 注入向 Discord 发送消息。这属于自动化操作，可能违反 [Discord 服务条款](https://discord.com/terms)。使用此功能即表示您理解并接受由此可能产生的账号风险（包括但不限于临时限制或封禁）。开发者不对因使用本扩展导致的 Discord 账号问题承担责任。

**`## Limitations`** (per D-90 精选 + D-91 推荐项):
- 仅支持 Chrome / Chromium 浏览器（Firefox / Safari 计划中）
- 仅支持 OpenClaw 和 Discord 两个平台（Telegram / Slack 等计划中）
- 长内容单条消息截断为 2000 字符（多条切分计划中）
- 无失败重试队列（计划中）
- Discord 投递使用 DOM 注入，存在 ToS 风险（详见上方平台说明）

**`## 开发`**:
- 前置条件：Node.js >= 20.19, pnpm 10.x, Chrome/Chromium
- 常用命令列表（简明）:
  ```bash
  pnpm install          # 安装依赖
  pnpm dev              # 开发模式 (HMR)
  pnpm build            # 生产构建
  pnpm test             # 单元测试
  pnpm test:e2e         # E2E 测试 (需 headed Chromium)
  pnpm typecheck        # 类型检查
  pnpm lint             # ESLint
  pnpm verify:manifest  # manifest 形态校验
  pnpm verify:zip       # zip 结构校验
  ```
- 技术栈一句话：WXT 0.20 + Preact + TypeScript + Tailwind v4 + Vitest + Playwright

**`## 隐私`**:
- 一句话概述 + 链接到 PRIVACY.zh_CN.md
- "详见 [隐私政策](./PRIVACY.zh_CN.md)"
  </action>
  <acceptance_criteria>
    - `README.md` first line contains `README.en.md` (cross-link)
    - `README.md` contains `# Web2Chat`
    - `README.md` contains `## 简介`
    - `README.md` contains `## 安装`
    - `README.md` contains `## 使用`
    - `README.md` contains `## 平台说明`
    - `README.md` contains `### OpenClaw`
    - `README.md` contains `### Discord`
    - `README.md` contains `Discord 服务条款` or `Discord Terms of Service` (ToS reference)
    - `README.md` contains `## Limitations`
    - `README.md` contains `Firefox` (in limitations)
    - `README.md` contains `2000` (truncation limit)
    - `README.md` contains `## 开发`
    - `README.md` contains `pnpm build`
    - `README.md` contains `## 隐私`
    - `README.md` contains `PRIVACY.zh_CN.md` (link to privacy policy)
    - `README.md` does NOT contain `Phase 1 手测脚本` (old content removed)
    - `README.md` does NOT contain `项目结构` as a heading (old content removed)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## 简介' README.md && grep -q '## Limitations' README.md && grep -q 'PRIVACY.zh_CN.md' README.md && ! grep -q 'Phase 1 手测' README.md && echo "PASS"</automated>
  </verify>
  <done>README.md completely rewritten as zh_CN user document with all 7 sections per D-85, Discord ToS notice per D-60, Limitations per D-91, no old developer-period content</done>
</task>

<task type="auto">
  <name>Task 2: Create README.en.md (English)</name>
  <files>README.en.md</files>
  <read_first>
    - README.md (the zh_CN version just created — maintain structural parity)
  </read_first>
  <action>
Create `README.en.md` at repo root. Per D-83 this is the English version, structurally equivalent to README.md per D-84.

Cross-link header (first line):
```
English | [简体中文](./README.md)
```

Translate all sections maintaining identical `## ` heading count (not literal heading text — use English equivalents):

- `# Web2Chat` + blockquote: "One-click clip and send structured web page info + custom prompt to IM / AI Agent chat sessions. Chrome MV3 extension."
- `## Introduction`
- `## Installation`
  - `### Load Unpacked (from source)`
  - `### Chrome Web Store` ("Coming soon.")
- `## Usage`
- `## Platform Notes`
  - `### OpenClaw`
  - `### Discord` (with ToS notice in English per D-60: "Web2Chat uses DOM injection to send messages to Discord. This constitutes automation and may violate the Discord Terms of Service. By using this feature, you acknowledge and accept any associated account risks (including but not limited to temporary restrictions or bans). The developer assumes no responsibility for Discord account issues caused by use of this extension.")
- `## Limitations` (same 5 items in English per D-91)
- `## Development`
- `## Privacy` — link to `./PRIVACY.md`

Heading count must match README.md exactly (same number of `## ` and `### ` lines).
  </action>
  <acceptance_criteria>
    - `README.en.md` exists at repo root
    - `README.en.md` first line contains `README.md` (cross-link)
    - `README.en.md` contains `# Web2Chat`
    - `README.en.md` contains `## Introduction`
    - `README.en.md` contains `## Installation`
    - `README.en.md` contains `## Usage`
    - `README.en.md` contains `## Platform Notes`
    - `README.en.md` contains `### OpenClaw`
    - `README.en.md` contains `### Discord`
    - `README.en.md` contains `Discord Terms of Service` (ToS reference)
    - `README.en.md` contains `## Limitations`
    - `README.en.md` contains `Firefox` (in limitations)
    - `README.en.md` contains `2000` (truncation limit)
    - `README.en.md` contains `## Development`
    - `README.en.md` contains `## Privacy`
    - `README.en.md` contains `PRIVACY.md` (link to privacy policy)
    - Number of `## ` headings in README.en.md equals number in README.md
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Introduction' README.en.md && grep -q '## Limitations' README.en.md && grep -q 'PRIVACY.md' README.en.md && test "$(grep -c '^## ' README.md)" = "$(grep -c '^## ' README.en.md)" && echo "PASS"</automated>
  </verify>
  <done>README.en.md exists with structurally equivalent English content, same heading count as README.md, Discord ToS notice, Limitations section, cross-link header</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> end user | Documentation must not contain sensitive internal details |
| README -> contributor | Development section must not leak credentials/secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | README.md | mitigate | No API keys, internal URLs, PII, or security-sensitive implementation details in user-facing documentation |
| T-07-03-02 | Spoofing | README.md | accept | README accurately represents extension capabilities; no inflated claims about security or privacy beyond what PRIVACY.md states |
</threat_model>

<verification>
- README.md is a complete zh_CN user document with 7 top-level sections
- README.en.md is a complete English document with matching structure
- Both files cross-link to each other
- Both contain Discord ToS notice
- Both contain 5 Limitations items
- Heading count matches between files
- No developer-period content remains (Phase 1 test scripts, project structure tree)
</verification>

<success_criteria>
- README.md fully rewritten as user-facing zh_CN doc per D-83/D-84/D-85/D-86
- README.en.md created as structurally equivalent English version
- Discord ToS risk clearly communicated per D-59/D-60
- Limitations section covers all 5 items per D-91
- Both files link to respective PRIVACY files
- `pnpm verify:readme` will pass once Plan 01 and Plan 02 also complete (cross-plan dependency)
</success_criteria>

<output>
After completion, create `.planning/phases/07-distribution/07-03-SUMMARY.md`
</output>
