---
phase: 05-discord
plan: "06"
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - entrypoints/background.ts
  - background/dispatch-pipeline.ts
autonomous: true
requirements: [ADD-03, ADD-04]
gap_closure: true
gaps_from: 05-UAT.md

must_haves:
  truths:
    - "粘贴发送后 Discord 输入框清空，不残留消息文本"
    - "SPA 路由切换后投递成功时 popup 显示成功状态，而非超时错误"
  artifacts:
    - path: "entrypoints/background.ts"
      provides: "discordMainWorldPaste — Enter 后延迟 200ms 派发 Escape keydown"
      contains: "Escape"
    - path: "background/dispatch-pipeline.ts"
      provides: "ADAPTER_RESPONSE_TIMEOUT_MS = 20_000"
      contains: "20_000"
  key_links:
    - from: "entrypoints/background.ts"
      to: "discordMainWorldPaste (MAIN world)"
      via: "setTimeout 200ms then dispatchEvent(KeyboardEvent Escape)"
      pattern: "Escape"
    - from: "background/dispatch-pipeline.ts"
      to: "ADAPTER_RESPONSE_TIMEOUT_MS"
      via: "constant increase 10_000 -> 20_000"
      pattern: "20_000"
---

<objective>
关闭 UAT 验收发现的两个 Gap：

- Gap 1（minor）：Discord 输入框在消息发送后残留消息文本
- Gap 2（major）：SPA 路由切换投递成功但 popup 报超时

两处均为单行 / 单函数级别修改，不涉及架构变更。

Purpose: 让 Phase 5 Discord 适配器通过完整 UAT，确保用户的一次投递在 popup 侧看到正确的成功状态，且 Discord 编辑器在发送后自动清空。

Output:
- `entrypoints/background.ts` — `discordMainWorldPaste` 在 Enter 后延迟 200ms 派发 Escape，触发 Discord 原生清空
- `background/dispatch-pipeline.ts` — `ADAPTER_RESPONSE_TIMEOUT_MS` 从 10_000 增至 20_000，覆盖两次 5s 内部等待 + paste 往返
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-discord/05-05-SUMMARY.md
@.planning/phases/05-discord/05-UAT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: discordMainWorldPaste — Enter 后延迟 200ms 派发 Escape</name>
  <files>entrypoints/background.ts, tests/unit/background/discordMainWorldPaste.spec.ts</files>
  <behavior>
    - Test 1: Enter 后调用 requestAnimationFrame / setTimeout ≥ 200ms，再派发 key=Escape keydown（冒泡）
    - Test 2: 若 editor 未找到，不派发任何事件，返回 false（现有行为不变）
    - Test 3: paste + Enter + Escape 三个事件均在同一 editor 元素上派发（顺序）
  </behavior>
  <action>
在 `entrypoints/background.ts` 的 `discordMainWorldPaste` 函数（当前第 40-73 行）中：

1. 当前末尾在派发 Enter keydown 后直接 `return true`。
2. 将函数签名改为 `async function discordMainWorldPaste(text: string): Promise<boolean>`（MAIN world 中 `async` 可用，`executeScript` 会等待 Promise resolve）。
3. 在 Enter keydown 派发之后，插入：

```typescript
await new Promise<void>((resolve) => setTimeout(resolve, 200));
editor.dispatchEvent(
  new KeyboardEvent('keydown', {
    key: 'Escape',
    bubbles: true,
    cancelable: true,
  }),
);
```

4. `return true` 保留在最末。

注意：`discordMainWorldPaste` 运行在 MAIN world（通过 `chrome.scripting.executeScript` 注入），因此可以使用 `setTimeout` + `async/await`。`executeScript` 的返回值会等待 Promise resolved 后才回传给 SW，所以 200ms delay 不会导致 Port 消息乱序。

不要修改函数外部的 Port 监听逻辑（background.ts 第 130-165 行）。
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose tests/unit/background/discordMainWorldPaste.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 新测试文件存在，3 条测试全绿
    - `entrypoints/background.ts` 中 `discordMainWorldPaste` 包含 `Escape` 关键字
    - `pnpm typecheck` 无新增错误
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ADAPTER_RESPONSE_TIMEOUT_MS 从 10_000 增至 20_000</name>
  <files>background/dispatch-pipeline.ts, tests/unit/background/dispatch-pipeline-timeout.spec.ts</files>
  <behavior>
    - Test 1: 导出常量 `ADAPTER_RESPONSE_TIMEOUT_MS` 的值等于 20000
    - Test 2: 该常量在 dispatch pipeline 内部被用于 alarms / timeout（existing reference 不破坏）
  </behavior>
  <action>
在 `background/dispatch-pipeline.ts` 第 189 行：

```typescript
// 旧
export const ADAPTER_RESPONSE_TIMEOUT_MS = 10_000;

// 新
export const ADAPTER_RESPONSE_TIMEOUT_MS = 20_000;
```

仅修改这一行。不要调整 WAIT_TIMEOUT_MS（discord.content.ts 中的 5000 不在本 gap 范围内）。

根因补充（仅供上下文，不影响改法）：
- discord.content.ts 内部 waitForElement (≤5s) + waitForNewMessage (≤5s) + paste 往返 (~1-2s) = 最坏 ~12s
- 旧 SW 超时 10s < 12s，消息实际发送但 SW 先超时报错
- 新 20s > 12s，留出足够余量

若已存在 `dispatch-pipeline-timeout.spec.ts`，追加 `ADAPTER_RESPONSE_TIMEOUT_MS === 20000` 断言即可；若不存在，新建最小文件仅断言该常量值。
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose tests/unit/background/dispatch-pipeline-timeout.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `ADAPTER_RESPONSE_TIMEOUT_MS` 在 `background/dispatch-pipeline.ts` 中值为 `20_000`
    - 相关测试绿灯
    - `pnpm typecheck` 无新增错误
  </done>
</task>

<task type="auto">
  <name>Task 3: 全量单元测试 + 构建验证</name>
  <files></files>
  <action>
依次运行：
1. `pnpm test` — 确认全量单元测试（193 条 + 本 plan 新增）全绿
2. `pnpm typecheck` — tsc --noEmit 无新增错误
3. `pnpm build` — 生产构建成功，manifest 校验通过

如有测试失败，定位根因并修复后重跑，直至全绿。不要跳过或注释任何已有测试。
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -10 && pnpm typecheck 2>&1 | tail -5 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `pnpm test` 无 failed，所有测试通过
    - `pnpm typecheck` 输出 0 errors
    - `pnpm build` 成功，dist/ 有 content-scripts/discord.js
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MAIN world → SW Port | discordMainWorldPaste 运行在 MAIN world，结果经 Port message 回传 SW |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Denial of Service | setTimeout 200ms in MAIN world | accept | 200ms 是一次性延迟，不循环；MAIN world 不影响 SW 生命周期 |
| T-05-06-02 | Tampering | Escape keydown 可能被页面脚本捕获 | accept | Escape 仅触发 Discord 原生清空，无敏感数据；页面脚本已可监听键盘事件 |
</threat_model>

<verification>
手工 E2E 验证步骤（执行完本 plan 后）：

1. `pnpm wxt build --mode development`
2. Chrome → `chrome://extensions` → Load unpacked → dist/
3. 打开任意页面，点击扩展 action 图标，填写 Discord 频道 URL + prompt
4. 点击发送 → 确认消息出现在 Discord 频道 → 观察 Discord 输入框是否清空（Gap 1）
5. 从 A 频道切换到 B 频道 URL 后发送 → 观察 popup 显示成功而非超时（Gap 2）
</verification>

<success_criteria>
- `discordMainWorldPaste` 在 Enter 后 200ms 派发 Escape，Discord 输入框清空
- `ADAPTER_RESPONSE_TIMEOUT_MS === 20_000`，SPA 切频道投递成功后 popup 显示 ok 而非 err
- `pnpm test` 全绿（含新增测试），`pnpm typecheck` 0 errors，`pnpm build` 成功
</success_criteria>

<output>
完成后创建 `.planning/phases/05-discord/05-06-SUMMARY.md`，记录：
- 两个 gap 的根因与修复方式
- 新增测试文件路径
- 单元测试总数（pre/post）
- 构建产物验证结果
</output>
