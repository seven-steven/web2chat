---
phase: 02-capture
plan: "07"
type: execute
wave: 6
depends_on:
  - "02-03"
  - "02-05"
  - "02-06"
files_modified:
  - tests/e2e/fixtures/article.html
  - tests/e2e/capture.spec.ts
  - playwright.config.ts
autonomous: true
requirements:
  - CAP-01
  - CAP-05

must_haves:
  truths:
    - "pnpm test:e2e 能运行（本地，不在 CI）"
    - "capture.spec.ts：fixture 文章页打开后，popup 在 2s 内显示 5 个字段（data-testid 可定位）"
    - "capture.spec.ts：title/description/content textarea 可以手动输入，值更新"
    - "capture.spec.ts：打开 popup 自身（chrome-extension:// URL 作为 active tab）→ [data-testid='capture-empty'] 可见（ROADMAP Phase 2 成功标准 #5 empty 路径由 Playwright 证明）"
    - "playwright.config.ts 中有 webServer 配置提供 fixture HTTP server"
    - "tests/e2e/fixtures/article.html 是有效 HTML 文章页（含 title / meta description / h1 / p 正文内容）"
  artifacts:
    - path: tests/e2e/fixtures/article.html
      provides: "本地 HTML fixture（Wikipedia/blog 风格文章页）"
      contains: "<meta name=\"description\""
      min_lines: 40
    - path: tests/e2e/capture.spec.ts
      provides: "E2E：fixture 页 → popup 2s 内填充 5 字段 + textarea 可编辑 + empty 状态"
      contains: "capture-field-title"
    - path: playwright.config.ts
      provides: "webServer 配置，提供 fixture HTTP server（http://localhost:PORT/）"
      contains: "webServer"
  key_links:
    - from: "playwright.config.ts webServer"
      to: "tests/e2e/capture.spec.ts"
      via: "http://localhost:PORT/article.html → 通过 SW URL scheme 预检（http:）"
      pattern: "webServer"
    - from: "tests/e2e/capture.spec.ts"
      to: "entrypoints/popup/App.tsx"
      via: "popup data-testid 定位 [data-testid=capture-success] + textarea 字段 + [data-testid=capture-empty]"
      pattern: "capture-success"
---

<objective>
创建 E2E 测试和 fixture，验证完整捕获链路：本地 HTML fixture 文章页 → popup 2s 内显示 5 字段并支持编辑；以及 chrome-extension:// URL 作为 active tab 时 popup 显示 empty 状态（ROADMAP 成功标准 #5 的 Playwright 覆盖）。

Purpose: ROADMAP Phase 2 成功标准 #1、#5 的 E2E 验证——"popup 在点击后 2s 内显示全部五个字段"、"用户可以编辑字段"，以及"empty 和 error 状态由 Playwright 证明"（empty 路径）。
Output: tests/e2e/fixtures/article.html + tests/e2e/capture.spec.ts + playwright.config.ts 更新；pnpm test:e2e 本地可运行。
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capture/02-CONTEXT.md
@.planning/phases/02-capture/02-RESEARCH.md
@.planning/phases/02-capture/02-PATTERNS.md

<interfaces>
<!-- 执行器需要的现有接口 -->

From tests/e2e/fixtures.ts (当前，不修改):
```typescript
export const test = base.extend<{
  context: BrowserContext;
  extensionId: string;
  reloadExtension: () => Promise<void>;
}>({
  context: async ({}, use) => {
    const extensionPath = resolve(process.cwd(), '.output/chrome-mv3');
    // launchPersistentContext + --load-extension
  },
  extensionId: async ({ context }, use) => {
    // parses from SW URL
  },
});
```

From tests/e2e/popup-rpc.spec.ts (pattern reference):
```typescript
import { test, expect } from './fixtures';

test('...', async ({ context, extensionId }) => {
  const popupUrl = `chrome-extension://${extensionId}/popup.html`;
  const popup = await context.newPage();
  await popup.goto(popupUrl);
  await popup.waitForSelector('[data-testid="popup-hello"]', { timeout: 2_000 });
});
```

From playwright.config.ts (当前):
```typescript
export default defineConfig({
  testDir: './tests/e2e',
  timeout: 30_000,
  fullyParallel: false,
  workers: 1,
  // 无 webServer 配置
});
```

App.tsx data-testid 约定 (Wave 5):
- [data-testid="capture-loading"] — loading skeleton
- [data-testid="capture-success"] — success state container
- [data-testid="capture-field-title"] — title textarea
- [data-testid="capture-field-description"] — description textarea
- [data-testid="capture-field-content"] — content textarea
- [data-testid="capture-field-url"] — url output
- [data-testid="capture-field-createAt"] — createAt output
- [data-testid="capture-empty"] — empty state（RESTRICTED_URL / EXTRACTION_EMPTY）

RESEARCH.md Open Question #1 resolution:
使用 playwright.config.ts webServer 配置起本地 HTTP server，
让 fixture 以 http://localhost:PORT/ 提供，通过 SW URL scheme 预检。

RESEARCH.md Open Question #2 + ROADMAP 成功标准 #5 resolution:
empty 路径由第三个 E2E test 覆盖：popup 自身（chrome-extension:// URL）作为
active tab 会被 SW URL scheme 预检拒绝，popup 渲染 [data-testid="capture-empty"]。
PATTERNS.md capture-restricted.spec.ts 模板提供此实现方式。

bringToFront() 注意：两个 article-page 测试在打开 popup 前需调用
articlePage.bringToFront() 以显式确保文章 tab 是 lastFocusedWindow 的活跃 tab，
防止 Playwright 聚焦顺序不确定导致 SW tabs.query 拿到错误的 active tab。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 fixture HTML + 更新 playwright.config.ts webServer</name>
  <files>tests/e2e/fixtures/article.html, playwright.config.ts</files>

  <read_first>
    - playwright.config.ts (当前全文，在末尾加 webServer 配置)
    - tests/e2e/fixtures.ts (了解 context fixture 和 extensionPath 结构，确认 fixtures/ 目录存在)
  </read_first>

  <action>
**创建 tests/e2e/fixtures/article.html：**

创建一个完整的 Wikipedia/blog 风格 HTML 文章页，确保 Readability 能正确抽取内容：

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="A comprehensive guide to understanding web scraping techniques and best practices for modern web development.">
  <title>Web Scraping: A Practical Guide</title>
</head>
<body>
  <header>
    <nav>
      <a href="/">Home</a>
      <a href="/blog">Blog</a>
    </nav>
  </header>

  <main>
    <article>
      <h1>Web Scraping: A Practical Guide</h1>
      <p class="byline">By Web Dev Weekly &middot; April 30, 2026</p>

      <p>Web scraping is the process of automatically extracting information from websites. Modern tools and techniques make this task both efficient and reliable when done correctly.</p>

      <h2>Understanding the Basics</h2>
      <p>At its core, web scraping involves sending HTTP requests to web pages, parsing the HTML response, and extracting the relevant data. The key challenge lies in handling the diverse structure of modern web pages.</p>

      <p>Before starting any scraping project, it is important to review the target website's terms of service and robots.txt file. Respecting these guidelines ensures ethical and legal compliance.</p>

      <h2>Choosing the Right Tools</h2>
      <p>Several libraries and frameworks are available for web scraping. The choice depends on your specific needs, including the complexity of the target site and the programming language you prefer.</p>

      <pre><code class="language-javascript">
// Example: Using fetch to retrieve page content
const response = await fetch('https://example.com/article');
const html = await response.text();
console.log(html.slice(0, 200));
      </code></pre>

      <h2>Handling Dynamic Content</h2>
      <p>Many modern websites use JavaScript to render content dynamically. Traditional HTTP-based scrapers may miss this content because it is loaded after the initial page response.</p>

      <p>For dynamic sites, headless browser tools provide a more robust solution by executing JavaScript and rendering the full page before extraction begins.</p>

      <h2>Best Practices</h2>
      <ul>
        <li>Rate limit your requests to avoid overwhelming target servers</li>
        <li>Use proper User-Agent headers to identify your scraper</li>
        <li>Handle errors gracefully and implement retry logic</li>
        <li>Cache responses when appropriate to reduce server load</li>
        <li>Monitor your scraper's impact on the target site</li>
      </ul>

      <h2>Conclusion</h2>
      <p>Web scraping, when done responsibly, is a powerful technique for data collection and analysis. By following best practices and respecting website policies, you can build reliable and ethical scraping solutions.</p>

      <p>For more information, see the <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP">MDN Web Docs on HTTP</a> and related resources.</p>
    </article>
  </main>

  <footer>
    <p>&copy; 2026 Web Dev Weekly. All rights reserved.</p>
  </footer>
</body>
</html>
```

**更新 playwright.config.ts：**

在现有配置中追加 webServer 字段。使用 `npx serve` 或 Node.js 内置 http 模块提供 fixture 文件。推荐使用 `npx serve` 因为项目已有 Node.js 环境：

读取当前 playwright.config.ts 后，在 `defineConfig({...})` 内追加 webServer：

```typescript
webServer: {
  command: 'npx --yes serve tests/e2e/fixtures --listen 4321 --no-clipboard',
  port: 4321,
  reuseExistingServer: !process.env.CI,
  timeout: 10_000,
},
```

这样 fixture 文件通过 `http://localhost:4321/article.html` 访问，通过 SW URL scheme 预检（http:）。

同时在 `use:` 块中追加 baseURL（供 E2E 测试使用）：
```typescript
use: {
  headless: false,
  actionTimeout: 5_000,
  baseURL: 'http://localhost:4321',
},
```
  </action>

  <verify>
    <automated>test -f /data/coding/projects/seven/web2chat/tests/e2e/fixtures/article.html && grep -c "meta name=\"description\"" /data/coding/projects/seven/web2chat/tests/e2e/fixtures/article.html && grep -c "webServer" /data/coding/projects/seven/web2chat/playwright.config.ts</automated>
  </verify>

  <acceptance_criteria>
    - tests/e2e/fixtures/article.html 存在且包含 `&lt;meta name="description"` 标签
    - tests/e2e/fixtures/article.html 包含 `&lt;h1&gt;` 标签（Readability 需要标题）
    - tests/e2e/fixtures/article.html 包含至少 3 个 `&lt;p&gt;` 标签（足够的正文内容供 Readability 抽取）
    - tests/e2e/fixtures/article.html 包含 `&lt;pre&gt;&lt;code&gt;` 块（测试 GFM fenced code 保留）
    - tests/e2e/fixtures/article.html 包含 `&lt;a href` 链接（测试 Markdown link 保留）
    - playwright.config.ts 包含字符串 "webServer"
    - playwright.config.ts 包含字符串 "4321"（端口号，与 baseURL 一致——端口策略只在 playwright.config.ts 一处定义，spec.ts 使用相对路径不重复此值）
    - playwright.config.ts 包含字符串 "baseURL"（供 spec.ts 相对路径解析）
    - pnpm typecheck 退出码 0
  </acceptance_criteria>

  <done>article.html 创建完毕（40+ 行），playwright.config.ts 追加 webServer 配置，typecheck 通过。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 tests/e2e/capture.spec.ts（含 empty 状态 E2E）</name>
  <files>tests/e2e/capture.spec.ts</files>

  <read_first>
    - tests/e2e/popup-rpc.spec.ts (完整阅读，理解 page.goto popup + waitForSelector 模式)
    - tests/e2e/fixtures.ts (理解 context / extensionId fixture，确认可直接复用)
    - playwright.config.ts (确认 webServer port 为 4321 和 baseURL)
    - .planning/phases/02-capture/02-PATTERNS.md (capture.spec.ts 模式节 + capture-restricted.spec.ts 模板节)
  </read_first>

  <behavior>
    - Test 1（5 字段填充）：在 2s 内 [data-testid="capture-success"] 出现；5 个字段均非空
    - Test 2（textarea 可编辑）：title/description textarea 可以用 fill() 写入，新值即时生效
    - Test 3（empty 状态）：直接打开 popup（chrome-extension:// 作为 active tab）→ [data-testid="capture-empty"] 出现

    **page 创建顺序（两个 article 测试的强制约束）：**
    (1) articlePage = newPage + goto('/article.html')（相对 URL，由 baseURL 自动拼接）
    (2) popup = newPage（blank — 不立即 navigate）
    (3) articlePage.bringToFront()（此时 popup newPage 已创建，不会再夺回焦点）
    (4) popup.goto(popupUrl)（page.goto 不改变 lastFocusedWindow，article 保持 active）

    Article URL 使用相对路径 '/article.html'（playwright.config.ts baseURL 自动拼接为 http://localhost:4321/article.html），端口仅在 playwright.config.ts 一处定义，spec.ts 不硬编码完整 URL。
  </behavior>

  <action>
创建 tests/e2e/capture.spec.ts（包含 3 个 E2E 测试）：

```typescript
/**
 * E2E tests for Phase 2 capture pipeline (CAP-01, CAP-05).
 *
 * Test strategy (RESEARCH.md Open Questions resolution):
 *
 * Q1 (E2E fixture URL): playwright.config.ts webServer serves tests/e2e/fixtures/
 *    at http://localhost:4321/ via baseURL. Article tests use relative paths
 *    ('/article.html') — port is defined only in playwright.config.ts, not here.
 *
 * Q2 + ROADMAP #5 (empty state by Playwright): Test 3 opens popup directly.
 *    When popup is the active tab (chrome-extension:// URL), SW's tabs.query
 *    returns the popup page itself, which fails the URL scheme precheck (D-16),
 *    triggering RESTRICTED_URL → popup renders [data-testid="capture-empty"].
 *    This satisfies ROADMAP Phase 2 success criterion #5 for the empty path.
 *
 * Page ordering for article tests (deterministic bringToFront):
 *   1. Open article tab (newPage + goto)
 *   2. Pre-create popup tab (newPage — blank, NOT yet navigated)
 *   3. articlePage.bringToFront() — ensures article is lastFocusedWindow active tab
 *   4. popup.goto(popupUrl) — page.goto on existing tab does NOT steal focus;
 *      SW's tabs.query at capture time sees the article tab as active.
 *
 * Why pre-create popup before bringToFront:
 *   context.newPage() automatically brings the new page to front.
 *   If we called newPage(popup) AFTER bringToFront(article), the new popup page
 *   would steal focus again. Pre-creating popup first, then bringToFront(article),
 *   then popup.goto() keeps article as the focused tab when capture fires.
 */

import { test, expect } from './fixtures';

test('capture: fixture article page fills 5 fields within 2s (CAP-01, CAP-05)', async ({
  context,
  extensionId,
}) => {
  // Step 1: Open the fixture article page (relative path — baseURL prepended)
  const articlePage = await context.newPage();
  await articlePage.goto('/article.html', {
    waitUntil: 'domcontentloaded',
  });

  // Step 2: Pre-create popup page (blank — not yet navigated to popup.html)
  const popupUrl = `chrome-extension://${extensionId}/popup.html`;
  const popup = await context.newPage();

  // Step 3: Bring article to front AFTER creating popup page so popup's newPage()
  // focus steal doesn't override the ordering.
  await articlePage.bringToFront();

  // Step 4: Navigate pre-created popup page — page.goto does NOT steal focus,
  // so articlePage stays as lastFocusedWindow active tab for SW's tabs.query.
  await popup.goto(popupUrl);

  // Step 5: Wait for loading skeleton to clear and success view to appear.
  // Timeout 2000ms per ROADMAP Phase 2 success criterion #1.
  await popup.waitForSelector('[data-testid="capture-success"]', { timeout: 2_000 });

  // Step 6: Assert all 5 fields are present and non-empty.
  const titleEl = popup.locator('[data-testid="capture-field-title"]');
  const descriptionEl = popup.locator('[data-testid="capture-field-description"]');
  const contentEl = popup.locator('[data-testid="capture-field-content"]');
  const urlEl = popup.locator('[data-testid="capture-field-url"]');
  const createAtEl = popup.locator('[data-testid="capture-field-createAt"]');

  await expect(titleEl).not.toHaveValue('');
  await expect(descriptionEl).not.toHaveValue('');
  await expect(contentEl).not.toHaveValue('');
  await expect(urlEl).not.toBeEmpty();
  await expect(createAtEl).not.toBeEmpty();

  // Step 7: Assert title textarea contains the article title
  const titleValue = await titleEl.inputValue();
  expect(titleValue.length).toBeGreaterThan(0);

  // Step 8: Assert URL output shows the fixture URL (contains localhost)
  const urlText = await urlEl.textContent();
  expect(urlText).toContain('localhost');

  await articlePage.close();
  await popup.close();
});

test('capture: textarea fields are editable after capture (CAP-05, D-21)', async ({
  context,
  extensionId,
}) => {
  // Open article page, pre-create popup, bringToFront article, then popup.goto
  const articlePage = await context.newPage();
  await articlePage.goto('/article.html', {
    waitUntil: 'domcontentloaded',
  });
  const popupUrl = `chrome-extension://${extensionId}/popup.html`;
  const popup = await context.newPage();
  await articlePage.bringToFront();
  await popup.goto(popupUrl);

  // Wait for success state
  await popup.waitForSelector('[data-testid="capture-success"]', { timeout: 2_000 });

  // Edit title textarea — D-21: always-on textarea
  const titleEl = popup.locator('[data-testid="capture-field-title"]');
  await titleEl.click();
  await titleEl.fill('Edited Title');
  await expect(titleEl).toHaveValue('Edited Title');

  // Edit description textarea
  const descriptionEl = popup.locator('[data-testid="capture-field-description"]');
  await descriptionEl.fill('Edited description for testing');
  await expect(descriptionEl).toHaveValue('Edited description for testing');

  await articlePage.close();
  await popup.close();
});

test('capture: chrome-extension:// active tab → empty state visible (ROADMAP #5, D-16)', async ({
  context,
  extensionId,
}) => {
  // Open popup directly — popup itself (chrome-extension:// URL) becomes the
  // active tab in lastFocusedWindow. SW's URL scheme precheck rejects it
  // (scheme !== 'http' && scheme !== 'https') → Err('RESTRICTED_URL') →
  // popup renders [data-testid="capture-empty"].
  //
  // This test proves the empty state by Playwright, satisfying ROADMAP Phase 2
  // success criterion #5. No article page is opened — popup is the only page.
  const popupUrl = `chrome-extension://${extensionId}/popup.html`;
  const popup = await context.newPage();
  await popup.goto(popupUrl);

  // Wait up to 2s for empty state — SW pipeline should fail fast on URL precheck
  await expect(popup.locator('[data-testid="capture-empty"]')).toBeVisible({ timeout: 2_000 });

  await popup.close();
});
```
  </action>

  <verify>
    <automated>grep -c "capture-success" /data/coding/projects/seven/web2chat/tests/e2e/capture.spec.ts && grep -c "capture-empty" /data/coding/projects/seven/web2chat/tests/e2e/capture.spec.ts && grep -c "bringToFront" /data/coding/projects/seven/web2chat/tests/e2e/capture.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - tests/e2e/capture.spec.ts 包含字符串 "capture-success"（waitForSelector 断言）
    - tests/e2e/capture.spec.ts 包含字符串 "capture-field-title"
    - tests/e2e/capture.spec.ts 包含字符串 "capture-field-description"
    - tests/e2e/capture.spec.ts 包含字符串 "capture-field-content"
    - tests/e2e/capture.spec.ts 包含字符串 "capture-field-url"
    - tests/e2e/capture.spec.ts 包含字符串 "capture-field-createAt"
    - tests/e2e/capture.spec.ts 包含字符串 "timeout: 2_000"（2s 时限）
    - tests/e2e/capture.spec.ts 包含字符串 "Edited Title"（编辑测试）
    - tests/e2e/capture.spec.ts 包含字符串 "'/article.html'"（相对 URL，依赖 playwright.config baseURL）
    - tests/e2e/capture.spec.ts 不包含字符串 "http://localhost"（fixture URL 不硬编码完整 URL）：`! grep -q 'http://localhost' tests/e2e/capture.spec.ts`
    - tests/e2e/capture.spec.ts 包含字符串 "capture-empty"（empty 状态 E2E 断言）
    - `grep -c "bringToFront" tests/e2e/capture.spec.ts` >= 2（两个 article 测试各调用一次）
    - tests/e2e/capture.spec.ts 包含 3 个 test() 调用（success + editable + empty）
    - pnpm typecheck 退出码 0
  </acceptance_criteria>

  <done>capture.spec.ts 创建完毕（3 个 E2E 测试：5 字段填充 2s + 编辑可用 + empty 状态 Playwright 证明），article 测试使用确定性 page 创建顺序（newPage popup → bringToFront article → popup.goto），fixture URL 使用相对路径，typecheck 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E fixture HTTP server → extension | fixture 以 localhost:4321 提供，通过 SW URL scheme 预检（http:）；fixture 内容受控 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | E2E fixture 被修改导致 Readability 无法抽取 | mitigate | article.html 设计为稳定的静态 HTML（无 JS 渲染），Readability 目标是 &lt;article&gt; 元素；fixture 提交进 git 版本控制 |
| T-02-07-02 | Information Disclosure | E2E 测试通过 localhost HTTP 泄露内容 | accept | fixture 仅在本地测试时运行，内容为虚构测试数据；无生产数据泄露风险 |
| T-02-07-03 | Denial of Service | webServer 端口冲突导致 E2E 失败 | accept | playwright.config.ts 使用 reuseExistingServer:!process.env.CI；本地开发时端口 4321 冲突罕见；CI 不跑 E2E（D-11） |
</threat_model>

<verification>
```bash
cd /data/coding/projects/seven/web2chat
test -f tests/e2e/fixtures/article.html && echo "article.html exists"
grep -c "meta name=\"description\"" tests/e2e/fixtures/article.html
grep -c "webServer" playwright.config.ts
grep -c "baseURL" playwright.config.ts
grep -c "capture-success" tests/e2e/capture.spec.ts
grep -c "capture-empty" tests/e2e/capture.spec.ts
grep -c "bringToFront" tests/e2e/capture.spec.ts  # 应 >= 2
! grep -q 'http://localhost' tests/e2e/capture.spec.ts && echo "no hardcoded URL in spec"
pnpm typecheck
pnpm build 2>&1 | tail -3
```
所有检查通过，bringToFront >= 2，无硬编码 localhost URL，typecheck + build 退出码 0。

E2E 本地运行（需要先 pnpm build）：
```bash
pnpm build && pnpm test:e2e -- capture.spec.ts
```
</verification>

<success_criteria>
tests/e2e/fixtures/article.html 创建完毕（含 meta description / h1 / 正文 / code block / 链接）；playwright.config.ts 追加 webServer 配置（port: 4321）+ baseURL；tests/e2e/capture.spec.ts 包含 3 个 E2E 测试（2s 内 5 字段填充 + textarea 可编辑 + empty 状态 Playwright 证明）；两个 article 测试使用确定性 page 顺序（newPage popup → bringToFront article → popup.goto）和相对 fixture URL；spec.ts 不硬编码完整 localhost URL；pnpm typecheck + build 通过；本地 pnpm test:e2e -- capture.spec.ts 可运行。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-capture/02-07-SUMMARY.md`
</output>
