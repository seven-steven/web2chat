---
phase: 11-telegram-adapter
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/adapters/telegram-format.ts
  - tests/unit/adapters/telegram-format.spec.ts
autonomous: true
requirements:
  - TG-03

must_haves:
  truths:
    - "composeTelegramMessage 输出纯文本（无 markdown 语法）"
    - "字段排列顺序为 prompt -> title -> url -> description -> timestamp -> content"
    - "内容超过 4096 字符时按 metadata 优先策略截断，追加 ...[truncated] 标记"
    - "metadata 本身超过 4096 字符时，content 完全省略，metadata 硬截断到 4096 字符"
    - "恰好 4096 字符的消息不截断"
  artifacts:
    - path: "shared/adapters/telegram-format.ts"
      provides: "Telegram 消息格式化 + metadata-first 截断"
      exports: ["composeTelegramMessage", "Snapshot"]
    - path: "tests/unit/adapters/telegram-format.spec.ts"
      provides: "Format + truncation 单元测试"
  key_links:
    - from: "shared/adapters/telegram-format.ts"
      to: "entrypoints/telegram.content.ts"
      via: "import { composeTelegramMessage }"
      pattern: "composeTelegramMessage"
---

<objective>
实现 Telegram 消息格式化模块（D-140..D-145）。

Purpose: 为 Telegram 适配器提供纯文本消息格式化和 4096 字符 metadata-first 截断功能。Telegram 有严格的 4096 字符单条消息限制，必须实现结构化截断（与 Slack 的 40K 限制不同）。纯函数模块，无 chrome.* / WXT 依赖，content script 和单元测试共用。
Output: `shared/adapters/telegram-format.ts` + 完整测试覆盖
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-telegram-adapter/11-CONTEXT.md
@.planning/phases/11-telegram-adapter/11-RESEARCH.md
@.planning/phases/11-telegram-adapter/11-PATTERNS.md

<interfaces>
<!-- Discord format module — structural template for Telegram format module -->
From shared/adapters/discord-format.ts:
```typescript
export interface Snapshot {
  title: string;
  url: string;
  description: string;
  create_at: string;
  content: string;
}
export const DISCORD_CHAR_LIMIT = 2000;
export const TRUNCATION_SUFFIX = '\n...[truncated]';
export function escapeMentions(text: string): string;
export function composeDiscordMarkdown(payload: {
  prompt: string;
  snapshot: Snapshot;
  timestampLabel?: string;
}): string;
```

<!-- Slack format module — field ordering pattern -->
From shared/adapters/slack-format.ts:
```typescript
export interface Snapshot {
  title: string;
  url: string;
  description: string;
  create_at: string;
  content: string;
}
export function composeSlackMrkdwn(payload: {
  prompt: string;
  snapshot: Snapshot;
  timestampLabel: string;
}): string;
```
</interfaces>
</context>

<feature>
  <name>Telegram plain-text formatting with metadata-first truncation</name>
  <files>shared/adapters/telegram-format.ts, tests/unit/adapters/telegram-format.spec.ts</files>
  <behavior>
    composeTelegramMessage:
    - 字段排列: prompt -> title -> url -> description -> timestamp -> content (D-142)
    - 纯文本输出，无 markdown 语法（D-141: Telegram Web K paste sanitizer 会 strip 外部格式）
    - 空字段整体省略
    - 4096 字符硬限制（D-143）
    - 截断发生在格式化之后（D-144）
    - metadata 优先截断策略:
      1. 先尝试保留 prompt + title + url + description + timestamp，截断 content
      2. 如果去掉 content 仍超 4096，再去掉 description，截断剩余部分
      3. 最后手段：硬截断到 4096 - suffix.length，追加 suffix
    - 截断标记: `\n...[truncated]`（D-145）
    - 恰好 4096 字符不截断

    Cases (input -> expected output):
    - Full snapshot with prompt -> "prompt\n\nTitle\n\nurl\n\nDesc\n\nCaptured at ts\n\nContent"
    - Empty prompt -> starts with "Title"
    - All fields empty -> empty string
    - Content > 4096 chars -> truncated with suffix, metadata preserved
    - Metadata-only > 4096 chars -> content omitted, metadata hard-truncated with suffix
    - Exactly 4096 chars -> no truncation
  </behavior>
  <implementation>
    参考 discord-format.ts / slack-format.ts 结构，创建 telegram-format.ts：
    1. 复用 Snapshot 接口（与 discord-format.ts / slack-format.ts 相同结构）
    2. 纯文本 compose — 无 escape 函数（Telegram 纯文本无 mention 风险 per RESEARCH）
    3. metadata-first truncation 逻辑（RESEARCH Code Examples 中的完整实现）
    4. 常量: TELEGRAM_CHAR_LIMIT = 4096, TRUNCATION_SUFFIX = '\n...[truncated]'
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Telegram format failing tests</name>
  <files>tests/unit/adapters/telegram-format.spec.ts</files>
  <read_first>
    tests/unit/adapters/discord-format.spec.ts
    shared/adapters/discord-format.ts
    shared/adapters/slack-format.ts
    .planning/phases/11-telegram-adapter/11-RESEARCH.md (Code Examples: Telegram Format with Metadata-First Truncation)
    .planning/phases/11-telegram-adapter/11-CONTEXT.md (D-140..D-145)
  </read_first>
  <acceptance_criteria>
    - File `tests/unit/adapters/telegram-format.spec.ts` exists
    - File imports `composeTelegramMessage` and `Snapshot` from `@/shared/adapters/telegram-format`
    - Contains `describe('adapters/telegram — composeTelegramMessage')` with tests for:
      - full formatting with all fields
      - empty fields omitted
      - prompt omission
      - content < 4096 chars: no truncation
      - content > 4096 chars: truncates with `...[truncated]` suffix, metadata preserved
      - metadata-only exceeds 4096: content omitted, metadata truncated with suffix
      - boundary: exactly 4096 chars (should NOT truncate)
      - plain text output (no markdown syntax in result)
    - `pnpm test:unit -- tests/unit/adapters/telegram-format.spec.ts` exits with failures (RED)
  </acceptance_criteria>
  <action>
    创建测试文件 `tests/unit/adapters/telegram-format.spec.ts`，参考 `tests/unit/adapters/discord-format.spec.ts` 结构。

    测试结构:
    1. `describe('adapters/telegram — composeTelegramMessage')`:
       - "formats prompt-first with plain text title": 使用 fullSnapshot，验证 lines[0] 为 prompt，输出包含 `Title`（无 `*Title*` 或 `**Title**`），包含 url, description, `Captured at` prefix, content
       - "omits empty fields entirely": sparse snapshot 只有 title，输出为 `Title`
       - "omits prompt when empty": 空 prompt，输出以 `Title` 开头
       - "does NOT truncate when under 4096 chars": 短 content，验证输出长度等于原始长度
       - "truncates content with suffix when over 4096 chars": 长 content（>4096），验证输出长度 <= 4096，以 `...[truncated]` 结尾，metadata（prompt/title/url/description/timestamp）完整保留在截断标记之前
       - "omits content and truncates metadata when metadata alone exceeds 4096": 超长 prompt + title + url + description + timestamp（总和 >4096），验证输出中不含 content 字段内容，以 `...[truncated]` 结尾
       - "does NOT truncate at exactly 4096 chars": 构造恰好 4096 字符的输入，验证输出不以 `...[truncated]` 结尾
       - "outputs plain text without markdown syntax": 验证输出不包含 `*`、`>`、`**` 等 markdown 标记

    Import from `@/shared/adapters/telegram-format`（文件尚未存在，运行必然失败）。
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/adapters/telegram-format.spec.ts 2>&1 | head -5; echo "EXIT: $?"</automated>
  </verify>
  <done>测试文件存在，所有测试因 import 失败而 RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement telegram-format.ts</name>
  <files>shared/adapters/telegram-format.ts</files>
  <read_first>
    shared/adapters/discord-format.ts
    shared/adapters/slack-format.ts
    tests/unit/adapters/telegram-format.spec.ts
    .planning/phases/11-telegram-adapter/11-RESEARCH.md (Code Examples: Telegram Format with Metadata-First Truncation)
    .planning/phases/11-telegram-adapter/11-CONTEXT.md (D-140..D-145)
  </read_first>
  <acceptance_criteria>
    - File `shared/adapters/telegram-format.ts` exists
    - Exports `composeTelegramMessage` and `Snapshot` interface
    - NO `chrome.*` or WXT imports
    - `TELEGRAM_CHAR_LIMIT = 4096`
    - `TRUNCATION_SUFFIX = '\n...[truncated]'`
    - Field order: prompt -> title -> url -> description -> timestamp -> content
    - Plain text output — no markdown syntax
    - Metadata-first truncation: tries content truncation first, then description omission, then hard truncate
    - `pnpm test:unit -- tests/unit/adapters/telegram-format.spec.ts` exits 0 (all GREEN)
  </acceptance_criteria>
  <action>
    创建 `shared/adapters/telegram-format.ts`，参考 `discord-format.ts` / `slack-format.ts` 结构但替换为纯文本 + metadata-first 截断。

    结构:
    ```typescript
    /**
     * Telegram message formatting (D-140..D-145).
     * Pure utility — no WXT or chrome.* imports.
     * Imported by both the adapter content script and unit tests.
     *
     * Telegram Web K has a 4096 character hard limit per message.
     * We use metadata-first truncation: preserve prompt/title/url/
     * description/timestamp, truncate content first, then description,
     * then hard-truncate with suffix as last resort.
     */

    export interface Snapshot {
      title: string;
      url: string;
      description: string;
      create_at: string;
      content: string;
    }

    const TELEGRAM_CHAR_LIMIT = 4096;
    const TRUNCATION_SUFFIX = '\n...[truncated]';

    export function composeTelegramMessage(payload: {
      prompt: string;
      snapshot: Snapshot;
      timestampLabel: string;
    }): string {
      const { prompt, snapshot, timestampLabel } = payload;

      // Build full message
      const lines: string[] = [];
      if (prompt) lines.push(prompt, '');
      if (snapshot.title) lines.push(snapshot.title, '');
      if (snapshot.url) lines.push(snapshot.url, '');
      if (snapshot.description) lines.push(snapshot.description, '');
      if (snapshot.create_at) lines.push(`${timestampLabel} ${snapshot.create_at}`, '');
      if (snapshot.content) lines.push(snapshot.content);

      let result = lines.join('\n').trim();
      if (result.length <= TELEGRAM_CHAR_LIMIT) return result;

      // Step 1: Truncate content first
      const headerLines: string[] = [];
      if (prompt) headerLines.push(prompt, '');
      if (snapshot.title) headerLines.push(snapshot.title, '');
      if (snapshot.url) headerLines.push(snapshot.url, '');
      if (snapshot.description) headerLines.push(snapshot.description, '');
      if (snapshot.create_at) headerLines.push(`${timestampLabel} ${snapshot.create_at}`, '');

      const headerText = headerLines.join('\n');
      const headerLen = headerText.length;

      if (snapshot.content) {
        const separator = headerLen > 0 ? '\n' : '';
        const available = TELEGRAM_CHAR_LIMIT - headerLen - separator.length - TRUNCATION_SUFFIX.length;
        if (available > 0) {
          const truncatedContent = snapshot.content.slice(0, available);
          return headerLen > 0
            ? `${headerText}${separator}${truncatedContent}${TRUNCATION_SUFFIX}`
            : `${truncatedContent}${TRUNCATION_SUFFIX}`;
        }
      }

      // Step 2: Content removed entirely, still over — truncate description
      const noContentLines: string[] = [];
      if (prompt) noContentLines.push(prompt, '');
      if (snapshot.title) noContentLines.push(snapshot.title, '');
      if (snapshot.url) noContentLines.push(snapshot.url, '');
      if (snapshot.create_at) noContentLines.push(`${timestampLabel} ${snapshot.create_at}`, '');

      const noContentText = noContentLines.join('\n');
      if (snapshot.description) {
        const separator = noContentText.length > 0 ? '\n' : '';
        const available = TELEGRAM_CHAR_LIMIT - noContentText.length - separator.length - TRUNCATION_SUFFIX.length;
        if (available > 0) {
          const truncatedDesc = snapshot.description.slice(0, available);
          return noContentText.length > 0
            ? `${noContentText}${separator}${truncatedDesc}${TRUNCATION_SUFFIX}`
            : `${truncatedDesc}${TRUNCATION_SUFFIX}`;
        }
      }

      // Step 3: Last resort — hard truncate
      return noContentText.slice(0, TELEGRAM_CHAR_LIMIT - TRUNCATION_SUFFIX.length).trim() + TRUNCATION_SUFFIX;
    }
    ```

    实现后运行测试确认 GREEN。
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/adapters/telegram-format.spec.ts --reporter=verbose</automated>
  </verify>
  <done>所有 telegram-format 测试 GREEN；模块导出 composeTelegramMessage + Snapshot；metadata-first 截断逻辑正确；4096 边界测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 -> composeTelegramMessage | 用户提供的 prompt/snapshot 字段进入格式化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | composeTelegramMessage | mitigate | 4096 字符截断防止超长消息导致 Telegram 拒绝或 UI 异常 |
| T-11-02 | Tampering | composeTelegramMessage | accept | 内容经 @mozilla/readability + dompurify 净化后才进入格式化；纯文本无 HTML 渲染能力 |
</threat_model>

<verification>
```bash
pnpm test:unit -- tests/unit/adapters/telegram-format.spec.ts --reporter=verbose
```
All tests pass.
</verification>

<success_criteria>
- `composeTelegramMessage` 输出纯文本（无 markdown 语法）
- 字段排列顺序: prompt -> title -> url -> description -> timestamp -> content
- 4096 字符 metadata-first 截断：content 先截，description 次之，最后硬截
- 恰好 4096 字符不截断
- 所有测试 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/11-telegram-adapter/11-01-SUMMARY.md`
</output>
