---
phase: 11-telegram-adapter
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/adapters/telegram-login-detect.ts
  - tests/unit/adapters/telegram-login.spec.ts
  - tests/unit/adapters/telegram-match.spec.ts
autonomous: true
requirements:
  - TG-01
  - TG-02

must_haves:
  truths:
    - "Telegram URL 匹配只接受 web.telegram.org 且 pathname 以 /a/ 开头"
    - "Telegram 登录墙 DOM 检测在电话输入框、auth class 存在时返回 true"
    - "[class*=\"login\"] 仅在 .input-message-input 不存在时匹配，防止已登录页面误报"
    - "正常聊天编辑器 UI 不会触发登录检测误报"
  artifacts:
    - path: "shared/adapters/telegram-login-detect.ts"
      provides: "Telegram DOM login wall detection"
      exports: ["detectLoginWall"]
    - path: "tests/unit/adapters/telegram-login.spec.ts"
      provides: "Login detection unit tests"
    - path: "tests/unit/adapters/telegram-match.spec.ts"
      provides: "URL match unit tests"
  key_links:
    - from: "shared/adapters/telegram-login-detect.ts"
      to: "entrypoints/telegram.content.ts"
      via: "import { detectLoginWall }"
      pattern: "detectLoginWall"
    - from: "shared/adapters/registry.ts"
      to: "tests/unit/adapters/telegram-match.spec.ts"
      via: "import { findAdapter, detectPlatformId }"
      pattern: "findAdapter.*telegram"
---

<objective>
实现 Telegram 登录墙 DOM 检测 + Telegram URL 匹配测试。

Purpose: 为 Telegram 适配器提供登录检测能力（D-152），并为 registry 的 Telegram URL 匹配提供测试覆盖（TG-01）。Telegram Web K 使用 hash-based 路由（`#/auth`），URL 层 `loggedOutPathPatterns` 只检查 pathname 无法捕获登录状态，因此 DOM 层检测是主要防线（RESEARCH Pitfall 1）。
Output: `telegram-login-detect.ts` + 测试 + URL 匹配测试
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-telegram-adapter/11-CONTEXT.md
@.planning/phases/11-telegram-adapter/11-RESEARCH.md
@.planning/phases/11-telegram-adapter/11-PATTERNS.md

<interfaces>
<!-- Slack login detect — structural template for Telegram login detect -->
From shared/adapters/slack-login-detect.ts:
```typescript
export function detectLoginWall(): boolean {
  // Unconditional markers
  if (document.querySelector('input[type="email"][name="email"]')) return true;
  if (document.querySelector('button[data-qa="sign_in_button"]')) return true;
  if (document.querySelector('[class*="signin"]')) return true;

  // Guarded marker — only match when editor is NOT present
  if (!document.querySelector('.ql-editor')) {
    if (document.querySelector('[class*="login"]')) return true;
  }

  return false;
}
```

<!-- Registry match function interface -->
From shared/adapters/registry.ts:
```typescript
export function findAdapter(url: string): AdapterRegistryEntry | undefined;
export function detectPlatformId(url: string): PlatformId | null;
```
</interfaces>
</context>

<feature>
  <name>Telegram login detection + URL match tests</name>
  <files>shared/adapters/telegram-login-detect.ts, tests/unit/adapters/telegram-login.spec.ts, tests/unit/adapters/telegram-match.spec.ts</files>
  <behavior>
    detectLoginWall:
    - 电话输入框存在 (`input[type="tel"]`, `input[name="phone"]`) -> true
    - auth class 存在 (`[class*="auth"]`) -> true
    - login class 存在且 `.input-message-input[contenteditable="true"]` 不存在 -> true（guard 防误报）
    - login class 存在且 `.input-message-input[contenteditable="true"]` 存在 -> false（已登录页面有 login class 不应误报）
    - 正常聊天编辑器（`.input-message-input[contenteditable="true"]`） -> false
    - 空 body -> false
    - 残留 contenteditable + 登录 UI 共存 -> true（不误报为正常）

    URL match (via registry — tested after Plan 03 adds the entry):
    - Valid: https://web.telegram.org/a/#123456
    - Valid: https://web.telegram.org/a/chat/123
    - Invalid: https://web.telegram.org/z/#123 (Web Z, not Web K)
    - Invalid: https://web.telegram.org/ (root path)
    - Invalid: https://discord.com/channels/1/2 (wrong platform)
    - Invalid: malformed / empty
  </behavior>
  <implementation>
    telegram-login-detect.ts: 参考 slack-login-detect.ts，替换 DOM 选择器为 Telegram 登录页标记。纯函数，无 chrome.* 依赖。

    telegram-match.spec.ts: 参考 discord-match.spec.ts / slack-match.spec.ts 结构。注意：此测试依赖 registry 中的 telegram entry（Plan 03 添加）。因此 RED 阶段测试会在 Plan 03 完成后才 GREEN。Plan 02 先创建测试文件和 login-detect 实现，URL match 测试在 Plan 03 完成后验证。
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Login detect tests + URL match tests</name>
  <files>tests/unit/adapters/telegram-login.spec.ts, tests/unit/adapters/telegram-match.spec.ts</files>
  <read_first>
    tests/unit/adapters/slack-login-detect.spec.ts
    tests/unit/adapters/discord-match.spec.ts
    tests/unit/adapters/slack-match.spec.ts
    shared/adapters/slack-login-detect.ts
    .planning/phases/11-telegram-adapter/11-RESEARCH.md (Pattern 4, Anti-Pitfall 1)
    .planning/phases/11-telegram-adapter/11-CONTEXT.md (D-152)
  </read_first>
  <acceptance_criteria>
    - File `tests/unit/adapters/telegram-login.spec.ts` exists
    - File imports `detectLoginWall` from `@/shared/adapters/telegram-login-detect`
    - Contains tests for: normal editor (false), phone input (true), auth class (true), login class without editor (true), login class WITH editor (false — guard), empty body (false), stray contenteditable + login (true)
    - File `tests/unit/adapters/telegram-match.spec.ts` exists
    - File imports `findAdapter, detectPlatformId` from `@/shared/adapters/registry`
    - Contains valid URL tests and invalid URL tests for Telegram
    - `pnpm test:unit -- tests/unit/adapters/telegram-login.spec.ts` exits with failures (RED — import missing)
  </acceptance_criteria>
  <action>
    创建 `tests/unit/adapters/telegram-login.spec.ts`，参考 `slack-login-detect.spec.ts` 结构：

    ```typescript
    import { afterEach, beforeEach, describe, it, expect } from 'vitest';
    import { detectLoginWall } from '@/shared/adapters/telegram-login-detect';

    describe('Telegram login wall detection (TG-02)', () => {
      beforeEach(() => { document.body.innerHTML = ''; });
      afterEach(() => { document.body.innerHTML = ''; });

      it('returns false when chat editor is present', () => {
        document.body.innerHTML = `
          <div class="input-message-input" contenteditable="true" role="textbox" aria-label="Message">
            <br>
          </div>
        `;
        expect(detectLoginWall()).toBe(false);
      });

      it('returns true when phone input is present', () => {
        document.body.innerHTML = `
          <form>
            <input type="tel" name="phone" />
            <button type="submit">Next</button>
          </form>
        `;
        expect(detectLoginWall()).toBe(true);
      });

      it('returns true when auth class fragment is present', () => {
        document.body.innerHTML = `
          <div class="auth-page-abc">Sign in to Telegram</div>
        `;
        expect(detectLoginWall()).toBe(true);
      });

      it('returns true when login class fragment present and no editor', () => {
        document.body.innerHTML = `
          <div class="login-overlay-xyz">Redirecting...</div>
        `;
        expect(detectLoginWall()).toBe(true);
      });

      it('returns false when login class present but editor exists (guard)', () => {
        // Logged-in page may have elements with "login" class fragments.
        // The guard ensures we only match [class*="login"] when the editor is absent.
        document.body.innerHTML = `
          <div class="input-message-input" contenteditable="true" role="textbox" aria-label="Message">
            <br>
          </div>
          <div class="login-as-another-user">Switch account</div>
        `;
        expect(detectLoginWall()).toBe(false);
      });

      it('returns false on an empty document body', () => {
        expect(detectLoginWall()).toBe(false);
      });

      it('returns true even if stray contenteditable exists alongside login UI', () => {
        document.body.innerHTML = `
          <div contenteditable="true"></div>
          <input type="tel" name="phone" />
        `;
        expect(detectLoginWall()).toBe(true);
      });
    });
    ```

    创建 `tests/unit/adapters/telegram-match.spec.ts`，参考 `slack-match.spec.ts` 结构：

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { findAdapter, detectPlatformId } from '@/shared/adapters/registry';

    describe('adapters/telegram — match (TG-01)', () => {
      const validUrls = [
        'https://web.telegram.org/a/#123456',
        'https://web.telegram.org/a/chat/123',
      ];

      for (const url of validUrls) {
        it(`matches: ${url}`, () => {
          const adapter = findAdapter(url);
          expect(adapter).not.toBeUndefined();
          expect(adapter!.id).toBe('telegram');
        });
      }

      it('detectPlatformId returns telegram for valid URL', () => {
        expect(detectPlatformId('https://web.telegram.org/a/#123')).toBe('telegram');
      });

      const invalidUrls = [
        { url: 'https://web.telegram.org/z/#123', label: 'Web Z path' },
        { url: 'https://web.telegram.org/', label: 'root path' },
        { url: 'https://discord.com/channels/1/2', label: 'discord URL' },
        { url: 'not-a-url', label: 'malformed' },
        { url: '', label: 'empty' },
      ];

      for (const { url, label } of invalidUrls) {
        it(`does NOT match: ${label}`, () => {
          const adapter = findAdapter(url);
          const isTelegram = adapter?.id === 'telegram';
          expect(isTelegram).toBe(false);
        });
      }
    });
    ```

    注意：telegram-match.spec.ts 在 Plan 03 添加 registry entry 后才能 GREEN。Plan 02 先创建文件。
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/adapters/telegram-login.spec.ts 2>&1 | tail -3; echo "EXIT: $?"</automated>
  </verify>
  <done>两个测试文件存在。telegram-login 测试因 import 失败而 RED。telegram-match 测试因 registry 中无 telegram entry 而 fail（预期行为，Plan 03 完成后 GREEN）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement telegram-login-detect.ts</name>
  <files>shared/adapters/telegram-login-detect.ts</files>
  <read_first>
    shared/adapters/slack-login-detect.ts
    tests/unit/adapters/telegram-login.spec.ts
    .planning/phases/11-telegram-adapter/11-RESEARCH.md (Pattern 4, Anti-Pitfall 1)
    .planning/phases/11-telegram-adapter/11-CONTEXT.md (D-152)
  </read_first>
  <acceptance_criteria>
    - File `shared/adapters/telegram-login-detect.ts` exists
    - Exports `detectLoginWall` function
    - NO `chrome.*` imports
    - Uses 2 unconditional DOM selectors: `input[name="phone"], input[type="tel"]` and `[class*="auth"]`
    - `[class*="login"]` selector is guarded by `.input-message-input[contenteditable="true"]` absence check (only matches when `document.querySelector('.input-message-input[contenteditable="true"]')` is null)
    - `pnpm test:unit -- tests/unit/adapters/telegram-login.spec.ts` exits 0 (GREEN)
  </acceptance_criteria>
  <action>
    创建 `shared/adapters/telegram-login-detect.ts`，参考 `slack-login-detect.ts` 结构：

    ```typescript
    /**
     * Telegram login wall DOM detection (D-152).
     *
     * Lives in `shared/adapters/` so tests (jsdom) and the telegram
     * content-script bundle can import the same implementation. No
     * `chrome.*` APIs — pure DOM lookups.
     *
     * CRITICAL: Telegram Web K uses hash-based routing (`#/auth`).
     * URL-layer `loggedOutPathPatterns` only checks pathname, so
     * DOM-level detection is the primary defense.
     *
     * Detection markers:
     *   1. input[type="tel"], input[name="phone"] — phone number input
     *   2. [class*="auth"] — auth container class fragment
     *   3. [class*="login"] — login overlay class fragment
     *      GUARDED: only matches when .input-message-input is NOT present.
     *      Logged-in Telegram pages may contain elements with "login"
     *      class fragments. The guard prevents false positives while
     *      maintaining fail-safe behavior (if the editor exists, the
     *      user is logged in).
     */
    export function detectLoginWall(): boolean {
      // Unconditional markers — these only appear on the login page
      if (document.querySelector('input[name="phone"], input[type="tel"]')) return true;
      if (document.querySelector('[class*="auth"]')) return true;

      // Guarded marker — [class*="login"] is broad and may match
      // elements on logged-in pages. Only treat as login wall if
      // the Telegram editor (.input-message-input) is NOT present.
      if (!document.querySelector('.input-message-input[contenteditable="true"]')) {
        if (document.querySelector('[class*="login"]')) return true;
      }

      return false;
    }
    ```

    实现后运行测试确认 GREEN。
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/adapters/telegram-login.spec.ts --reporter=verbose</automated>
  </verify>
  <done>telegram-login-detect 测试全部 GREEN；detectLoginWall 检测 3 种 Telegram 登录标记（login class 有 .input-message-input guard），正常编辑器不误报</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM 环境 -> detectLoginWall | 运行在 Telegram 页面的 ISOLATED world，读取 live DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Denial of Service | detectLoginWall | accept | 纯 DOM querySelector 调用，无副作用；误报仅导致 NOT_LOGGED_IN 替代 INPUT_NOT_FOUND，用户体验略差但安全。login class guard 进一步降低已登录页面误报风险。 |
</threat_model>

<verification>
```bash
pnpm test:unit -- tests/unit/adapters/telegram-login.spec.ts --reporter=verbose
```
All login detect tests pass. telegram-match.spec.ts will pass after Plan 03 adds registry entry.
</verification>

<success_criteria>
- `detectLoginWall` 在 3 种登录标记场景返回 true，正常编辑器返回 false
- `[class*="login"]` 有 .input-message-input guard，已登录页面不会误报
- telegram-login 测试全部 GREEN
- telegram-match 测试文件就绪（等待 Plan 03 registry entry 后 GREEN）
</success_criteria>

<output>
After completion, create `.planning/phases/11-telegram-adapter/11-02-SUMMARY.md`
</output>
