---
phase: 10-slack-adapter
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/adapters/slack-format.ts
  - tests/unit/adapters/slack-format.spec.ts
autonomous: true
requirements:
  - SLK-03

must_haves:
  truths:
    - "composeSlackMrkdwn 输出正确的 mrkdwn 语法（bold 用 *text*，blockquote 用 >）"
    - "escapeSlackMentions 打断所有 Slack mention 模式（<!everyone>, <!here>, <@U123>, <@W123>, <#C123>）"
    - "字段排列顺序为 prompt -> *title* -> url -> description -> timestamp -> content"
    - "无 truncation 逻辑"
  artifacts:
    - path: "shared/adapters/slack-format.ts"
      provides: "Slack mrkdwn formatting + mention escaping"
      exports: ["composeSlackMrkdwn", "escapeSlackMentions"]
    - path: "tests/unit/adapters/slack-format.spec.ts"
      provides: "Unit tests for Slack formatting"
  key_links:
    - from: "shared/adapters/slack-format.ts"
      to: "entrypoints/slack.content.ts"
      via: "import { composeSlackMrkdwn, escapeSlackMentions }"
      pattern: "composeSlackMrkdwn"
---

<objective>
实现 Slack mrkdwn 格式化模块（D-128, D-129, D-130, D-131）。

Purpose: 为 Slack 适配器提供消息格式化和 mention escape 功能。纯函数模块，无 chrome.* / WXT 依赖，content script 和单元测试共用。
Output: `shared/adapters/slack-format.ts` + 完整测试覆盖
</objective>

<execution_context>
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seven/data/coding/projects/seven/web2chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-slack-adapter/10-CONTEXT.md
@.planning/phases/10-slack-adapter/10-RESEARCH.md
@.planning/phases/10-slack-adapter/10-PATTERNS.md

<interfaces>
<!-- Discord format module — structural template for Slack format module -->
From shared/adapters/discord-format.ts:
```typescript
export interface Snapshot {
  title: string;
  url: string;
  description: string;
  create_at: string;
  content: string;
}
export const DISCORD_CHAR_LIMIT = 2000;
export const TRUNCATION_SUFFIX = '\n...[truncated]';
export function escapeMentions(text: string): string;
export function composeDiscordMarkdown(payload: {
  prompt: string;
  snapshot: Snapshot;
  timestampLabel?: string;
}): string;
```
</interfaces>
</context>

<feature>
  <name>Slack mrkdwn formatting</name>
  <files>shared/adapters/slack-format.ts, tests/unit/adapters/slack-format.spec.ts</files>
  <behavior>
    composeSlackMrkdwn:
    - 字段排列: prompt -> *title* -> url -> description -> timestamp -> content (D-131)
    - bold 用 `*text*`（mrkdwn），不用 `**text**`
    - blockquote 用 `> text`（与 Discord 相同语法）
    - 空字段整体省略
    - 无 truncation（D-129: Slack 40K 字符限制）
    - escapeSlackMentions 应用于 prompt, title, description, content（不应用于 url, create_at）
    - 默认 timestampLabel 为 '采集时间:'（与 Discord 保持一致）

    escapeSlackMentions:
    - `<!everyone>` -> `<!​everyone>`（ZWS 插入 ! 之后）
    - `<!here>` -> `<!​here>`
    - `<!channel>` -> `<!​channel>`
    - `<@U123>` -> `<​@U123>`（ZWS 插入 < 之后）
    - `<@W123>` -> `<​@W123>`
    - `<#C123>` -> `<​#C123>`
    - `@everyone` (bare) -> `@​everyone`（ZWS 插入 @ 之后）
    - `@here` (bare) -> `@​here`
    - 不匹配 `@hereford`、`@everywhere`（word boundary）
    - 不匹配 `user@everyone.com`（lookbehind）
    - 正常文本不变

    Cases (input -> expected output):
    - Full snapshot with prompt -> "prompt\n\n*title*\n\nurl\n\n> desc\n\n> 采集时间: ts\n\ncontent"
    - Empty prompt -> starts with *title*
    - All fields empty -> empty string
    - Mention in title -> mention escaped, bold still applied
  </behavior>
  <implementation>
    参考 discord-format.ts 结构，创建 slack-format.ts：
    1. 复用 Snapshot 接口（从 discord-format.ts 复制或提取到 shared/types）
    2. escapeSlackMentions: 四条 regex 替换，ZWS 插入
    3. composeSlackMrkdwn: 与 composeDiscordMarkdown 结构对称，但:
       - bold 用 `*${safeTitle}*`
       - 无 DISCORD_CHAR_LIMIT / TRUNCATION_SUFFIX
       - 无 truncation 逻辑
    4. 直接从 discord-format.ts 复制 Snapshot 接口（避免跨模块依赖重构）
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Slack format failing tests</name>
  <files>tests/unit/adapters/slack-format.spec.ts</files>
  <read_first>
    tests/unit/adapters/discord-format.spec.ts
    shared/adapters/discord-format.ts
    .planning/phases/10-slack-adapter/10-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - File `tests/unit/adapters/slack-format.spec.ts` exists
    - File imports `composeSlackMrkdwn` and `escapeSlackMentions` from `@/shared/adapters/slack-format`
    - Contains `describe('adapters/slack — composeSlackMrkdwn')` with tests for: full formatting, empty fields, prompt omission, mrkdwn bold syntax
    - Contains `describe('adapters/slack — escapeSlackMentions')` with tests for: `<!everyone>`, `<!here>`, `<!channel>`, `<@U123>`, `<@W123>`, `<#C123>`, bare `@everyone`, bare `@here`, normal text unchanged, mixed mentions, word boundary protection
    - `pnpm test:unit -- tests/unit/adapters/slack-format.spec.ts` exits with failures (RED)
  </acceptance_criteria>
  <action>
    创建测试文件 `tests/unit/adapters/slack-format.spec.ts`，参考 `tests/unit/adapters/discord-format.spec.ts` 结构。

    测试结构:
    1. `describe('adapters/slack — composeSlackMrkdwn')`:
       - "formats prompt-first with *bold* title (mrkdwn)": 使用 fullSnapshot，验证 lines[0] 为 prompt，输出包含 `*Test Article*`（mrkdwn bold），不包含 `**Test Article**`，包含 url, `> A test description`, `> 采集时间:` prefix, content
       - "omits empty fields entirely": sparse snapshot 只有 title，输出为 `*Only Title*`
       - "omits prompt when empty": 空 prompt，输出以 `*Test Article*` 开头
       - "does NOT truncate long content (D-129)": 5000 字符 content，验证输出长度 > 2000（不做 truncation）

    2. `describe('adapters/slack — escapeSlackMentions')`:
       定义 `const ZWS = '​';`
       - `<!everyone>` -> `<!${ZWS}everyone>`
       - `<!here>` -> `<!${ZWS}here>`
       - `<!channel>` -> `<!${ZWS}channel>`
       - `<@U123456>` -> `<${ZWS}@U123456>`
       - `<@W123>` -> `<${ZWS}@W123>`
       - `<#C789>` -> `<${ZWS}#C789>`
       - bare `@everyone` -> `@${ZWS}everyone`
       - bare `@here` -> `@${ZWS}here`
       - normal text unchanged
       - mixed mentions all escaped
       - `@hereford` unchanged (no partial match)
       - `user@everyone.com` unchanged (lookbehind)
       - `@everywhere` unchanged (word boundary)

    Import from `@/shared/adapters/slack-format`（文件尚未存在，运行必然失败）。
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/adapters/slack-format.spec.ts 2>&1 | head -5; echo "EXIT: $?"</automated>
  </verify>
  <done>测试文件存在，所有测试因 import 失败而 RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement slack-format.ts</name>
  <files>shared/adapters/slack-format.ts</files>
  <read_first>
    shared/adapters/discord-format.ts
    tests/unit/adapters/slack-format.spec.ts
    .planning/phases/10-slack-adapter/10-RESEARCH.md (Code Examples section)
  </read_first>
  <acceptance_criteria>
    - File `shared/adapters/slack-format.ts` exists
    - Exports `composeSlackMrkdwn` and `escapeSlackMentions`
    - Exports `Snapshot` interface (identical to discord-format.ts)
    - NO `DISCORD_CHAR_LIMIT`, `TRUNCATION_SUFFIX`, or truncation logic
    - NO `chrome.*` or WXT imports
    - Bold uses `*text*` not `**text**`
    - `pnpm test:unit -- tests/unit/adapters/slack-format.spec.ts` exits 0 (all GREEN)
  </acceptance_criteria>
  <action>
    创建 `shared/adapters/slack-format.ts`，参考 `discord-format.ts` 结构但替换为 Slack mrkdwn 语法。

    结构:
    ```typescript
    /**
     * Slack message formatting (D-128, D-129, D-130, D-131).
     * Pure utility — no WXT or chrome.* imports.
     * Imported by both the adapter content script and unit tests.
     */

    export interface Snapshot {
      title: string;
      url: string;
      description: string;
      create_at: string;
      content: string;
    }

    // D-130: Break Slack mention patterns via ZWS insertion
    export function escapeSlackMentions(text: string): string {
      let result = text;
      // Break <!everyone>, <!here>, <!channel> with ZWS after !
      result = result.replace(/<!(everyone|here|channel)>/g, '<!​$1>');
      // Break <@U123>, <@W123> user mentions with ZWS after <
      result = result.replace(/<@([UW][A-Z0-9]+)>/g, '<​@$1>');
      // Break <#C123> channel mentions with ZWS after <
      result = result.replace(/<#([A-Z0-9]+)>/g, '<​#$1>');
      // Break bare @everyone / @here with ZWS after @
      result = result.replace(/(?<!\w)@(everyone|here)\b/g, '@​$1');
      return result;
    }

    // D-128, D-131: compose Slack mrkdwn. Field order matches Discord.
    // D-129: No truncation (Slack 40K char limit).
    export function composeSlackMrkdwn(payload: {
      prompt: string;
      snapshot: Snapshot;
      timestampLabel?: string;
    }): string {
      const { prompt, snapshot, timestampLabel = '采集时间:' } = payload;

      const safePrompt = prompt ? escapeSlackMentions(prompt) : '';
      const safeTitle = snapshot.title ? escapeSlackMentions(snapshot.title) : '';
      const safeDescription = snapshot.description ? escapeSlackMentions(snapshot.description) : '';
      const safeContent = snapshot.content ? escapeSlackMentions(snapshot.content) : '';

      const lines: string[] = [];
      if (safePrompt) lines.push(safePrompt, '');
      if (safeTitle) lines.push(`*${safeTitle}*`, '');  // mrkdwn bold: *text*
      if (snapshot.url) lines.push(snapshot.url, '');
      if (safeDescription) lines.push(`> ${safeDescription}`, '');
      if (snapshot.create_at) lines.push(`> ${timestampLabel} ${snapshot.create_at}`, '');
      if (safeContent) lines.push(safeContent);

      return lines.join('\n').trim();
    }
    ```

    实现后运行测试确认 GREEN。
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/adapters/slack-format.spec.ts --reporter=verbose</automated>
  </verify>
  <done>所有 slack-format 测试 GREEN；模块导出 composeSlackMrkdwn + escapeSlackMentions + Snapshot；无 truncation 逻辑</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 -> escapeSlackMentions | 用户提供的 prompt/title/description 可能包含 Slack mention 模式 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | escapeSlackMentions | mitigate | ZWS 插入打断所有已知 mention 模式，防止意外触发通知 |
| T-10-02 | Tampering | composeSlackMrkdwn | accept | 内容经 @mozilla/readability + dompurify 净化后才进入格式化；mrkdwn 无 HTML 渲染能力 |
</threat_model>

<verification>
```bash
pnpm test:unit -- tests/unit/adapters/slack-format.spec.ts --reporter=verbose
```
All tests pass.
</verification>

<success_criteria>
- `composeSlackMrkdwn` 输出 mrkdwn 格式（bold `*text*`，blockquote `> text`）
- `escapeSlackMentions` 对 12 种 Slack mention 模式均插入 ZWS 打断
- 字段排列顺序: prompt -> title -> url -> description -> timestamp -> content
- 无 truncation 逻辑
- 所有测试 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/10-slack-adapter/10-01-SUMMARY.md`
</output>
